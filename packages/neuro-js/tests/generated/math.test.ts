// Auto-generated by scripts/generate-tests.ts. Do not edit.
// Per-method routing & error-path tests for the `math` group.
/* eslint-disable import/no-relative-packages */
import { afterEach, describe, expect, test, vi } from 'vitest';
import { configureClient, neuro, resetClient } from '../../dist/index.js';

interface MethodCase {
  name: string;
  functionId: string;
  routedInput: Record<string, unknown>;
  nativeInput: Record<string, unknown>;
  invoke: (input: Record<string, unknown>) => Promise<unknown>;
}

const cases: MethodCase[] = [
  {
    name: 'abs',
    functionId: 'Math.abs',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.abs as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'acos',
    functionId: 'Math.acos',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.acos as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'acosh',
    functionId: 'Math.acosh',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.acosh as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'asin',
    functionId: 'Math.asin',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.asin as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'asinh',
    functionId: 'Math.asinh',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.asinh as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'atan',
    functionId: 'Math.atan',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.atan as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'atan2',
    functionId: 'Math.atan2',
    routedInput: { y: undefined, x: undefined, prompt: 'test' },
    nativeInput: { y: undefined, x: undefined },
    invoke: (input) => (neuro.math.atan2 as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'atanh',
    functionId: 'Math.atanh',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.atanh as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'cbrt',
    functionId: 'Math.cbrt',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.cbrt as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'ceil',
    functionId: 'Math.ceil',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.ceil as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'clz32',
    functionId: 'Math.clz32',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.clz32 as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'cos',
    functionId: 'Math.cos',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.cos as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'cosh',
    functionId: 'Math.cosh',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.cosh as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'exp',
    functionId: 'Math.exp',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.exp as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'expm1',
    functionId: 'Math.expm1',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.expm1 as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'f16round',
    functionId: 'Math.f16round',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.f16round as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'floor',
    functionId: 'Math.floor',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.floor as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'fround',
    functionId: 'Math.fround',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.fround as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'hypot',
    functionId: 'Math.hypot',
    routedInput: { values: [], prompt: 'test' },
    nativeInput: { values: [] },
    invoke: (input) => (neuro.math.hypot as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'imul',
    functionId: 'Math.imul',
    routedInput: { x: undefined, y: undefined, prompt: 'test' },
    nativeInput: { x: undefined, y: undefined },
    invoke: (input) => (neuro.math.imul as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'log',
    functionId: 'Math.log',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.log as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'log10',
    functionId: 'Math.log10',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.log10 as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'log1p',
    functionId: 'Math.log1p',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.log1p as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'log2',
    functionId: 'Math.log2',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.log2 as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'max',
    functionId: 'Math.max',
    routedInput: { values: [], prompt: 'test' },
    nativeInput: { values: [] },
    invoke: (input) => (neuro.math.max as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'min',
    functionId: 'Math.min',
    routedInput: { values: [], prompt: 'test' },
    nativeInput: { values: [] },
    invoke: (input) => (neuro.math.min as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'pow',
    functionId: 'Math.pow',
    routedInput: { x: undefined, y: undefined, prompt: 'test' },
    nativeInput: { x: undefined, y: undefined },
    invoke: (input) => (neuro.math.pow as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'random',
    functionId: 'Math.random',
    routedInput: { prompt: 'test' },
    nativeInput: {  },
    invoke: (input) => (neuro.math.random as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'round',
    functionId: 'Math.round',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.round as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'sign',
    functionId: 'Math.sign',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.sign as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'sin',
    functionId: 'Math.sin',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.sin as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'sinh',
    functionId: 'Math.sinh',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.sinh as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'sqrt',
    functionId: 'Math.sqrt',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.sqrt as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'tan',
    functionId: 'Math.tan',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.tan as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'tanh',
    functionId: 'Math.tanh',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.tanh as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'trunc',
    functionId: 'Math.trunc',
    routedInput: { x: undefined, prompt: 'test' },
    nativeInput: { x: undefined },
    invoke: (input) => (neuro.math.trunc as (i: Record<string, unknown>) => Promise<unknown>)(input),
  }
];

afterEach(() => {
  resetClient();
  vi.unstubAllGlobals();
  vi.restoreAllMocks();
});

describe('math routing', () => {
  test.each(cases)('$name routes to LLM when prompt is set', async ({ functionId, routedInput, invoke }) => {
    const fetchMock = vi.fn(
      async () =>
        new Response(JSON.stringify({ result: null }), {
          status: 200,
          headers: { 'content-type': 'application/json' },
        }),
    );
    vi.stubGlobal('fetch', fetchMock);
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await invoke(routedInput);
    expect(fetchMock).toHaveBeenCalledOnce();
    const sent = JSON.parse(String(fetchMock.mock.calls[0][1]?.body));
    expect(sent.functionId).toBe(functionId);
    expect(sent.prompt).toBe('test');
  });

  test.each(cases)('$name does not call the LLM when prompt is omitted', async ({ nativeInput, invoke }) => {
    const fetchMock = vi.fn();
    vi.stubGlobal('fetch', fetchMock);
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    // We do not assert the native return value (many methods need real
    // arguments to run); we only assert that fetch is never invoked.
    await invoke(nativeInput).catch(() => {
      /* native may throw with stub args; the routing decision is what
       * we are testing here. */
    });
    expect(fetchMock).not.toHaveBeenCalled();
  });

  test.each(cases)('$name treats prompt: "" as absent (no LLM call)', async ({ nativeInput, invoke }) => {
    const fetchMock = vi.fn();
    vi.stubGlobal('fetch', fetchMock);
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await invoke({ ...nativeInput, prompt: '' }).catch(() => {});
    expect(fetchMock).not.toHaveBeenCalled();
  });

  test.each(cases)('$name surfaces network outage as NeuroClientError', async ({ functionId, routedInput, invoke }) => {
    vi.stubGlobal('fetch', async () => {
      throw new Error('network down');
    });
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await expect(invoke(routedInput)).rejects.toThrow(/Proxy fetch failed/);
    // The thrown error includes the functionId for postmortems.
    await expect(invoke(routedInput)).rejects.toThrow(new RegExp(functionId.replace(/[.\\]/g, '\\$&')));
  });

  test.each(cases)('$name surfaces a 500 from the proxy', async ({ routedInput, invoke }) => {
    vi.stubGlobal(
      'fetch',
      async () => new Response('boom', { status: 500, statusText: 'Server Error' }),
    );
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await expect(invoke(routedInput)).rejects.toThrow(/Proxy.*responded 500/);
  });
});
