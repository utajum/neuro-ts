// Auto-generated by scripts/generate-tests.ts. Do not edit.
// Per-method routing & error-path tests for the `object` group.
/* eslint-disable import/no-relative-packages */
import { afterEach, describe, expect, test, vi } from 'vitest';
import { configureClient, neuro, resetClient } from '../../dist/index.js';

interface MethodCase {
  name: string;
  functionId: string;
  routedInput: Record<string, unknown>;
  nativeInput: Record<string, unknown>;
  invoke: (input: Record<string, unknown>) => Promise<unknown>;
}

const cases: MethodCase[] = [
  {
    name: 'assign',
    functionId: 'Object.assign',
    routedInput: { target: undefined, source: undefined, source1: undefined, source2: undefined, source3: undefined, sources: [], prompt: 'test' },
    nativeInput: { target: undefined, source: undefined, source1: undefined, source2: undefined, source3: undefined, sources: [] },
    invoke: (input) => (neuro.object.assign as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'create',
    functionId: 'Object.create',
    routedInput: { o: undefined, properties: undefined, prompt: 'test' },
    nativeInput: { o: undefined, properties: undefined },
    invoke: (input) => (neuro.object.create as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'defineProperties',
    functionId: 'Object.defineProperties',
    routedInput: { o: undefined, properties: undefined, prompt: 'test' },
    nativeInput: { o: undefined, properties: undefined },
    invoke: (input) => (neuro.object.defineProperties as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'defineProperty',
    functionId: 'Object.defineProperty',
    routedInput: { o: undefined, p: undefined, attributes: undefined, prompt: 'test' },
    nativeInput: { o: undefined, p: undefined, attributes: undefined },
    invoke: (input) => (neuro.object.defineProperty as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'entries',
    functionId: 'Object.entries',
    routedInput: { o: undefined, prompt: 'test' },
    nativeInput: { o: undefined },
    invoke: (input) => (neuro.object.entries as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'freeze',
    functionId: 'Object.freeze',
    routedInput: { f: undefined, o: undefined, prompt: 'test' },
    nativeInput: { f: undefined, o: undefined },
    invoke: (input) => (neuro.object.freeze as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'fromEntries',
    functionId: 'Object.fromEntries',
    routedInput: { entries: undefined, prompt: 'test' },
    nativeInput: { entries: undefined },
    invoke: (input) => (neuro.object.fromEntries as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'getOwnPropertyDescriptor',
    functionId: 'Object.getOwnPropertyDescriptor',
    routedInput: { o: undefined, p: undefined, prompt: 'test' },
    nativeInput: { o: undefined, p: undefined },
    invoke: (input) => (neuro.object.getOwnPropertyDescriptor as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'getOwnPropertyDescriptors',
    functionId: 'Object.getOwnPropertyDescriptors',
    routedInput: { o: undefined, prompt: 'test' },
    nativeInput: { o: undefined },
    invoke: (input) => (neuro.object.getOwnPropertyDescriptors as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'getOwnPropertyNames',
    functionId: 'Object.getOwnPropertyNames',
    routedInput: { o: undefined, prompt: 'test' },
    nativeInput: { o: undefined },
    invoke: (input) => (neuro.object.getOwnPropertyNames as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'getOwnPropertySymbols',
    functionId: 'Object.getOwnPropertySymbols',
    routedInput: { o: undefined, prompt: 'test' },
    nativeInput: { o: undefined },
    invoke: (input) => (neuro.object.getOwnPropertySymbols as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'getPrototypeOf',
    functionId: 'Object.getPrototypeOf',
    routedInput: { o: undefined, prompt: 'test' },
    nativeInput: { o: undefined },
    invoke: (input) => (neuro.object.getPrototypeOf as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'groupBy',
    functionId: 'Object.groupBy',
    routedInput: { items: undefined, keySelector: undefined, prompt: 'test' },
    nativeInput: { items: undefined, keySelector: undefined },
    invoke: (input) => (neuro.object.groupBy as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'hasOwn',
    functionId: 'Object.hasOwn',
    routedInput: { o: undefined, v: undefined, prompt: 'test' },
    nativeInput: { o: undefined, v: undefined },
    invoke: (input) => (neuro.object.hasOwn as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'is',
    functionId: 'Object.is',
    routedInput: { value1: undefined, value2: undefined, prompt: 'test' },
    nativeInput: { value1: undefined, value2: undefined },
    invoke: (input) => (neuro.object.is as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'isExtensible',
    functionId: 'Object.isExtensible',
    routedInput: { o: undefined, prompt: 'test' },
    nativeInput: { o: undefined },
    invoke: (input) => (neuro.object.isExtensible as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'isFrozen',
    functionId: 'Object.isFrozen',
    routedInput: { o: undefined, prompt: 'test' },
    nativeInput: { o: undefined },
    invoke: (input) => (neuro.object.isFrozen as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'isSealed',
    functionId: 'Object.isSealed',
    routedInput: { o: undefined, prompt: 'test' },
    nativeInput: { o: undefined },
    invoke: (input) => (neuro.object.isSealed as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'keys',
    functionId: 'Object.keys',
    routedInput: { o: undefined, prompt: 'test' },
    nativeInput: { o: undefined },
    invoke: (input) => (neuro.object.keys as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'preventExtensions',
    functionId: 'Object.preventExtensions',
    routedInput: { o: undefined, prompt: 'test' },
    nativeInput: { o: undefined },
    invoke: (input) => (neuro.object.preventExtensions as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'seal',
    functionId: 'Object.seal',
    routedInput: { o: undefined, prompt: 'test' },
    nativeInput: { o: undefined },
    invoke: (input) => (neuro.object.seal as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'setPrototypeOf',
    functionId: 'Object.setPrototypeOf',
    routedInput: { o: undefined, proto: undefined, prompt: 'test' },
    nativeInput: { o: undefined, proto: undefined },
    invoke: (input) => (neuro.object.setPrototypeOf as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'values',
    functionId: 'Object.values',
    routedInput: { o: undefined, prompt: 'test' },
    nativeInput: { o: undefined },
    invoke: (input) => (neuro.object.values as (i: Record<string, unknown>) => Promise<unknown>)(input),
  }
];

afterEach(() => {
  resetClient();
  vi.unstubAllGlobals();
  vi.restoreAllMocks();
});

describe('object routing', () => {
  test.each(cases)('$name routes to LLM when prompt is set', async ({ functionId, routedInput, invoke }) => {
    const fetchMock = vi.fn(
      async () =>
        new Response(JSON.stringify({ result: null }), {
          status: 200,
          headers: { 'content-type': 'application/json' },
        }),
    );
    vi.stubGlobal('fetch', fetchMock);
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await invoke(routedInput);
    expect(fetchMock).toHaveBeenCalledOnce();
    const sent = JSON.parse(String(fetchMock.mock.calls[0][1]?.body));
    expect(sent.functionId).toBe(functionId);
    expect(sent.prompt).toBe('test');
  });

  test.each(cases)('$name does not call the LLM when prompt is omitted', async ({ nativeInput, invoke }) => {
    const fetchMock = vi.fn();
    vi.stubGlobal('fetch', fetchMock);
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    // We do not assert the native return value (many methods need real
    // arguments to run); we only assert that fetch is never invoked.
    await invoke(nativeInput).catch(() => {
      /* native may throw with stub args; the routing decision is what
       * we are testing here. */
    });
    expect(fetchMock).not.toHaveBeenCalled();
  });

  test.each(cases)('$name treats prompt: "" as absent (no LLM call)', async ({ nativeInput, invoke }) => {
    const fetchMock = vi.fn();
    vi.stubGlobal('fetch', fetchMock);
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await invoke({ ...nativeInput, prompt: '' }).catch(() => {});
    expect(fetchMock).not.toHaveBeenCalled();
  });

  test.each(cases)('$name surfaces network outage as NeuroClientError', async ({ functionId, routedInput, invoke }) => {
    vi.stubGlobal('fetch', async () => {
      throw new Error('network down');
    });
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await expect(invoke(routedInput)).rejects.toThrow(/Proxy fetch failed/);
    // The thrown error includes the functionId for postmortems.
    await expect(invoke(routedInput)).rejects.toThrow(new RegExp(functionId.replace(/[.\\]/g, '\\$&')));
  });

  test.each(cases)('$name surfaces a 500 from the proxy', async ({ routedInput, invoke }) => {
    vi.stubGlobal(
      'fetch',
      async () => new Response('boom', { status: 500, statusText: 'Server Error' }),
    );
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await expect(invoke(routedInput)).rejects.toThrow(/Proxy.*responded 500/);
  });
});
