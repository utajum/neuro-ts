// Auto-generated by scripts/generate-tests.ts. Do not edit.
// Per-method routing & error-path tests for the `set` group.
/* eslint-disable import/no-relative-packages */
import { afterEach, describe, expect, test, vi } from 'vitest';
import { configureClient, neuro, resetClient } from '../../dist/index.js';

interface MethodCase {
  name: string;
  functionId: string;
  routedInput: Record<string, unknown>;
  nativeInput: Record<string, unknown>;
  invoke: (input: Record<string, unknown>) => Promise<unknown>;
}

const cases: MethodCase[] = [
  {
    name: 'add',
    functionId: 'Set.prototype.add',
    routedInput: { set: undefined, value: undefined, prompt: 'test' },
    nativeInput: { set: undefined, value: undefined },
    invoke: (input) => (neuro.set.add as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'clear',
    functionId: 'Set.prototype.clear',
    routedInput: { set: undefined, prompt: 'test' },
    nativeInput: { set: undefined },
    invoke: (input) => (neuro.set.clear as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'delete',
    functionId: 'Set.prototype.delete',
    routedInput: { set: undefined, value: undefined, prompt: 'test' },
    nativeInput: { set: undefined, value: undefined },
    invoke: (input) => (neuro.set.delete as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'difference',
    functionId: 'Set.prototype.difference',
    routedInput: { set: undefined, other: undefined, prompt: 'test' },
    nativeInput: { set: undefined, other: undefined },
    invoke: (input) => (neuro.set.difference as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'entries',
    functionId: 'Set.prototype.entries',
    routedInput: { set: undefined, prompt: 'test' },
    nativeInput: { set: undefined },
    invoke: (input) => (neuro.set.entries as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'forEach',
    functionId: 'Set.prototype.forEach',
    routedInput: { set: undefined, callbackfn: undefined, thisArg: undefined, prompt: 'test' },
    nativeInput: { set: undefined, callbackfn: undefined, thisArg: undefined },
    invoke: (input) => (neuro.set.forEach as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'has',
    functionId: 'Set.prototype.has',
    routedInput: { set: undefined, value: undefined, prompt: 'test' },
    nativeInput: { set: undefined, value: undefined },
    invoke: (input) => (neuro.set.has as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'intersection',
    functionId: 'Set.prototype.intersection',
    routedInput: { set: undefined, other: undefined, prompt: 'test' },
    nativeInput: { set: undefined, other: undefined },
    invoke: (input) => (neuro.set.intersection as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'isDisjointFrom',
    functionId: 'Set.prototype.isDisjointFrom',
    routedInput: { set: undefined, other: undefined, prompt: 'test' },
    nativeInput: { set: undefined, other: undefined },
    invoke: (input) => (neuro.set.isDisjointFrom as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'isSubsetOf',
    functionId: 'Set.prototype.isSubsetOf',
    routedInput: { set: undefined, other: undefined, prompt: 'test' },
    nativeInput: { set: undefined, other: undefined },
    invoke: (input) => (neuro.set.isSubsetOf as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'isSupersetOf',
    functionId: 'Set.prototype.isSupersetOf',
    routedInput: { set: undefined, other: undefined, prompt: 'test' },
    nativeInput: { set: undefined, other: undefined },
    invoke: (input) => (neuro.set.isSupersetOf as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'keys',
    functionId: 'Set.prototype.keys',
    routedInput: { set: undefined, prompt: 'test' },
    nativeInput: { set: undefined },
    invoke: (input) => (neuro.set.keys as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'symmetricDifference',
    functionId: 'Set.prototype.symmetricDifference',
    routedInput: { set: undefined, other: undefined, prompt: 'test' },
    nativeInput: { set: undefined, other: undefined },
    invoke: (input) => (neuro.set.symmetricDifference as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'union',
    functionId: 'Set.prototype.union',
    routedInput: { set: undefined, other: undefined, prompt: 'test' },
    nativeInput: { set: undefined, other: undefined },
    invoke: (input) => (neuro.set.union as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'values',
    functionId: 'Set.prototype.values',
    routedInput: { set: undefined, prompt: 'test' },
    nativeInput: { set: undefined },
    invoke: (input) => (neuro.set.values as (i: Record<string, unknown>) => Promise<unknown>)(input),
  }
];

afterEach(() => {
  resetClient();
  vi.unstubAllGlobals();
  vi.restoreAllMocks();
});

describe('set routing', () => {
  test.each(cases)('$name routes to LLM when prompt is set', async ({ functionId, routedInput, invoke }) => {
    const fetchMock = vi.fn(
      async () =>
        new Response(JSON.stringify({ result: null }), {
          status: 200,
          headers: { 'content-type': 'application/json' },
        }),
    );
    vi.stubGlobal('fetch', fetchMock);
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await invoke(routedInput);
    expect(fetchMock).toHaveBeenCalledOnce();
    const sent = JSON.parse(String(fetchMock.mock.calls[0][1]?.body));
    expect(sent.functionId).toBe(functionId);
    expect(sent.prompt).toBe('test');
  });

  test.each(cases)('$name does not call the LLM when prompt is omitted', async ({ nativeInput, invoke }) => {
    const fetchMock = vi.fn();
    vi.stubGlobal('fetch', fetchMock);
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    // We do not assert the native return value (many methods need real
    // arguments to run); we only assert that fetch is never invoked.
    await invoke(nativeInput).catch(() => {
      /* native may throw with stub args; the routing decision is what
       * we are testing here. */
    });
    expect(fetchMock).not.toHaveBeenCalled();
  });

  test.each(cases)('$name treats prompt: "" as absent (no LLM call)', async ({ nativeInput, invoke }) => {
    const fetchMock = vi.fn();
    vi.stubGlobal('fetch', fetchMock);
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await invoke({ ...nativeInput, prompt: '' }).catch(() => {});
    expect(fetchMock).not.toHaveBeenCalled();
  });

  test.each(cases)('$name surfaces network outage as NeuroClientError', async ({ functionId, routedInput, invoke }) => {
    vi.stubGlobal('fetch', async () => {
      throw new Error('network down');
    });
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await expect(invoke(routedInput)).rejects.toThrow(/Proxy fetch failed/);
    // The thrown error includes the functionId for postmortems.
    await expect(invoke(routedInput)).rejects.toThrow(new RegExp(functionId.replace(/[.\\]/g, '\\$&')));
  });

  test.each(cases)('$name surfaces a 500 from the proxy', async ({ routedInput, invoke }) => {
    vi.stubGlobal(
      'fetch',
      async () => new Response('boom', { status: 500, statusText: 'Server Error' }),
    );
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await expect(invoke(routedInput)).rejects.toThrow(/Proxy.*responded 500/);
  });
});


describe('set native values', () => {
  test('add native fallback returns correct value', async () => {
    const result = await (neuro.set.add as (i: Record<string, unknown>) => Promise<unknown>)({ set: new Set([1, 2, 3]), value: 4 });
    expect(Array.from(result as any as any).sort((a: any, b: any) => a - b)).toEqual([1, 2, 3, 4]);
  });

  test('clear native fallback returns undefined (void)', async () => {
    const result = await (neuro.set.clear as (i: Record<string, unknown>) => Promise<unknown>)({ set: new Set([1, 2, 3]) });
    expect(result).toBeUndefined();
  });

  test('delete native fallback matches native built-in', async () => {
    const result = await (neuro.set.delete as (i: Record<string, unknown>) => Promise<unknown>)({ set: new Set([1, 2, 3]), value: 2 });
    expect(result).toBe(new Set([1, 2, 3]).delete(2));
  });

  test.runIf('difference' in Set.prototype)('difference native fallback returns correct value', async () => {
    const result = await (neuro.set.difference as (i: Record<string, unknown>) => Promise<unknown>)({ set: new Set([1, 2, 3]), other: new Set([2, 3, 4]) });
    expect(Array.from(result as any).sort((a,b)=>a-b)).toEqual([1]);
  });

  test('entries native fallback iterator matches native', async () => {
    const result = await (neuro.set.entries as (i: Record<string, unknown>) => Promise<unknown>)({ set: new Set([1, 2]) });
    const native = new Set([1, 2]).entries();
    expect(Array.from(result as Iterable<unknown>)).toEqual(Array.from(native as Iterable<unknown>));
  });

  test('forEach native fallback returns undefined (void)', async () => {
    const result = await (neuro.set.forEach as (i: Record<string, unknown>) => Promise<unknown>)({ set: new Set([1, 2]), callbackfn: () => {} });
    expect(result).toBeUndefined();
  });

  test('has native fallback matches native built-in', async () => {
    const result = await (neuro.set.has as (i: Record<string, unknown>) => Promise<unknown>)({ set: new Set([1, 2, 3]), value: 2 });
    expect(result).toBe(new Set([1, 2, 3]).has(2));
  });

  test.runIf('intersection' in Set.prototype)('intersection native fallback returns correct value', async () => {
    const result = await (neuro.set.intersection as (i: Record<string, unknown>) => Promise<unknown>)({ set: new Set([1, 2, 3]), other: new Set([2, 3, 4]) });
    expect(Array.from(result as any).sort((a,b)=>a-b)).toEqual([2, 3]);
  });

  test.runIf('isDisjointFrom' in Set.prototype)('isDisjointFrom native fallback matches native built-in', async () => {
    const result = await (neuro.set.isDisjointFrom as (i: Record<string, unknown>) => Promise<unknown>)({ set: new Set([1, 2]), other: new Set([3, 4]) });
    expect(result).toBe(new Set([1, 2]).isDisjointFrom(new Set([3, 4])));
  });

  test.runIf('isSubsetOf' in Set.prototype)('isSubsetOf native fallback matches native built-in', async () => {
    const result = await (neuro.set.isSubsetOf as (i: Record<string, unknown>) => Promise<unknown>)({ set: new Set([1, 2]), other: new Set([1, 2, 3]) });
    expect(result).toBe(new Set([1, 2]).isSubsetOf(new Set([1, 2, 3])));
  });

  test.runIf('isSupersetOf' in Set.prototype)('isSupersetOf native fallback matches native built-in', async () => {
    const result = await (neuro.set.isSupersetOf as (i: Record<string, unknown>) => Promise<unknown>)({ set: new Set([1, 2, 3]), other: new Set([1, 2]) });
    expect(result).toBe(new Set([1, 2, 3]).isSupersetOf(new Set([1, 2])  ));
  });

  test('keys native fallback iterator matches native', async () => {
    const result = await (neuro.set.keys as (i: Record<string, unknown>) => Promise<unknown>)({ set: new Set([1, 2]) });
    const native = new Set([1, 2]).keys();
    expect(Array.from(result as Iterable<unknown>)).toEqual(Array.from(native as Iterable<unknown>));
  });

  test.runIf('symmetricDifference' in Set.prototype)('symmetricDifference native fallback returns correct value', async () => {
    const result = await (neuro.set.symmetricDifference as (i: Record<string, unknown>) => Promise<unknown>)({ set: new Set([1, 2, 3]), other: new Set([2, 3, 4]) });
    expect(Array.from(result as any).sort((a,b)=>a-b)).toEqual([1, 4]);
  });

  test.runIf('union' in Set.prototype)('union native fallback returns correct value', async () => {
    const result = await (neuro.set.union as (i: Record<string, unknown>) => Promise<unknown>)({ set: new Set([1, 2]), other: new Set([3, 4]) });
    expect(Array.from(result as any).sort((a,b)=>a-b)).toEqual([1, 2, 3, 4]);
  });

  test('values native fallback iterator matches native', async () => {
    const result = await (neuro.set.values as (i: Record<string, unknown>) => Promise<unknown>)({ set: new Set([1, 2]) });
    const native = new Set([1, 2]).values();
    expect(Array.from(result as Iterable<unknown>)).toEqual(Array.from(native as Iterable<unknown>));
  });
});
