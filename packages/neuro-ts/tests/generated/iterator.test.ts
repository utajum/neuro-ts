// Auto-generated by scripts/generate-tests.ts. Do not edit.
// Per-method routing & error-path tests for the `iterator` group.
/* eslint-disable import/no-relative-packages */
import { afterEach, describe, expect, test, vi } from 'vitest';
import { configureClient, neuro, resetClient } from '../../dist/index.js';

interface MethodCase {
  name: string;
  functionId: string;
  routedInput: Record<string, unknown>;
  nativeInput: Record<string, unknown>;
  invoke: (input: Record<string, unknown>) => Promise<unknown>;
}

const cases: MethodCase[] = [
  {
    name: 'drop',
    functionId: 'Iterator.prototype.drop',
    routedInput: { iterator: undefined, count: undefined, prompt: 'test' },
    nativeInput: { iterator: undefined, count: undefined },
    invoke: (input) => (neuro.iterator.drop as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'every',
    functionId: 'Iterator.prototype.every',
    routedInput: { iterator: undefined, predicate: undefined, prompt: 'test' },
    nativeInput: { iterator: undefined, predicate: undefined },
    invoke: (input) => (neuro.iterator.every as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'filter',
    functionId: 'Iterator.prototype.filter',
    routedInput: { iterator: undefined, predicate: undefined, prompt: 'test' },
    nativeInput: { iterator: undefined, predicate: undefined },
    invoke: (input) => (neuro.iterator.filter as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'find',
    functionId: 'Iterator.prototype.find',
    routedInput: { iterator: undefined, predicate: undefined, prompt: 'test' },
    nativeInput: { iterator: undefined, predicate: undefined },
    invoke: (input) => (neuro.iterator.find as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'flatMap',
    functionId: 'Iterator.prototype.flatMap',
    routedInput: { iterator: undefined, callback: undefined, prompt: 'test' },
    nativeInput: { iterator: undefined, callback: undefined },
    invoke: (input) => (neuro.iterator.flatMap as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'forEach',
    functionId: 'Iterator.prototype.forEach',
    routedInput: { iterator: undefined, callbackfn: undefined, prompt: 'test' },
    nativeInput: { iterator: undefined, callbackfn: undefined },
    invoke: (input) => (neuro.iterator.forEach as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'from',
    functionId: 'Iterator.from',
    routedInput: { value: undefined, prompt: 'test' },
    nativeInput: { value: undefined },
    invoke: (input) => (neuro.iterator.from as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'map',
    functionId: 'Iterator.prototype.map',
    routedInput: { iterator: undefined, callbackfn: undefined, prompt: 'test' },
    nativeInput: { iterator: undefined, callbackfn: undefined },
    invoke: (input) => (neuro.iterator.map as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'next',
    functionId: 'Iterator.prototype.next',
    routedInput: { iterator: undefined, __0: [], prompt: 'test' },
    nativeInput: { iterator: undefined, __0: [] },
    invoke: (input) => (neuro.iterator.next as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'reduce',
    functionId: 'Iterator.prototype.reduce',
    routedInput: { iterator: undefined, callbackfn: undefined, initialValue: undefined, prompt: 'test' },
    nativeInput: { iterator: undefined, callbackfn: undefined, initialValue: undefined },
    invoke: (input) => (neuro.iterator.reduce as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'return',
    functionId: 'Iterator.prototype.return',
    routedInput: { iterator: undefined, value: undefined, prompt: 'test' },
    nativeInput: { iterator: undefined, value: undefined },
    invoke: (input) => (neuro.iterator.return as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'some',
    functionId: 'Iterator.prototype.some',
    routedInput: { iterator: undefined, predicate: undefined, prompt: 'test' },
    nativeInput: { iterator: undefined, predicate: undefined },
    invoke: (input) => (neuro.iterator.some as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'take',
    functionId: 'Iterator.prototype.take',
    routedInput: { iterator: undefined, limit: undefined, prompt: 'test' },
    nativeInput: { iterator: undefined, limit: undefined },
    invoke: (input) => (neuro.iterator.take as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'throw',
    functionId: 'Iterator.prototype.throw',
    routedInput: { iterator: undefined, e: undefined, prompt: 'test' },
    nativeInput: { iterator: undefined, e: undefined },
    invoke: (input) => (neuro.iterator.throw as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'toArray',
    functionId: 'Iterator.prototype.toArray',
    routedInput: { iterator: undefined, prompt: 'test' },
    nativeInput: { iterator: undefined },
    invoke: (input) => (neuro.iterator.toArray as (i: Record<string, unknown>) => Promise<unknown>)(input),
  }
];

afterEach(() => {
  resetClient();
  vi.unstubAllGlobals();
  vi.restoreAllMocks();
});

describe('iterator routing', () => {
  test.each(cases)('$name routes to LLM when prompt is set', async ({ functionId, routedInput, invoke }) => {
    const fetchMock = vi.fn(
      async () =>
        new Response(JSON.stringify({ result: null }), {
          status: 200,
          headers: { 'content-type': 'application/json' },
        }),
    );
    vi.stubGlobal('fetch', fetchMock);
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await invoke(routedInput);
    expect(fetchMock).toHaveBeenCalledOnce();
    const sent = JSON.parse(String(fetchMock.mock.calls[0][1]?.body));
    expect(sent.functionId).toBe(functionId);
    expect(sent.prompt).toBe('test');
  });

  test.each(cases)('$name does not call the LLM when prompt is omitted', async ({ nativeInput, invoke }) => {
    const fetchMock = vi.fn();
    vi.stubGlobal('fetch', fetchMock);
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    // We do not assert the native return value (many methods need real
    // arguments to run); we only assert that fetch is never invoked.
    await invoke(nativeInput).catch(() => {
      /* native may throw with stub args; the routing decision is what
       * we are testing here. */
    });
    expect(fetchMock).not.toHaveBeenCalled();
  });

  test.each(cases)('$name treats prompt: "" as absent (no LLM call)', async ({ nativeInput, invoke }) => {
    const fetchMock = vi.fn();
    vi.stubGlobal('fetch', fetchMock);
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await invoke({ ...nativeInput, prompt: '' }).catch(() => {});
    expect(fetchMock).not.toHaveBeenCalled();
  });

  test.each(cases)('$name surfaces network outage as NeuroClientError', async ({ functionId, routedInput, invoke }) => {
    vi.stubGlobal('fetch', async () => {
      throw new Error('network down');
    });
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await expect(invoke(routedInput)).rejects.toThrow(/Proxy fetch failed/);
    // The thrown error includes the functionId for postmortems.
    await expect(invoke(routedInput)).rejects.toThrow(new RegExp(functionId.replace(/[.\\]/g, '\\$&')));
  });

  test.each(cases)('$name surfaces a 500 from the proxy', async ({ routedInput, invoke }) => {
    vi.stubGlobal(
      'fetch',
      async () => new Response('boom', { status: 500, statusText: 'Server Error' }),
    );
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await expect(invoke(routedInput)).rejects.toThrow(/Proxy.*responded 500/);
  });
});


describe('iterator native values', () => {
  test.skip('drop native fallback does not throw', async () => { // group 'iterator' not handled
    await expect((neuro.iterator.drop as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('every native fallback does not throw', async () => { // group 'iterator' not handled
    await expect((neuro.iterator.every as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('filter native fallback does not throw', async () => { // group 'iterator' not handled
    await expect((neuro.iterator.filter as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('find native fallback does not throw', async () => { // group 'iterator' not handled
    await expect((neuro.iterator.find as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('flatMap native fallback does not throw', async () => { // group 'iterator' not handled
    await expect((neuro.iterator.flatMap as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('forEach native fallback does not throw', async () => { // group 'iterator' not handled
    await expect((neuro.iterator.forEach as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('from native fallback does not throw', async () => { // group 'iterator' not handled
    await expect((neuro.iterator.from as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('map native fallback does not throw', async () => { // group 'iterator' not handled
    await expect((neuro.iterator.map as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('next native fallback does not throw', async () => { // group 'iterator' not handled
    await expect((neuro.iterator.next as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('reduce native fallback does not throw', async () => { // group 'iterator' not handled
    await expect((neuro.iterator.reduce as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('return native fallback does not throw', async () => { // group 'iterator' not handled
    await expect((neuro.iterator.return as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('some native fallback does not throw', async () => { // group 'iterator' not handled
    await expect((neuro.iterator.some as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('take native fallback does not throw', async () => { // group 'iterator' not handled
    await expect((neuro.iterator.take as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('throw native fallback does not throw', async () => { // group 'iterator' not handled
    await expect((neuro.iterator.throw as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('toArray native fallback does not throw', async () => { // group 'iterator' not handled
    await expect((neuro.iterator.toArray as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });
});
