// Auto-generated by scripts/generate-tests.ts. Do not edit.
// Per-method routing & error-path tests for the `uint8Array` group.
/* eslint-disable import/no-relative-packages */
import { afterEach, describe, expect, test, vi } from 'vitest';
import { configureClient, neuro, resetClient } from '../../dist/index.js';

interface MethodCase {
  name: string;
  functionId: string;
  routedInput: Record<string, unknown>;
  nativeInput: Record<string, unknown>;
  invoke: (input: Record<string, unknown>) => Promise<unknown>;
}

const cases: MethodCase[] = [
  {
    name: 'at',
    functionId: 'Uint8Array.prototype.at',
    routedInput: { uint8Array: undefined, index: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, index: undefined },
    invoke: (input) => (neuro.uint8Array.at as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'copyWithin',
    functionId: 'Uint8Array.prototype.copyWithin',
    routedInput: { uint8Array: undefined, target: undefined, start: undefined, end: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, target: undefined, start: undefined, end: undefined },
    invoke: (input) => (neuro.uint8Array.copyWithin as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'entries',
    functionId: 'Uint8Array.prototype.entries',
    routedInput: { uint8Array: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined },
    invoke: (input) => (neuro.uint8Array.entries as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'every',
    functionId: 'Uint8Array.prototype.every',
    routedInput: { uint8Array: undefined, predicate: undefined, thisArg: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, predicate: undefined, thisArg: undefined },
    invoke: (input) => (neuro.uint8Array.every as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'fill',
    functionId: 'Uint8Array.prototype.fill',
    routedInput: { uint8Array: undefined, value: undefined, start: undefined, end: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, value: undefined, start: undefined, end: undefined },
    invoke: (input) => (neuro.uint8Array.fill as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'filter',
    functionId: 'Uint8Array.prototype.filter',
    routedInput: { uint8Array: undefined, predicate: undefined, thisArg: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, predicate: undefined, thisArg: undefined },
    invoke: (input) => (neuro.uint8Array.filter as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'find',
    functionId: 'Uint8Array.prototype.find',
    routedInput: { uint8Array: undefined, predicate: undefined, thisArg: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, predicate: undefined, thisArg: undefined },
    invoke: (input) => (neuro.uint8Array.find as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'findIndex',
    functionId: 'Uint8Array.prototype.findIndex',
    routedInput: { uint8Array: undefined, predicate: undefined, thisArg: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, predicate: undefined, thisArg: undefined },
    invoke: (input) => (neuro.uint8Array.findIndex as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'findLast',
    functionId: 'Uint8Array.prototype.findLast',
    routedInput: { uint8Array: undefined, predicate: undefined, thisArg: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, predicate: undefined, thisArg: undefined },
    invoke: (input) => (neuro.uint8Array.findLast as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'findLastIndex',
    functionId: 'Uint8Array.prototype.findLastIndex',
    routedInput: { uint8Array: undefined, predicate: undefined, thisArg: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, predicate: undefined, thisArg: undefined },
    invoke: (input) => (neuro.uint8Array.findLastIndex as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'forEach',
    functionId: 'Uint8Array.prototype.forEach',
    routedInput: { uint8Array: undefined, callbackfn: undefined, thisArg: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, callbackfn: undefined, thisArg: undefined },
    invoke: (input) => (neuro.uint8Array.forEach as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'from',
    functionId: 'Uint8Array.from',
    routedInput: { arrayLike: undefined, mapfn: undefined, thisArg: undefined, elements: undefined, prompt: 'test' },
    nativeInput: { arrayLike: undefined, mapfn: undefined, thisArg: undefined, elements: undefined },
    invoke: (input) => (neuro.uint8Array.from as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'fromBase64',
    functionId: 'Uint8Array.fromBase64',
    routedInput: { string: undefined, options: undefined, prompt: 'test' },
    nativeInput: { string: undefined, options: undefined },
    invoke: (input) => (neuro.uint8Array.fromBase64 as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'fromHex',
    functionId: 'Uint8Array.fromHex',
    routedInput: { string: undefined, prompt: 'test' },
    nativeInput: { string: undefined },
    invoke: (input) => (neuro.uint8Array.fromHex as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'includes',
    functionId: 'Uint8Array.prototype.includes',
    routedInput: { uint8Array: undefined, searchElement: undefined, fromIndex: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, searchElement: undefined, fromIndex: undefined },
    invoke: (input) => (neuro.uint8Array.includes as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'indexOf',
    functionId: 'Uint8Array.prototype.indexOf',
    routedInput: { uint8Array: undefined, searchElement: undefined, fromIndex: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, searchElement: undefined, fromIndex: undefined },
    invoke: (input) => (neuro.uint8Array.indexOf as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'join',
    functionId: 'Uint8Array.prototype.join',
    routedInput: { uint8Array: undefined, separator: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, separator: undefined },
    invoke: (input) => (neuro.uint8Array.join as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'keys',
    functionId: 'Uint8Array.prototype.keys',
    routedInput: { uint8Array: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined },
    invoke: (input) => (neuro.uint8Array.keys as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'lastIndexOf',
    functionId: 'Uint8Array.prototype.lastIndexOf',
    routedInput: { uint8Array: undefined, searchElement: undefined, fromIndex: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, searchElement: undefined, fromIndex: undefined },
    invoke: (input) => (neuro.uint8Array.lastIndexOf as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'map',
    functionId: 'Uint8Array.prototype.map',
    routedInput: { uint8Array: undefined, callbackfn: undefined, thisArg: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, callbackfn: undefined, thisArg: undefined },
    invoke: (input) => (neuro.uint8Array.map as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'of',
    functionId: 'Uint8Array.of',
    routedInput: { items: [], prompt: 'test' },
    nativeInput: { items: [] },
    invoke: (input) => (neuro.uint8Array.of as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'reduce',
    functionId: 'Uint8Array.prototype.reduce',
    routedInput: { uint8Array: undefined, callbackfn: undefined, initialValue: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, callbackfn: undefined, initialValue: undefined },
    invoke: (input) => (neuro.uint8Array.reduce as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'reduceRight',
    functionId: 'Uint8Array.prototype.reduceRight',
    routedInput: { uint8Array: undefined, callbackfn: undefined, initialValue: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, callbackfn: undefined, initialValue: undefined },
    invoke: (input) => (neuro.uint8Array.reduceRight as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'reverse',
    functionId: 'Uint8Array.prototype.reverse',
    routedInput: { uint8Array: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined },
    invoke: (input) => (neuro.uint8Array.reverse as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'set',
    functionId: 'Uint8Array.prototype.set',
    routedInput: { uint8Array: undefined, array: undefined, offset: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, array: undefined, offset: undefined },
    invoke: (input) => (neuro.uint8Array.set as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'setFromBase64',
    functionId: 'Uint8Array.prototype.setFromBase64',
    routedInput: { uint8Array: undefined, string: undefined, options: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, string: undefined, options: undefined },
    invoke: (input) => (neuro.uint8Array.setFromBase64 as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'setFromHex',
    functionId: 'Uint8Array.prototype.setFromHex',
    routedInput: { uint8Array: undefined, string: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, string: undefined },
    invoke: (input) => (neuro.uint8Array.setFromHex as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'slice',
    functionId: 'Uint8Array.prototype.slice',
    routedInput: { uint8Array: undefined, start: undefined, end: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, start: undefined, end: undefined },
    invoke: (input) => (neuro.uint8Array.slice as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'some',
    functionId: 'Uint8Array.prototype.some',
    routedInput: { uint8Array: undefined, predicate: undefined, thisArg: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, predicate: undefined, thisArg: undefined },
    invoke: (input) => (neuro.uint8Array.some as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'sort',
    functionId: 'Uint8Array.prototype.sort',
    routedInput: { uint8Array: undefined, compareFn: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, compareFn: undefined },
    invoke: (input) => (neuro.uint8Array.sort as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'subarray',
    functionId: 'Uint8Array.prototype.subarray',
    routedInput: { uint8Array: undefined, begin: undefined, end: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, begin: undefined, end: undefined },
    invoke: (input) => (neuro.uint8Array.subarray as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'toBase64',
    functionId: 'Uint8Array.prototype.toBase64',
    routedInput: { uint8Array: undefined, options: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, options: undefined },
    invoke: (input) => (neuro.uint8Array.toBase64 as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'toHex',
    functionId: 'Uint8Array.prototype.toHex',
    routedInput: { uint8Array: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined },
    invoke: (input) => (neuro.uint8Array.toHex as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'toReversed',
    functionId: 'Uint8Array.prototype.toReversed',
    routedInput: { uint8Array: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined },
    invoke: (input) => (neuro.uint8Array.toReversed as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'toSorted',
    functionId: 'Uint8Array.prototype.toSorted',
    routedInput: { uint8Array: undefined, compareFn: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, compareFn: undefined },
    invoke: (input) => (neuro.uint8Array.toSorted as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'values',
    functionId: 'Uint8Array.prototype.values',
    routedInput: { uint8Array: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined },
    invoke: (input) => (neuro.uint8Array.values as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'with',
    functionId: 'Uint8Array.prototype.with',
    routedInput: { uint8Array: undefined, index: undefined, value: undefined, prompt: 'test' },
    nativeInput: { uint8Array: undefined, index: undefined, value: undefined },
    invoke: (input) => (neuro.uint8Array.with as (i: Record<string, unknown>) => Promise<unknown>)(input),
  }
];

afterEach(() => {
  resetClient();
  vi.unstubAllGlobals();
  vi.restoreAllMocks();
});

describe('uint8Array routing', () => {
  test.each(cases)('$name routes to LLM when prompt is set', async ({ functionId, routedInput, invoke }) => {
    const fetchMock = vi.fn(
      async () =>
        new Response(JSON.stringify({ result: null }), {
          status: 200,
          headers: { 'content-type': 'application/json' },
        }),
    );
    vi.stubGlobal('fetch', fetchMock);
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await invoke(routedInput);
    expect(fetchMock).toHaveBeenCalledOnce();
    const sent = JSON.parse(String(fetchMock.mock.calls[0][1]?.body));
    expect(sent.functionId).toBe(functionId);
    expect(sent.prompt).toBe('test');
  });

  test.each(cases)('$name does not call the LLM when prompt is omitted', async ({ nativeInput, invoke }) => {
    const fetchMock = vi.fn();
    vi.stubGlobal('fetch', fetchMock);
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    // We do not assert the native return value (many methods need real
    // arguments to run); we only assert that fetch is never invoked.
    await invoke(nativeInput).catch(() => {
      /* native may throw with stub args; the routing decision is what
       * we are testing here. */
    });
    expect(fetchMock).not.toHaveBeenCalled();
  });

  test.each(cases)('$name treats prompt: "" as absent (no LLM call)', async ({ nativeInput, invoke }) => {
    const fetchMock = vi.fn();
    vi.stubGlobal('fetch', fetchMock);
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await invoke({ ...nativeInput, prompt: '' }).catch(() => {});
    expect(fetchMock).not.toHaveBeenCalled();
  });

  test.each(cases)('$name surfaces network outage as NeuroClientError', async ({ functionId, routedInput, invoke }) => {
    vi.stubGlobal('fetch', async () => {
      throw new Error('network down');
    });
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await expect(invoke(routedInput)).rejects.toThrow(/Proxy fetch failed/);
    // The thrown error includes the functionId for postmortems.
    await expect(invoke(routedInput)).rejects.toThrow(new RegExp(functionId.replace(/[.\\]/g, '\\$&')));
  });

  test.each(cases)('$name surfaces a 500 from the proxy', async ({ routedInput, invoke }) => {
    vi.stubGlobal(
      'fetch',
      async () => new Response('boom', { status: 500, statusText: 'Server Error' }),
    );
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await expect(invoke(routedInput)).rejects.toThrow(/Proxy.*responded 500/);
  });
});


describe('uint8Array native values', () => {
  test('at native fallback matches native built-in', async () => {
    const result = await (neuro.uint8Array.at as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]), index: 1 });
    expect(result).toBe(new Uint8Array([10, 20, 30, 40]).at(1));
  });

  test('copyWithin native fallback matches native built-in (deep)', async () => {
    const result = await (neuro.uint8Array.copyWithin as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]), target: 0, start: 2 });
    expect(result).toEqual(new Uint8Array([10, 20, 30, 40]).copyWithin(0, 2));
  });

  test('entries native fallback iterator matches native', async () => {
    const result = await (neuro.uint8Array.entries as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]) });
    const native = new Uint8Array([10, 20, 30, 40]).entries();
    expect(Array.from(result as Iterable<unknown>)).toEqual(Array.from(native as Iterable<unknown>));
  });

  test('every native fallback matches native built-in', async () => {
    const result = await (neuro.uint8Array.every as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]), predicate: (v) => v > 0 });
    expect(result).toBe(new Uint8Array([10, 20, 30, 40]).every(v => v > 0));
  });

  test('fill native fallback matches native built-in (deep)', async () => {
    const result = await (neuro.uint8Array.fill as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]), value: 99 });
    expect(result).toEqual(new Uint8Array([10, 20, 30, 40]).fill(99));
  });

  test('filter native fallback matches native built-in (deep)', async () => {
    const result = await (neuro.uint8Array.filter as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]), predicate: (v) => v > 15 });
    expect(result).toEqual(new Uint8Array([10, 20, 30, 40]).filter(v => v > 15));
  });

  test('find native fallback matches native built-in', async () => {
    const result = await (neuro.uint8Array.find as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]), predicate: (v) => v > 15 });
    expect(result).toBe(new Uint8Array([10, 20, 30, 40]).find(v => v > 15));
  });

  test('findIndex native fallback matches native built-in', async () => {
    const result = await (neuro.uint8Array.findIndex as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]), predicate: (v) => v > 15 });
    expect(result).toBe(new Uint8Array([10, 20, 30, 40]).findIndex(v => v > 15));
  });

  test('findLast native fallback matches native built-in', async () => {
    const result = await (neuro.uint8Array.findLast as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]), predicate: (v) => v < 35 });
    expect(result).toBe(new Uint8Array([10, 20, 30, 40]).findLast(v => v < 35));
  });

  test('findLastIndex native fallback matches native built-in', async () => {
    const result = await (neuro.uint8Array.findLastIndex as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]), predicate: (v) => v < 35 });
    expect(result).toBe(new Uint8Array([10, 20, 30, 40]).findLastIndex(v => v < 35));
  });

  test('forEach native fallback returns undefined (void)', async () => {
    const result = await (neuro.uint8Array.forEach as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]), callbackfn: () => {} });
    expect(result).toBeUndefined();
  });

  test('from native fallback matches native built-in (deep)', async () => {
    const result = await (neuro.uint8Array.from as (i: Record<string, unknown>) => Promise<unknown>)({ arrayLike: [1, 2, 3] });
    expect(result).toEqual(Uint8Array.from([1, 2, 3]));
  });

  test.skip('fromBase64 native fallback does not throw', async () => { // Uint8Array.fromBase64 not available in this Node.js version
    await expect((neuro.uint8Array.fromBase64 as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]) })).resolves.not.toThrow();
  });

  test.skip('fromHex native fallback does not throw', async () => { // Uint8Array.fromHex not available in this Node.js version
    await expect((neuro.uint8Array.fromHex as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]) })).resolves.not.toThrow();
  });

  test('includes native fallback matches native built-in', async () => {
    const result = await (neuro.uint8Array.includes as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]), searchElement: 10 });
    expect(result).toBe(new Uint8Array([10, 20, 30, 40]).includes(10));
  });

  test('indexOf native fallback matches native built-in', async () => {
    const result = await (neuro.uint8Array.indexOf as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]), searchElement: 10 });
    expect(result).toBe(new Uint8Array([10, 20, 30, 40]).indexOf(10));
  });

  test('join native fallback matches native built-in', async () => {
    const result = await (neuro.uint8Array.join as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]), separator: '-' });
    expect(result).toBe(new Uint8Array([10, 20, 30, 40]).join('-'));
  });

  test('keys native fallback iterator matches native', async () => {
    const result = await (neuro.uint8Array.keys as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]) });
    const native = new Uint8Array([10, 20, 30, 40]).keys();
    expect(Array.from(result as Iterable<unknown>)).toEqual(Array.from(native as Iterable<unknown>));
  });

  test('lastIndexOf native fallback matches native built-in', async () => {
    const result = await (neuro.uint8Array.lastIndexOf as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]), searchElement: 10 });
    expect(result).toBe(new Uint8Array([10, 20, 30, 40]).lastIndexOf(10));
  });

  test('map native fallback matches native built-in (deep)', async () => {
    const result = await (neuro.uint8Array.map as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]), callbackfn: (v) => v * 2 });
    expect(result).toEqual(new Uint8Array([10, 20, 30, 40]).map(v => v * 2));
  });

  test('of native fallback matches native built-in (deep)', async () => {
    const result = await (neuro.uint8Array.of as (i: Record<string, unknown>) => Promise<unknown>)({ items: [1, 2, 3] });
    expect(result).toEqual(Uint8Array.of(1, 2, 3));
  });

  test('reduce native fallback matches native built-in', async () => {
    const result = await (neuro.uint8Array.reduce as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]), callbackfn: (a, v) => a + v, initialValue: 0 });
    expect(result).toBe(new Uint8Array([10, 20, 30, 40]).reduce((a, v) => a + v, 0));
  });

  test('reduceRight native fallback matches native built-in', async () => {
    const result = await (neuro.uint8Array.reduceRight as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]), callbackfn: (a, v) => a + v, initialValue: 0 });
    expect(result).toBe(new Uint8Array([10, 20, 30, 40]).reduceRight((a, v) => a + v, 0));
  });

  test('reverse native fallback matches native built-in (deep)', async () => {
    const result = await (neuro.uint8Array.reverse as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]) });
    expect(result).toEqual(new Uint8Array([10, 20, 30, 40]).reverse());
  });

  test('set native fallback returns undefined (void)', async () => {
    const result = await (neuro.uint8Array.set as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]), array: [99] });
    expect(result).toBeUndefined();
  });

  test.skip('setFromBase64 native fallback does not throw', async () => { // Uint8Array.setFromBase64 not available in this Node.js version
    await expect((neuro.uint8Array.setFromBase64 as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]) })).resolves.not.toThrow();
  });

  test.skip('setFromHex native fallback does not throw', async () => { // Uint8Array.setFromHex not available in this Node.js version
    await expect((neuro.uint8Array.setFromHex as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]) })).resolves.not.toThrow();
  });

  test('slice native fallback matches native built-in (deep)', async () => {
    const result = await (neuro.uint8Array.slice as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]), start: 1, end: 3 });
    expect(result).toEqual(new Uint8Array([10, 20, 30, 40]).slice(1, 3));
  });

  test('some native fallback matches native built-in', async () => {
    const result = await (neuro.uint8Array.some as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]), predicate: (v) => v > 30 });
    expect(result).toBe(new Uint8Array([10, 20, 30, 40]).some(v => v > 30));
  });

  test('sort native fallback matches native built-in (deep)', async () => {
    const result = await (neuro.uint8Array.sort as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]) });
    expect(result).toEqual(new Uint8Array([10, 20, 30, 40]).sort());
  });

  test('subarray native fallback matches native built-in (deep)', async () => {
    const result = await (neuro.uint8Array.subarray as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]), begin: 1, end: 3 });
    expect(result).toEqual(new Uint8Array([10, 20, 30, 40]).subarray(1, 3));
  });

  test.skip('toBase64 native fallback does not throw', async () => { // Uint8Array.toBase64 not available in this Node.js version
    await expect((neuro.uint8Array.toBase64 as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]) })).resolves.not.toThrow();
  });

  test.skip('toHex native fallback does not throw', async () => { // Uint8Array.toHex not available in this Node.js version
    await expect((neuro.uint8Array.toHex as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]) })).resolves.not.toThrow();
  });

  test('toReversed native fallback matches native built-in (deep)', async () => {
    const result = await (neuro.uint8Array.toReversed as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]) });
    expect(result).toEqual(new Uint8Array([10, 20, 30, 40]).toReversed());
  });

  test('toSorted native fallback matches native built-in (deep)', async () => {
    const result = await (neuro.uint8Array.toSorted as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]) });
    expect(result).toEqual(new Uint8Array([10, 20, 30, 40]).toSorted());
  });

  test('values native fallback iterator matches native', async () => {
    const result = await (neuro.uint8Array.values as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]) });
    const native = new Uint8Array([10, 20, 30, 40]).values();
    expect(Array.from(result as Iterable<unknown>)).toEqual(Array.from(native as Iterable<unknown>));
  });

  test('with native fallback matches native built-in (deep)', async () => {
    const result = await (neuro.uint8Array.with as (i: Record<string, unknown>) => Promise<unknown>)({ uint8Array: new Uint8Array([10, 20, 30, 40]), index: 1, value: 99 });
    expect(result).toEqual(new Uint8Array([10, 20, 30, 40]).with(1, 99));
  });
});
