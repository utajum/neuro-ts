// Auto-generated by scripts/generate-tests.ts. Do not edit.
// Per-method routing & error-path tests for the `atomics` group.
/* eslint-disable import/no-relative-packages */
import { afterEach, describe, expect, test, vi } from 'vitest';
import { configureClient, neuro, resetClient } from '../../dist/index.js';

interface MethodCase {
  name: string;
  functionId: string;
  routedInput: Record<string, unknown>;
  nativeInput: Record<string, unknown>;
  invoke: (input: Record<string, unknown>) => Promise<unknown>;
}

const cases: MethodCase[] = [
  {
    name: 'add',
    functionId: 'Atomics.add',
    routedInput: { typedArray: undefined, index: undefined, value: undefined, prompt: 'test' },
    nativeInput: { typedArray: undefined, index: undefined, value: undefined },
    invoke: (input) => (neuro.atomics.add as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'and',
    functionId: 'Atomics.and',
    routedInput: { typedArray: undefined, index: undefined, value: undefined, prompt: 'test' },
    nativeInput: { typedArray: undefined, index: undefined, value: undefined },
    invoke: (input) => (neuro.atomics.and as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'compareExchange',
    functionId: 'Atomics.compareExchange',
    routedInput: { typedArray: undefined, index: undefined, expectedValue: undefined, replacementValue: undefined, prompt: 'test' },
    nativeInput: { typedArray: undefined, index: undefined, expectedValue: undefined, replacementValue: undefined },
    invoke: (input) => (neuro.atomics.compareExchange as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'exchange',
    functionId: 'Atomics.exchange',
    routedInput: { typedArray: undefined, index: undefined, value: undefined, prompt: 'test' },
    nativeInput: { typedArray: undefined, index: undefined, value: undefined },
    invoke: (input) => (neuro.atomics.exchange as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'isLockFree',
    functionId: 'Atomics.isLockFree',
    routedInput: { size: undefined, prompt: 'test' },
    nativeInput: { size: undefined },
    invoke: (input) => (neuro.atomics.isLockFree as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'load',
    functionId: 'Atomics.load',
    routedInput: { typedArray: undefined, index: undefined, prompt: 'test' },
    nativeInput: { typedArray: undefined, index: undefined },
    invoke: (input) => (neuro.atomics.load as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'notify',
    functionId: 'Atomics.notify',
    routedInput: { typedArray: undefined, index: undefined, count: undefined, prompt: 'test' },
    nativeInput: { typedArray: undefined, index: undefined, count: undefined },
    invoke: (input) => (neuro.atomics.notify as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'or',
    functionId: 'Atomics.or',
    routedInput: { typedArray: undefined, index: undefined, value: undefined, prompt: 'test' },
    nativeInput: { typedArray: undefined, index: undefined, value: undefined },
    invoke: (input) => (neuro.atomics.or as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'pause',
    functionId: 'Atomics.pause',
    routedInput: { n: undefined, prompt: 'test' },
    nativeInput: { n: undefined },
    invoke: (input) => (neuro.atomics.pause as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'store',
    functionId: 'Atomics.store',
    routedInput: { typedArray: undefined, index: undefined, value: undefined, prompt: 'test' },
    nativeInput: { typedArray: undefined, index: undefined, value: undefined },
    invoke: (input) => (neuro.atomics.store as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'sub',
    functionId: 'Atomics.sub',
    routedInput: { typedArray: undefined, index: undefined, value: undefined, prompt: 'test' },
    nativeInput: { typedArray: undefined, index: undefined, value: undefined },
    invoke: (input) => (neuro.atomics.sub as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'wait',
    functionId: 'Atomics.wait',
    routedInput: { typedArray: undefined, index: undefined, value: undefined, timeout: undefined, prompt: 'test' },
    nativeInput: { typedArray: undefined, index: undefined, value: undefined, timeout: undefined },
    invoke: (input) => (neuro.atomics.wait as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'waitAsync',
    functionId: 'Atomics.waitAsync',
    routedInput: { typedArray: undefined, index: undefined, value: undefined, timeout: undefined, prompt: 'test' },
    nativeInput: { typedArray: undefined, index: undefined, value: undefined, timeout: undefined },
    invoke: (input) => (neuro.atomics.waitAsync as (i: Record<string, unknown>) => Promise<unknown>)(input),
  },
  {
    name: 'xor',
    functionId: 'Atomics.xor',
    routedInput: { typedArray: undefined, index: undefined, value: undefined, prompt: 'test' },
    nativeInput: { typedArray: undefined, index: undefined, value: undefined },
    invoke: (input) => (neuro.atomics.xor as (i: Record<string, unknown>) => Promise<unknown>)(input),
  }
];

afterEach(() => {
  resetClient();
  vi.unstubAllGlobals();
  vi.restoreAllMocks();
});

describe('atomics routing', () => {
  test.each(cases)('$name routes to LLM when prompt is set', async ({ functionId, routedInput, invoke }) => {
    const fetchMock = vi.fn(
      async () =>
        new Response(JSON.stringify({ result: null }), {
          status: 200,
          headers: { 'content-type': 'application/json' },
        }),
    );
    vi.stubGlobal('fetch', fetchMock);
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await invoke(routedInput);
    expect(fetchMock).toHaveBeenCalledOnce();
    const sent = JSON.parse(String(fetchMock.mock.calls[0][1]?.body));
    expect(sent.functionId).toBe(functionId);
    expect(sent.prompt).toBe('test');
  });

  test.each(cases)('$name does not call the LLM when prompt is omitted', async ({ nativeInput, invoke }) => {
    const fetchMock = vi.fn();
    vi.stubGlobal('fetch', fetchMock);
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    // We do not assert the native return value (many methods need real
    // arguments to run); we only assert that fetch is never invoked.
    await invoke(nativeInput).catch(() => {
      /* native may throw with stub args; the routing decision is what
       * we are testing here. */
    });
    expect(fetchMock).not.toHaveBeenCalled();
  });

  test.each(cases)('$name treats prompt: "" as absent (no LLM call)', async ({ nativeInput, invoke }) => {
    const fetchMock = vi.fn();
    vi.stubGlobal('fetch', fetchMock);
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await invoke({ ...nativeInput, prompt: '' }).catch(() => {});
    expect(fetchMock).not.toHaveBeenCalled();
  });

  test.each(cases)('$name surfaces network outage as NeuroClientError', async ({ functionId, routedInput, invoke }) => {
    vi.stubGlobal('fetch', async () => {
      throw new Error('network down');
    });
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await expect(invoke(routedInput)).rejects.toThrow(/Proxy fetch failed/);
    // The thrown error includes the functionId for postmortems.
    await expect(invoke(routedInput)).rejects.toThrow(new RegExp(functionId.replace(/[.\\]/g, '\\$&')));
  });

  test.each(cases)('$name surfaces a 500 from the proxy', async ({ routedInput, invoke }) => {
    vi.stubGlobal(
      'fetch',
      async () => new Response('boom', { status: 500, statusText: 'Server Error' }),
    );
    configureClient({ proxyUrl: 'https://example.test/neuro' });
    await expect(invoke(routedInput)).rejects.toThrow(/Proxy.*responded 500/);
  });
});


describe('atomics native values', () => {
  test.skip('add native fallback does not throw', async () => { // Atomics requires SharedArrayBuffer which needs cross-origin isolation
    await expect((neuro.atomics.add as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('and native fallback does not throw', async () => { // Atomics requires SharedArrayBuffer which needs cross-origin isolation
    await expect((neuro.atomics.and as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('compareExchange native fallback does not throw', async () => { // Atomics requires SharedArrayBuffer which needs cross-origin isolation
    await expect((neuro.atomics.compareExchange as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('exchange native fallback does not throw', async () => { // Atomics requires SharedArrayBuffer which needs cross-origin isolation
    await expect((neuro.atomics.exchange as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('isLockFree native fallback does not throw', async () => { // Atomics requires SharedArrayBuffer which needs cross-origin isolation
    await expect((neuro.atomics.isLockFree as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('load native fallback does not throw', async () => { // Atomics requires SharedArrayBuffer which needs cross-origin isolation
    await expect((neuro.atomics.load as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('notify native fallback does not throw', async () => { // Atomics requires SharedArrayBuffer which needs cross-origin isolation
    await expect((neuro.atomics.notify as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('or native fallback does not throw', async () => { // Atomics requires SharedArrayBuffer which needs cross-origin isolation
    await expect((neuro.atomics.or as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('pause native fallback does not throw', async () => { // Atomics requires SharedArrayBuffer which needs cross-origin isolation
    await expect((neuro.atomics.pause as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('store native fallback does not throw', async () => { // Atomics requires SharedArrayBuffer which needs cross-origin isolation
    await expect((neuro.atomics.store as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('sub native fallback does not throw', async () => { // Atomics requires SharedArrayBuffer which needs cross-origin isolation
    await expect((neuro.atomics.sub as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('wait native fallback does not throw', async () => { // Atomics requires SharedArrayBuffer which needs cross-origin isolation
    await expect((neuro.atomics.wait as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('waitAsync native fallback does not throw', async () => { // Atomics requires SharedArrayBuffer which needs cross-origin isolation
    await expect((neuro.atomics.waitAsync as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });

  test.skip('xor native fallback does not throw', async () => { // Atomics requires SharedArrayBuffer which needs cross-origin isolation
    await expect((neuro.atomics.xor as (i: Record<string, unknown>) => Promise<unknown>)({})).resolves.not.toThrow();
  });
});
