// Auto-generated by scripts/generate-wrappers.ts. Do not edit.
import { runMethod, resolveNative, type GeneratedMethodSpec } from '../../runtime';

/** `Date.prototype.getDate` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getDate_0_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'getDate',
  functionId: 'Date.prototype.getDate',
  kind: 'instance',
  native: resolveNative('Date.prototype.getDate'),
  receiverKey: "date",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.getDate\`.
## Original signature(s)
  Overload 1: () => number
## JSDoc
Gets the day-of-the-month, using local time.

## How to respond
- Behave EXACTLY as the original \`getDate\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function getDate(input: { date: Date; prompt?: string }): Promise<number>;
export async function getDate(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getDate_0_spec, input); }

/** `Date.prototype.getDay` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getDay_1_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'getDay',
  functionId: 'Date.prototype.getDay',
  kind: 'instance',
  native: resolveNative('Date.prototype.getDay'),
  receiverKey: "date",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.getDay\`.
## Original signature(s)
  Overload 1: () => number
## JSDoc
Gets the day of the week, using local time.

## How to respond
- Behave EXACTLY as the original \`getDay\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function getDay(input: { date: Date; prompt?: string }): Promise<number>;
export async function getDay(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getDay_1_spec, input); }

/** `Date.prototype.getFullYear` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getFullYear_2_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'getFullYear',
  functionId: 'Date.prototype.getFullYear',
  kind: 'instance',
  native: resolveNative('Date.prototype.getFullYear'),
  receiverKey: "date",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.getFullYear\`.
## Original signature(s)
  Overload 1: () => number
## JSDoc
Gets the year, using local time.

## How to respond
- Behave EXACTLY as the original \`getFullYear\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function getFullYear(input: { date: Date; prompt?: string }): Promise<number>;
export async function getFullYear(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getFullYear_2_spec, input); }

/** `Date.prototype.getHours` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getHours_3_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'getHours',
  functionId: 'Date.prototype.getHours',
  kind: 'instance',
  native: resolveNative('Date.prototype.getHours'),
  receiverKey: "date",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.getHours\`.
## Original signature(s)
  Overload 1: () => number
## JSDoc
Gets the hours in a date, using local time.

## How to respond
- Behave EXACTLY as the original \`getHours\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function getHours(input: { date: Date; prompt?: string }): Promise<number>;
export async function getHours(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getHours_3_spec, input); }

/** `Date.prototype.getMilliseconds` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getMilliseconds_4_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'getMilliseconds',
  functionId: 'Date.prototype.getMilliseconds',
  kind: 'instance',
  native: resolveNative('Date.prototype.getMilliseconds'),
  receiverKey: "date",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.getMilliseconds\`.
## Original signature(s)
  Overload 1: () => number
## JSDoc
Gets the milliseconds of a Date, using local time.

## How to respond
- Behave EXACTLY as the original \`getMilliseconds\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function getMilliseconds(input: { date: Date; prompt?: string }): Promise<number>;
export async function getMilliseconds(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getMilliseconds_4_spec, input); }

/** `Date.prototype.getMinutes` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getMinutes_5_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'getMinutes',
  functionId: 'Date.prototype.getMinutes',
  kind: 'instance',
  native: resolveNative('Date.prototype.getMinutes'),
  receiverKey: "date",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.getMinutes\`.
## Original signature(s)
  Overload 1: () => number
## JSDoc
Gets the minutes of a Date object, using local time.

## How to respond
- Behave EXACTLY as the original \`getMinutes\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function getMinutes(input: { date: Date; prompt?: string }): Promise<number>;
export async function getMinutes(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getMinutes_5_spec, input); }

/** `Date.prototype.getMonth` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getMonth_6_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'getMonth',
  functionId: 'Date.prototype.getMonth',
  kind: 'instance',
  native: resolveNative('Date.prototype.getMonth'),
  receiverKey: "date",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.getMonth\`.
## Original signature(s)
  Overload 1: () => number
## JSDoc
Gets the month, using local time.

## How to respond
- Behave EXACTLY as the original \`getMonth\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function getMonth(input: { date: Date; prompt?: string }): Promise<number>;
export async function getMonth(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getMonth_6_spec, input); }

/** `Date.prototype.getSeconds` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getSeconds_7_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'getSeconds',
  functionId: 'Date.prototype.getSeconds',
  kind: 'instance',
  native: resolveNative('Date.prototype.getSeconds'),
  receiverKey: "date",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.getSeconds\`.
## Original signature(s)
  Overload 1: () => number
## JSDoc
Gets the seconds of a Date object, using local time.

## How to respond
- Behave EXACTLY as the original \`getSeconds\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function getSeconds(input: { date: Date; prompt?: string }): Promise<number>;
export async function getSeconds(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getSeconds_7_spec, input); }

/** `Date.prototype.getTime` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getTime_8_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'getTime',
  functionId: 'Date.prototype.getTime',
  kind: 'instance',
  native: resolveNative('Date.prototype.getTime'),
  receiverKey: "date",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.getTime\`.
## Original signature(s)
  Overload 1: () => number
## JSDoc
Returns the stored time value in milliseconds since midnight, January 1, 1970 UTC.

## How to respond
- Behave EXACTLY as the original \`getTime\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function getTime(input: { date: Date; prompt?: string }): Promise<number>;
export async function getTime(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getTime_8_spec, input); }

/** `Date.prototype.getTimezoneOffset` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getTimezoneOffset_9_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'getTimezoneOffset',
  functionId: 'Date.prototype.getTimezoneOffset',
  kind: 'instance',
  native: resolveNative('Date.prototype.getTimezoneOffset'),
  receiverKey: "date",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.getTimezoneOffset\`.
## Original signature(s)
  Overload 1: () => number
## JSDoc
Gets the difference in minutes between Universal Coordinated Time (UTC) and the time on the local computer.

## How to respond
- Behave EXACTLY as the original \`getTimezoneOffset\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function getTimezoneOffset(input: { date: Date; prompt?: string }): Promise<number>;
export async function getTimezoneOffset(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getTimezoneOffset_9_spec, input); }

/** `Date.prototype.getUTCDate` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getUTCDate_10_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'getUTCDate',
  functionId: 'Date.prototype.getUTCDate',
  kind: 'instance',
  native: resolveNative('Date.prototype.getUTCDate'),
  receiverKey: "date",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.getUTCDate\`.
## Original signature(s)
  Overload 1: () => number
## JSDoc
Gets the day-of-the-month, using Universal Coordinated Time (UTC).

## How to respond
- Behave EXACTLY as the original \`getUTCDate\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function getUTCDate(input: { date: Date; prompt?: string }): Promise<number>;
export async function getUTCDate(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getUTCDate_10_spec, input); }

/** `Date.prototype.getUTCDay` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getUTCDay_11_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'getUTCDay',
  functionId: 'Date.prototype.getUTCDay',
  kind: 'instance',
  native: resolveNative('Date.prototype.getUTCDay'),
  receiverKey: "date",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.getUTCDay\`.
## Original signature(s)
  Overload 1: () => number
## JSDoc
Gets the day of the week using Universal Coordinated Time (UTC).

## How to respond
- Behave EXACTLY as the original \`getUTCDay\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function getUTCDay(input: { date: Date; prompt?: string }): Promise<number>;
export async function getUTCDay(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getUTCDay_11_spec, input); }

/** `Date.prototype.getUTCFullYear` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getUTCFullYear_12_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'getUTCFullYear',
  functionId: 'Date.prototype.getUTCFullYear',
  kind: 'instance',
  native: resolveNative('Date.prototype.getUTCFullYear'),
  receiverKey: "date",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.getUTCFullYear\`.
## Original signature(s)
  Overload 1: () => number
## JSDoc
Gets the year using Universal Coordinated Time (UTC).

## How to respond
- Behave EXACTLY as the original \`getUTCFullYear\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function getUTCFullYear(input: { date: Date; prompt?: string }): Promise<number>;
export async function getUTCFullYear(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getUTCFullYear_12_spec, input); }

/** `Date.prototype.getUTCHours` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getUTCHours_13_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'getUTCHours',
  functionId: 'Date.prototype.getUTCHours',
  kind: 'instance',
  native: resolveNative('Date.prototype.getUTCHours'),
  receiverKey: "date",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.getUTCHours\`.
## Original signature(s)
  Overload 1: () => number
## JSDoc
Gets the hours value in a Date object using Universal Coordinated Time (UTC).

## How to respond
- Behave EXACTLY as the original \`getUTCHours\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function getUTCHours(input: { date: Date; prompt?: string }): Promise<number>;
export async function getUTCHours(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getUTCHours_13_spec, input); }

/** `Date.prototype.getUTCMilliseconds` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getUTCMilliseconds_14_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'getUTCMilliseconds',
  functionId: 'Date.prototype.getUTCMilliseconds',
  kind: 'instance',
  native: resolveNative('Date.prototype.getUTCMilliseconds'),
  receiverKey: "date",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.getUTCMilliseconds\`.
## Original signature(s)
  Overload 1: () => number
## JSDoc
Gets the milliseconds of a Date object using Universal Coordinated Time (UTC).

## How to respond
- Behave EXACTLY as the original \`getUTCMilliseconds\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function getUTCMilliseconds(input: { date: Date; prompt?: string }): Promise<number>;
export async function getUTCMilliseconds(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getUTCMilliseconds_14_spec, input); }

/** `Date.prototype.getUTCMinutes` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getUTCMinutes_15_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'getUTCMinutes',
  functionId: 'Date.prototype.getUTCMinutes',
  kind: 'instance',
  native: resolveNative('Date.prototype.getUTCMinutes'),
  receiverKey: "date",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.getUTCMinutes\`.
## Original signature(s)
  Overload 1: () => number
## JSDoc
Gets the minutes of a Date object using Universal Coordinated Time (UTC).

## How to respond
- Behave EXACTLY as the original \`getUTCMinutes\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function getUTCMinutes(input: { date: Date; prompt?: string }): Promise<number>;
export async function getUTCMinutes(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getUTCMinutes_15_spec, input); }

/** `Date.prototype.getUTCMonth` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getUTCMonth_16_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'getUTCMonth',
  functionId: 'Date.prototype.getUTCMonth',
  kind: 'instance',
  native: resolveNative('Date.prototype.getUTCMonth'),
  receiverKey: "date",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.getUTCMonth\`.
## Original signature(s)
  Overload 1: () => number
## JSDoc
Gets the month of a Date object using Universal Coordinated Time (UTC).

## How to respond
- Behave EXACTLY as the original \`getUTCMonth\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function getUTCMonth(input: { date: Date; prompt?: string }): Promise<number>;
export async function getUTCMonth(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getUTCMonth_16_spec, input); }

/** `Date.prototype.getUTCSeconds` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getUTCSeconds_17_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'getUTCSeconds',
  functionId: 'Date.prototype.getUTCSeconds',
  kind: 'instance',
  native: resolveNative('Date.prototype.getUTCSeconds'),
  receiverKey: "date",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.getUTCSeconds\`.
## Original signature(s)
  Overload 1: () => number
## JSDoc
Gets the seconds of a Date object using Universal Coordinated Time (UTC).

## How to respond
- Behave EXACTLY as the original \`getUTCSeconds\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function getUTCSeconds(input: { date: Date; prompt?: string }): Promise<number>;
export async function getUTCSeconds(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getUTCSeconds_17_spec, input); }

/** `Date.now` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __now_18_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'now',
  functionId: 'Date.now',
  kind: 'static',
  native: resolveNative('Date.now'),
  receiverKey: "",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.now\`.
## Original signature(s)
  Overload 1: () => number
## JSDoc
Returns the number of milliseconds elapsed since midnight, January 1, 1970 Universal Coordinated Time (UTC).

## How to respond
- Behave EXACTLY as the original \`now\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function now(input: { prompt?: string }): Promise<number>;
export async function now(input: Record<string, unknown>): Promise<unknown> { return runMethod(__now_18_spec, input); }

/** `Date.parse` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __parse_19_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'parse',
  functionId: 'Date.parse',
  kind: 'static',
  native: resolveNative('Date.parse'),
  receiverKey: "",
  paramOrder: ["s"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.parse\`.
## Original signature(s)
  Overload 1: (s: string) => number
## JSDoc
Parses a string containing a date, and returns the number of milliseconds between that date and midnight, January 1, 1970.

## How to respond
- Behave EXACTLY as the original \`parse\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"s","type":"string"}],
};
export function parse(input: { s: string; prompt?: string }): Promise<number>;
export async function parse(input: Record<string, unknown>): Promise<unknown> { return runMethod(__parse_19_spec, input); }

/** `Date.prototype.setDate` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setDate_20_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'setDate',
  functionId: 'Date.prototype.setDate',
  kind: 'instance',
  native: resolveNative('Date.prototype.setDate'),
  receiverKey: "date",
  paramOrder: ["date_arg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.setDate\`.
## Original signature(s)
  Overload 1: (date_arg: number) => number
## JSDoc
Sets the numeric day-of-the-month value of the Date object using local time.

## How to respond
- Behave EXACTLY as the original \`setDate\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"date_arg","type":"number"}],
};
export function setDate(input: { date: Date; date_arg: number; prompt?: string }): Promise<number>;
export async function setDate(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setDate_20_spec, input); }

/** `Date.prototype.setFullYear` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setFullYear_21_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'setFullYear',
  functionId: 'Date.prototype.setFullYear',
  kind: 'instance',
  native: resolveNative('Date.prototype.setFullYear'),
  receiverKey: "date",
  paramOrder: ["year","month","date_arg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.setFullYear\`.
## Original signature(s)
  Overload 1: (year: number, month?: number, date_arg?: number) => number
## JSDoc
Sets the year of the Date object using local time.

## How to respond
- Behave EXACTLY as the original \`setFullYear\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"year","type":"number"},{"name":"month?","type":"number"},{"name":"date_arg?","type":"number"}],
};
export function setFullYear(input: { date: Date; year: number; month?: number; date_arg?: number; prompt?: string }): Promise<number>;
export async function setFullYear(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setFullYear_21_spec, input); }

/** `Date.prototype.setHours` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setHours_22_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'setHours',
  functionId: 'Date.prototype.setHours',
  kind: 'instance',
  native: resolveNative('Date.prototype.setHours'),
  receiverKey: "date",
  paramOrder: ["hours","min","sec","ms"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.setHours\`.
## Original signature(s)
  Overload 1: (hours: number, min?: number, sec?: number, ms?: number) => number
## JSDoc
Sets the hour value in the Date object using local time.

## How to respond
- Behave EXACTLY as the original \`setHours\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"hours","type":"number"},{"name":"min?","type":"number"},{"name":"sec?","type":"number"},{"name":"ms?","type":"number"}],
};
export function setHours(input: { date: Date; hours: number; min?: number; sec?: number; ms?: number; prompt?: string }): Promise<number>;
export async function setHours(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setHours_22_spec, input); }

/** `Date.prototype.setMilliseconds` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setMilliseconds_23_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'setMilliseconds',
  functionId: 'Date.prototype.setMilliseconds',
  kind: 'instance',
  native: resolveNative('Date.prototype.setMilliseconds'),
  receiverKey: "date",
  paramOrder: ["ms"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.setMilliseconds\`.
## Original signature(s)
  Overload 1: (ms: number) => number
## JSDoc
Sets the milliseconds value in the Date object using local time.

## How to respond
- Behave EXACTLY as the original \`setMilliseconds\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"ms","type":"number"}],
};
export function setMilliseconds(input: { date: Date; ms: number; prompt?: string }): Promise<number>;
export async function setMilliseconds(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setMilliseconds_23_spec, input); }

/** `Date.prototype.setMinutes` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setMinutes_24_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'setMinutes',
  functionId: 'Date.prototype.setMinutes',
  kind: 'instance',
  native: resolveNative('Date.prototype.setMinutes'),
  receiverKey: "date",
  paramOrder: ["min","sec","ms"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.setMinutes\`.
## Original signature(s)
  Overload 1: (min: number, sec?: number, ms?: number) => number
## JSDoc
Sets the minutes value in the Date object using local time.

## How to respond
- Behave EXACTLY as the original \`setMinutes\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"min","type":"number"},{"name":"sec?","type":"number"},{"name":"ms?","type":"number"}],
};
export function setMinutes(input: { date: Date; min: number; sec?: number; ms?: number; prompt?: string }): Promise<number>;
export async function setMinutes(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setMinutes_24_spec, input); }

/** `Date.prototype.setMonth` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setMonth_25_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'setMonth',
  functionId: 'Date.prototype.setMonth',
  kind: 'instance',
  native: resolveNative('Date.prototype.setMonth'),
  receiverKey: "date",
  paramOrder: ["month","date_arg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.setMonth\`.
## Original signature(s)
  Overload 1: (month: number, date_arg?: number) => number
## JSDoc
Sets the month value in the Date object using local time.

## How to respond
- Behave EXACTLY as the original \`setMonth\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"month","type":"number"},{"name":"date_arg?","type":"number"}],
};
export function setMonth(input: { date: Date; month: number; date_arg?: number; prompt?: string }): Promise<number>;
export async function setMonth(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setMonth_25_spec, input); }

/** `Date.prototype.setSeconds` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setSeconds_26_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'setSeconds',
  functionId: 'Date.prototype.setSeconds',
  kind: 'instance',
  native: resolveNative('Date.prototype.setSeconds'),
  receiverKey: "date",
  paramOrder: ["sec","ms"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.setSeconds\`.
## Original signature(s)
  Overload 1: (sec: number, ms?: number) => number
## JSDoc
Sets the seconds value in the Date object using local time.

## How to respond
- Behave EXACTLY as the original \`setSeconds\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"sec","type":"number"},{"name":"ms?","type":"number"}],
};
export function setSeconds(input: { date: Date; sec: number; ms?: number; prompt?: string }): Promise<number>;
export async function setSeconds(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setSeconds_26_spec, input); }

/** `Date.prototype.setTime` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setTime_27_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'setTime',
  functionId: 'Date.prototype.setTime',
  kind: 'instance',
  native: resolveNative('Date.prototype.setTime'),
  receiverKey: "date",
  paramOrder: ["time"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.setTime\`.
## Original signature(s)
  Overload 1: (time: number) => number
## JSDoc
Sets the date and time value in the Date object.

## How to respond
- Behave EXACTLY as the original \`setTime\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"time","type":"number"}],
};
export function setTime(input: { date: Date; time: number; prompt?: string }): Promise<number>;
export async function setTime(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setTime_27_spec, input); }

/** `Date.prototype.setUTCDate` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setUTCDate_28_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'setUTCDate',
  functionId: 'Date.prototype.setUTCDate',
  kind: 'instance',
  native: resolveNative('Date.prototype.setUTCDate'),
  receiverKey: "date",
  paramOrder: ["date_arg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.setUTCDate\`.
## Original signature(s)
  Overload 1: (date_arg: number) => number
## JSDoc
Sets the numeric day of the month in the Date object using Universal Coordinated Time (UTC).

## How to respond
- Behave EXACTLY as the original \`setUTCDate\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"date_arg","type":"number"}],
};
export function setUTCDate(input: { date: Date; date_arg: number; prompt?: string }): Promise<number>;
export async function setUTCDate(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setUTCDate_28_spec, input); }

/** `Date.prototype.setUTCFullYear` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setUTCFullYear_29_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'setUTCFullYear',
  functionId: 'Date.prototype.setUTCFullYear',
  kind: 'instance',
  native: resolveNative('Date.prototype.setUTCFullYear'),
  receiverKey: "date",
  paramOrder: ["year","month","date_arg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.setUTCFullYear\`.
## Original signature(s)
  Overload 1: (year: number, month?: number, date_arg?: number) => number
## JSDoc
Sets the year value in the Date object using Universal Coordinated Time (UTC).

## How to respond
- Behave EXACTLY as the original \`setUTCFullYear\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"year","type":"number"},{"name":"month?","type":"number"},{"name":"date_arg?","type":"number"}],
};
export function setUTCFullYear(input: { date: Date; year: number; month?: number; date_arg?: number; prompt?: string }): Promise<number>;
export async function setUTCFullYear(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setUTCFullYear_29_spec, input); }

/** `Date.prototype.setUTCHours` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setUTCHours_30_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'setUTCHours',
  functionId: 'Date.prototype.setUTCHours',
  kind: 'instance',
  native: resolveNative('Date.prototype.setUTCHours'),
  receiverKey: "date",
  paramOrder: ["hours","min","sec","ms"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.setUTCHours\`.
## Original signature(s)
  Overload 1: (hours: number, min?: number, sec?: number, ms?: number) => number
## JSDoc
Sets the hours value in the Date object using Universal Coordinated Time (UTC).

## How to respond
- Behave EXACTLY as the original \`setUTCHours\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"hours","type":"number"},{"name":"min?","type":"number"},{"name":"sec?","type":"number"},{"name":"ms?","type":"number"}],
};
export function setUTCHours(input: { date: Date; hours: number; min?: number; sec?: number; ms?: number; prompt?: string }): Promise<number>;
export async function setUTCHours(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setUTCHours_30_spec, input); }

/** `Date.prototype.setUTCMilliseconds` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setUTCMilliseconds_31_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'setUTCMilliseconds',
  functionId: 'Date.prototype.setUTCMilliseconds',
  kind: 'instance',
  native: resolveNative('Date.prototype.setUTCMilliseconds'),
  receiverKey: "date",
  paramOrder: ["ms"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.setUTCMilliseconds\`.
## Original signature(s)
  Overload 1: (ms: number) => number
## JSDoc
Sets the milliseconds value in the Date object using Universal Coordinated Time (UTC).

## How to respond
- Behave EXACTLY as the original \`setUTCMilliseconds\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"ms","type":"number"}],
};
export function setUTCMilliseconds(input: { date: Date; ms: number; prompt?: string }): Promise<number>;
export async function setUTCMilliseconds(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setUTCMilliseconds_31_spec, input); }

/** `Date.prototype.setUTCMinutes` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setUTCMinutes_32_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'setUTCMinutes',
  functionId: 'Date.prototype.setUTCMinutes',
  kind: 'instance',
  native: resolveNative('Date.prototype.setUTCMinutes'),
  receiverKey: "date",
  paramOrder: ["min","sec","ms"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.setUTCMinutes\`.
## Original signature(s)
  Overload 1: (min: number, sec?: number, ms?: number) => number
## JSDoc
Sets the minutes value in the Date object using Universal Coordinated Time (UTC).

## How to respond
- Behave EXACTLY as the original \`setUTCMinutes\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"min","type":"number"},{"name":"sec?","type":"number"},{"name":"ms?","type":"number"}],
};
export function setUTCMinutes(input: { date: Date; min: number; sec?: number; ms?: number; prompt?: string }): Promise<number>;
export async function setUTCMinutes(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setUTCMinutes_32_spec, input); }

/** `Date.prototype.setUTCMonth` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setUTCMonth_33_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'setUTCMonth',
  functionId: 'Date.prototype.setUTCMonth',
  kind: 'instance',
  native: resolveNative('Date.prototype.setUTCMonth'),
  receiverKey: "date",
  paramOrder: ["month","date_arg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.setUTCMonth\`.
## Original signature(s)
  Overload 1: (month: number, date_arg?: number) => number
## JSDoc
Sets the month value in the Date object using Universal Coordinated Time (UTC).

## How to respond
- Behave EXACTLY as the original \`setUTCMonth\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"month","type":"number"},{"name":"date_arg?","type":"number"}],
};
export function setUTCMonth(input: { date: Date; month: number; date_arg?: number; prompt?: string }): Promise<number>;
export async function setUTCMonth(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setUTCMonth_33_spec, input); }

/** `Date.prototype.setUTCSeconds` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setUTCSeconds_34_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'setUTCSeconds',
  functionId: 'Date.prototype.setUTCSeconds',
  kind: 'instance',
  native: resolveNative('Date.prototype.setUTCSeconds'),
  receiverKey: "date",
  paramOrder: ["sec","ms"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.setUTCSeconds\`.
## Original signature(s)
  Overload 1: (sec: number, ms?: number) => number
## JSDoc
Sets the seconds value in the Date object using Universal Coordinated Time (UTC).

## How to respond
- Behave EXACTLY as the original \`setUTCSeconds\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"sec","type":"number"},{"name":"ms?","type":"number"}],
};
export function setUTCSeconds(input: { date: Date; sec: number; ms?: number; prompt?: string }): Promise<number>;
export async function setUTCSeconds(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setUTCSeconds_34_spec, input); }

/** `Date.prototype.toDateString` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toDateString_35_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'toDateString',
  functionId: 'Date.prototype.toDateString',
  kind: 'instance',
  native: resolveNative('Date.prototype.toDateString'),
  receiverKey: "date",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.toDateString\`.
## Original signature(s)
  Overload 1: () => string
## JSDoc
Returns a date as a string value.

## How to respond
- Behave EXACTLY as the original \`toDateString\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function toDateString(input: { date: Date; prompt?: string }): Promise<string>;
export async function toDateString(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toDateString_35_spec, input); }

/** `Date.prototype.toISOString` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toISOString_36_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'toISOString',
  functionId: 'Date.prototype.toISOString',
  kind: 'instance',
  native: resolveNative('Date.prototype.toISOString'),
  receiverKey: "date",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.toISOString\`.
## Original signature(s)
  Overload 1: () => string
## JSDoc
Returns a date as a string value in ISO format.

## How to respond
- Behave EXACTLY as the original \`toISOString\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function toISOString(input: { date: Date; prompt?: string }): Promise<string>;
export async function toISOString(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toISOString_36_spec, input); }

/** `Date.prototype.toJSON` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toJSON_37_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'toJSON',
  functionId: 'Date.prototype.toJSON',
  kind: 'instance',
  native: resolveNative('Date.prototype.toJSON'),
  receiverKey: "date",
  paramOrder: ["key"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.toJSON\`.
## Original signature(s)
  Overload 1: (key?: any) => string
## JSDoc
Used by the JSON.stringify method to enable the transformation of an object's data for JavaScript Object Notation (JSON) serialization.

## How to respond
- Behave EXACTLY as the original \`toJSON\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"key?","type":"any"}],
};
export function toJSON(input: { date: Date; key?: any; prompt?: string }): Promise<string>;
export async function toJSON(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toJSON_37_spec, input); }

/** `Date.prototype.toLocaleDateString` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toLocaleDateString_38_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'toLocaleDateString',
  functionId: 'Date.prototype.toLocaleDateString',
  kind: 'instance',
  native: resolveNative('Date.prototype.toLocaleDateString'),
  receiverKey: "date",
  paramOrder: ["locales","options"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.toLocaleDateString\`.
## Original signature(s)
  Overload 1: () => string
  Overload 2: (locales?: string | string[], options?: Intl.DateTimeFormatOptions) => string
  Overload 3: (locales?: Intl.LocalesArgument, options?: Intl.DateTimeFormatOptions) => string
## JSDoc
Returns a date as a string value appropriate to the host environment's current locale.

## How to respond
- Behave EXACTLY as the original \`toLocaleDateString\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function toLocaleDateString(input: { date: Date; prompt?: string }): Promise<string>;
export function toLocaleDateString(input: { date: Date; locales?: string | string[]; options?: Intl.DateTimeFormatOptions; prompt?: string }): Promise<string>;
export function toLocaleDateString(input: { date: Date; locales?: Intl.LocalesArgument; options?: Intl.DateTimeFormatOptions; prompt?: string }): Promise<string>;
export async function toLocaleDateString(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toLocaleDateString_38_spec, input); }

/** `Date.prototype.toLocaleTimeString` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toLocaleTimeString_39_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'toLocaleTimeString',
  functionId: 'Date.prototype.toLocaleTimeString',
  kind: 'instance',
  native: resolveNative('Date.prototype.toLocaleTimeString'),
  receiverKey: "date",
  paramOrder: ["locales","options"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.toLocaleTimeString\`.
## Original signature(s)
  Overload 1: () => string
  Overload 2: (locales?: string | string[], options?: Intl.DateTimeFormatOptions) => string
  Overload 3: (locales?: Intl.LocalesArgument, options?: Intl.DateTimeFormatOptions) => string
## JSDoc
Returns a time as a string value appropriate to the host environment's current locale.

## How to respond
- Behave EXACTLY as the original \`toLocaleTimeString\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function toLocaleTimeString(input: { date: Date; prompt?: string }): Promise<string>;
export function toLocaleTimeString(input: { date: Date; locales?: string | string[]; options?: Intl.DateTimeFormatOptions; prompt?: string }): Promise<string>;
export function toLocaleTimeString(input: { date: Date; locales?: Intl.LocalesArgument; options?: Intl.DateTimeFormatOptions; prompt?: string }): Promise<string>;
export async function toLocaleTimeString(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toLocaleTimeString_39_spec, input); }

/** `Date.prototype.toTimeString` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toTimeString_40_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'toTimeString',
  functionId: 'Date.prototype.toTimeString',
  kind: 'instance',
  native: resolveNative('Date.prototype.toTimeString'),
  receiverKey: "date",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.toTimeString\`.
## Original signature(s)
  Overload 1: () => string
## JSDoc
Returns a time as a string value.

## How to respond
- Behave EXACTLY as the original \`toTimeString\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function toTimeString(input: { date: Date; prompt?: string }): Promise<string>;
export async function toTimeString(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toTimeString_40_spec, input); }

/** `Date.prototype.toUTCString` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toUTCString_41_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'toUTCString',
  functionId: 'Date.prototype.toUTCString',
  kind: 'instance',
  native: resolveNative('Date.prototype.toUTCString'),
  receiverKey: "date",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.prototype.toUTCString\`.
## Original signature(s)
  Overload 1: () => string
## JSDoc
Returns a date converted to a string using Universal Coordinated Time (UTC).

## How to respond
- Behave EXACTLY as the original \`toUTCString\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function toUTCString(input: { date: Date; prompt?: string }): Promise<string>;
export async function toUTCString(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toUTCString_41_spec, input); }

/** `Date.UTC` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __UTC_42_spec: GeneratedMethodSpec = {
  group: 'date',
  methodName: 'UTC',
  functionId: 'Date.UTC',
  kind: 'static',
  native: resolveNative('Date.UTC'),
  receiverKey: "",
  paramOrder: ["year","monthIndex","date","hours","minutes","seconds","ms"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Date.UTC\`.
## Original signature(s)
  Overload 1: (year: number, monthIndex: number, date?: number, hours?: number, minutes?: number, seconds?: number, ms?: number) => number
  Overload 2: (year: number, monthIndex?: number, date?: number, hours?: number, minutes?: number, seconds?: number, ms?: number) => number
## JSDoc
Returns the number of milliseconds between midnight, January 1, 1970 Universal Coordinated Time (UTC) (or GMT) and the specified date.

## How to respond
- Behave EXACTLY as the original \`UTC\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"year","type":"number"},{"name":"monthIndex","type":"number"},{"name":"date?","type":"number"},{"name":"hours?","type":"number"},{"name":"minutes?","type":"number"},{"name":"seconds?","type":"number"},{"name":"ms?","type":"number"}],
};
export function UTC(input: { year: number; monthIndex: number; date?: number; hours?: number; minutes?: number; seconds?: number; ms?: number; prompt?: string }): Promise<number>;
export function UTC(input: { year: number; monthIndex?: number; date?: number; hours?: number; minutes?: number; seconds?: number; ms?: number; prompt?: string }): Promise<number>;
export async function UTC(input: Record<string, unknown>): Promise<unknown> { return runMethod(__UTC_42_spec, input); }


