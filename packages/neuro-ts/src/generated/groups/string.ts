// Auto-generated by scripts/generate-wrappers.ts. Do not edit.
import { runMethod, resolveNative, type GeneratedMethodSpec } from '../../runtime';

/** `String.prototype.anchor` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __anchor_0_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'anchor',
  functionId: 'String.prototype.anchor',
  kind: 'instance',
  native: resolveNative('String.prototype.anchor'),
  receiverKey: "string",
  paramOrder: ["name"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.anchor\`.
## Original signature(s)
  Overload 1: (name: string) => string
## JSDoc
Returns an \`<a>\` HTML anchor element and sets the name attribute to the text value

## How to respond
- Behave EXACTLY as the original \`anchor\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"name","type":"string"}],
};
export function anchor(input: { string: string; name: string; prompt?: string }): Promise<string>;
export async function anchor(input: Record<string, unknown>): Promise<unknown> { return runMethod(__anchor_0_spec, input); }

/** `String.prototype.at` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __at_1_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'at',
  functionId: 'String.prototype.at',
  kind: 'instance',
  native: resolveNative('String.prototype.at'),
  receiverKey: "string",
  paramOrder: ["index"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.at\`.
## Original signature(s)
  Overload 1: (index: number) => string
## JSDoc
Returns a new String consisting of the single UTF-16 code unit located at the specified index.

## How to respond
- Behave EXACTLY as the original \`at\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"index","type":"number"}],
};
export function at(input: { string: string; index: number; prompt?: string }): Promise<string>;
export async function at(input: Record<string, unknown>): Promise<unknown> { return runMethod(__at_1_spec, input); }

/** `String.prototype.big` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __big_2_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'big',
  functionId: 'String.prototype.big',
  kind: 'instance',
  native: resolveNative('String.prototype.big'),
  receiverKey: "string",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.big\`.
## Original signature(s)
  Overload 1: () => string
## JSDoc
Returns a \`<big>\` HTML element

## How to respond
- Behave EXACTLY as the original \`big\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function big(input: { string: string; prompt?: string }): Promise<string>;
export async function big(input: Record<string, unknown>): Promise<unknown> { return runMethod(__big_2_spec, input); }

/** `String.prototype.blink` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __blink_3_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'blink',
  functionId: 'String.prototype.blink',
  kind: 'instance',
  native: resolveNative('String.prototype.blink'),
  receiverKey: "string",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.blink\`.
## Original signature(s)
  Overload 1: () => string
## JSDoc
Returns a \`<blink>\` HTML element

## How to respond
- Behave EXACTLY as the original \`blink\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function blink(input: { string: string; prompt?: string }): Promise<string>;
export async function blink(input: Record<string, unknown>): Promise<unknown> { return runMethod(__blink_3_spec, input); }

/** `String.prototype.bold` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __bold_4_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'bold',
  functionId: 'String.prototype.bold',
  kind: 'instance',
  native: resolveNative('String.prototype.bold'),
  receiverKey: "string",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.bold\`.
## Original signature(s)
  Overload 1: () => string
## JSDoc
Returns a \`<b>\` HTML element

## How to respond
- Behave EXACTLY as the original \`bold\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function bold(input: { string: string; prompt?: string }): Promise<string>;
export async function bold(input: Record<string, unknown>): Promise<unknown> { return runMethod(__bold_4_spec, input); }

/** `String.prototype.charAt` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __charAt_5_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'charAt',
  functionId: 'String.prototype.charAt',
  kind: 'instance',
  native: resolveNative('String.prototype.charAt'),
  receiverKey: "string",
  paramOrder: ["pos"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.charAt\`.
## Original signature(s)
  Overload 1: (pos: number) => string
## JSDoc
Returns the character at the specified index.

## How to respond
- Behave EXACTLY as the original \`charAt\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"pos","type":"number"}],
};
export function charAt(input: { string: string; pos: number; prompt?: string }): Promise<string>;
export async function charAt(input: Record<string, unknown>): Promise<unknown> { return runMethod(__charAt_5_spec, input); }

/** `String.prototype.charCodeAt` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __charCodeAt_6_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'charCodeAt',
  functionId: 'String.prototype.charCodeAt',
  kind: 'instance',
  native: resolveNative('String.prototype.charCodeAt'),
  receiverKey: "string",
  paramOrder: ["index"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.charCodeAt\`.
## Original signature(s)
  Overload 1: (index: number) => number
## JSDoc
Returns the Unicode value of the character at the specified location.

## How to respond
- Behave EXACTLY as the original \`charCodeAt\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"index","type":"number"}],
};
export function charCodeAt(input: { string: string; index: number; prompt?: string }): Promise<number>;
export async function charCodeAt(input: Record<string, unknown>): Promise<unknown> { return runMethod(__charCodeAt_6_spec, input); }

/** `String.prototype.codePointAt` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __codePointAt_7_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'codePointAt',
  functionId: 'String.prototype.codePointAt',
  kind: 'instance',
  native: resolveNative('String.prototype.codePointAt'),
  receiverKey: "string",
  paramOrder: ["pos"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.codePointAt\`.
## Original signature(s)
  Overload 1: (pos: number) => number
## JSDoc
Returns a nonnegative integer Number less than 1114112 (0x110000) that is the code point
value of the UTF-16 encoded code point starting at the string element at position pos in
the String resulting from converting this object to a String.
If there is no element at that position, the result is undefined.
If a valid UTF-16 surrogate pair does not begin at pos, the result is the code unit at pos.

## How to respond
- Behave EXACTLY as the original \`codePointAt\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"pos","type":"number"}],
};
export function codePointAt(input: { string: string; pos: number; prompt?: string }): Promise<number>;
export async function codePointAt(input: Record<string, unknown>): Promise<unknown> { return runMethod(__codePointAt_7_spec, input); }

/** `String.prototype.concat` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __concat_8_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'concat',
  functionId: 'String.prototype.concat',
  kind: 'instance',
  native: resolveNative('String.prototype.concat'),
  receiverKey: "string",
  paramOrder: ["strings"],
  variadicKey: "strings",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.concat\`.
## Original signature(s)
  Overload 1: (...strings: string[]) => string
## JSDoc
Returns a string that contains the concatenation of two or more strings.

## How to respond
- Behave EXACTLY as the original \`concat\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"strings","type":"string[]"}],
};
export function concat(input: { string: string; strings?: (string)[]; prompt?: string }): Promise<string>;
export async function concat(input: Record<string, unknown>): Promise<unknown> { return runMethod(__concat_8_spec, input); }

/** `String.prototype.endsWith` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __endsWith_9_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'endsWith',
  functionId: 'String.prototype.endsWith',
  kind: 'instance',
  native: resolveNative('String.prototype.endsWith'),
  receiverKey: "string",
  paramOrder: ["searchString","endPosition"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.endsWith\`.
## Original signature(s)
  Overload 1: (searchString: string, endPosition?: number) => boolean
## JSDoc
Returns true if the sequence of elements of searchString converted to a String is the
same as the corresponding elements of this object (converted to a String) starting at
endPosition - length(this). Otherwise returns false.

## How to respond
- Behave EXACTLY as the original \`endsWith\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"searchString","type":"string"},{"name":"endPosition?","type":"number"}],
};
export function endsWith(input: { string: string; searchString: string; endPosition?: number; prompt?: string }): Promise<boolean>;
export async function endsWith(input: Record<string, unknown>): Promise<unknown> { return runMethod(__endsWith_9_spec, input); }

/** `String.prototype.fixed` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __fixed_10_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'fixed',
  functionId: 'String.prototype.fixed',
  kind: 'instance',
  native: resolveNative('String.prototype.fixed'),
  receiverKey: "string",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.fixed\`.
## Original signature(s)
  Overload 1: () => string
## JSDoc
Returns a \`<tt>\` HTML element

## How to respond
- Behave EXACTLY as the original \`fixed\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function fixed(input: { string: string; prompt?: string }): Promise<string>;
export async function fixed(input: Record<string, unknown>): Promise<unknown> { return runMethod(__fixed_10_spec, input); }

/** `String.prototype.fontcolor` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __fontcolor_11_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'fontcolor',
  functionId: 'String.prototype.fontcolor',
  kind: 'instance',
  native: resolveNative('String.prototype.fontcolor'),
  receiverKey: "string",
  paramOrder: ["color"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.fontcolor\`.
## Original signature(s)
  Overload 1: (color: string) => string
## JSDoc
Returns a \`<font>\` HTML element and sets the color attribute value

## How to respond
- Behave EXACTLY as the original \`fontcolor\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"color","type":"string"}],
};
export function fontcolor(input: { string: string; color: string; prompt?: string }): Promise<string>;
export async function fontcolor(input: Record<string, unknown>): Promise<unknown> { return runMethod(__fontcolor_11_spec, input); }

/** `String.prototype.fontsize` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __fontsize_12_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'fontsize',
  functionId: 'String.prototype.fontsize',
  kind: 'instance',
  native: resolveNative('String.prototype.fontsize'),
  receiverKey: "string",
  paramOrder: ["size"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.fontsize\`.
## Original signature(s)
  Overload 1: (size: number) => string
  Overload 2: (size: string) => string
## JSDoc
Returns a \`<font>\` HTML element and sets the size attribute value

## How to respond
- Behave EXACTLY as the original \`fontsize\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"size","type":"number"}],
};
export function fontsize(input: { string: string; size: number; prompt?: string }): Promise<string>;
export function fontsize(input: { string: string; size: string; prompt?: string }): Promise<string>;
export async function fontsize(input: Record<string, unknown>): Promise<unknown> { return runMethod(__fontsize_12_spec, input); }

/** `String.fromCharCode` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __fromCharCode_13_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'fromCharCode',
  functionId: 'String.fromCharCode',
  kind: 'static',
  native: resolveNative('String.fromCharCode'),
  receiverKey: "",
  paramOrder: ["codes"],
  variadicKey: "codes",
  systemPrompt: `You are simulating the JavaScript built-in \`String.fromCharCode\`.
## Original signature(s)
  Overload 1: (...codes: number[]) => string
## How to respond
- Behave EXACTLY as the original \`fromCharCode\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"codes","type":"number[]"}],
};
export function fromCharCode(input: { codes?: (number)[]; prompt?: string }): Promise<string>;
export async function fromCharCode(input: Record<string, unknown>): Promise<unknown> { return runMethod(__fromCharCode_13_spec, input); }

/** `String.fromCodePoint` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __fromCodePoint_14_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'fromCodePoint',
  functionId: 'String.fromCodePoint',
  kind: 'static',
  native: resolveNative('String.fromCodePoint'),
  receiverKey: "",
  paramOrder: ["codePoints"],
  variadicKey: "codePoints",
  systemPrompt: `You are simulating the JavaScript built-in \`String.fromCodePoint\`.
## Original signature(s)
  Overload 1: (...codePoints: number[]) => string
## JSDoc
Return the String value whose elements are, in order, the elements in the List elements.
If length is 0, the empty string is returned.

## How to respond
- Behave EXACTLY as the original \`fromCodePoint\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"codePoints","type":"number[]"}],
};
export function fromCodePoint(input: { codePoints?: (number)[]; prompt?: string }): Promise<string>;
export async function fromCodePoint(input: Record<string, unknown>): Promise<unknown> { return runMethod(__fromCodePoint_14_spec, input); }

/** `String.prototype.includes` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __includes_15_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'includes',
  functionId: 'String.prototype.includes',
  kind: 'instance',
  native: resolveNative('String.prototype.includes'),
  receiverKey: "string",
  paramOrder: ["searchString","position"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.includes\`.
## Original signature(s)
  Overload 1: (searchString: string, position?: number) => boolean
## JSDoc
Returns true if searchString appears as a substring of the result of converting this
object to a String, at one or more positions that are
greater than or equal to position; otherwise, returns false.

## How to respond
- Behave EXACTLY as the original \`includes\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"searchString","type":"string"},{"name":"position?","type":"number"}],
};
export function includes(input: { string: string; searchString: string; position?: number; prompt?: string }): Promise<boolean>;
export async function includes(input: Record<string, unknown>): Promise<unknown> { return runMethod(__includes_15_spec, input); }

/** `String.prototype.indexOf` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __indexOf_16_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'indexOf',
  functionId: 'String.prototype.indexOf',
  kind: 'instance',
  native: resolveNative('String.prototype.indexOf'),
  receiverKey: "string",
  paramOrder: ["searchString","position"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.indexOf\`.
## Original signature(s)
  Overload 1: (searchString: string, position?: number) => number
## JSDoc
Returns the position of the first occurrence of a substring, or -1 if it is not present.

## How to respond
- Behave EXACTLY as the original \`indexOf\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"searchString","type":"string"},{"name":"position?","type":"number"}],
};
export function indexOf(input: { string: string; searchString: string; position?: number; prompt?: string }): Promise<number>;
export async function indexOf(input: Record<string, unknown>): Promise<unknown> { return runMethod(__indexOf_16_spec, input); }

/** `String.prototype.isWellFormed` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __isWellFormed_17_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'isWellFormed',
  functionId: 'String.prototype.isWellFormed',
  kind: 'instance',
  native: resolveNative('String.prototype.isWellFormed'),
  receiverKey: "string",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.isWellFormed\`.
## Original signature(s)
  Overload 1: () => boolean
## JSDoc
Returns true if all leading surrogates and trailing surrogates appear paired and in order.

## How to respond
- Behave EXACTLY as the original \`isWellFormed\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function isWellFormed(input: { string: string; prompt?: string }): Promise<boolean>;
export async function isWellFormed(input: Record<string, unknown>): Promise<unknown> { return runMethod(__isWellFormed_17_spec, input); }

/** `String.prototype.italics` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __italics_18_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'italics',
  functionId: 'String.prototype.italics',
  kind: 'instance',
  native: resolveNative('String.prototype.italics'),
  receiverKey: "string",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.italics\`.
## Original signature(s)
  Overload 1: () => string
## JSDoc
Returns an \`<i>\` HTML element

## How to respond
- Behave EXACTLY as the original \`italics\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function italics(input: { string: string; prompt?: string }): Promise<string>;
export async function italics(input: Record<string, unknown>): Promise<unknown> { return runMethod(__italics_18_spec, input); }

/** `String.prototype.lastIndexOf` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __lastIndexOf_19_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'lastIndexOf',
  functionId: 'String.prototype.lastIndexOf',
  kind: 'instance',
  native: resolveNative('String.prototype.lastIndexOf'),
  receiverKey: "string",
  paramOrder: ["searchString","position"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.lastIndexOf\`.
## Original signature(s)
  Overload 1: (searchString: string, position?: number) => number
## JSDoc
Returns the last occurrence of a substring in the string, or -1 if it is not present.

## How to respond
- Behave EXACTLY as the original \`lastIndexOf\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"searchString","type":"string"},{"name":"position?","type":"number"}],
};
export function lastIndexOf(input: { string: string; searchString: string; position?: number; prompt?: string }): Promise<number>;
export async function lastIndexOf(input: Record<string, unknown>): Promise<unknown> { return runMethod(__lastIndexOf_19_spec, input); }

/** `String.prototype.link` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __link_20_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'link',
  functionId: 'String.prototype.link',
  kind: 'instance',
  native: resolveNative('String.prototype.link'),
  receiverKey: "string",
  paramOrder: ["url"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.link\`.
## Original signature(s)
  Overload 1: (url: string) => string
## JSDoc
Returns an \`<a>\` HTML element and sets the href attribute value

## How to respond
- Behave EXACTLY as the original \`link\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"url","type":"string"}],
};
export function link(input: { string: string; url: string; prompt?: string }): Promise<string>;
export async function link(input: Record<string, unknown>): Promise<unknown> { return runMethod(__link_20_spec, input); }

/** `String.prototype.localeCompare` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __localeCompare_21_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'localeCompare',
  functionId: 'String.prototype.localeCompare',
  kind: 'instance',
  native: resolveNative('String.prototype.localeCompare'),
  receiverKey: "string",
  paramOrder: ["that","locales","options"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.localeCompare\`.
## Original signature(s)
  Overload 1: (that: string) => number
  Overload 2: (that: string, locales?: string | string[], options?: Intl.CollatorOptions) => number
  Overload 3: (that: string, locales?: Intl.LocalesArgument, options?: Intl.CollatorOptions) => number
## JSDoc
Determines whether two strings are equivalent in the current locale.

## How to respond
- Behave EXACTLY as the original \`localeCompare\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"that","type":"string"}],
};
export function localeCompare(input: { string: string; that: string; prompt?: string }): Promise<number>;
export function localeCompare(input: { string: string; that: string; locales?: string | string[]; options?: Intl.CollatorOptions; prompt?: string }): Promise<number>;
export function localeCompare(input: { string: string; that: string; locales?: Intl.LocalesArgument; options?: Intl.CollatorOptions; prompt?: string }): Promise<number>;
export async function localeCompare(input: Record<string, unknown>): Promise<unknown> { return runMethod(__localeCompare_21_spec, input); }

/** `String.prototype.match` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __match_22_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'match',
  functionId: 'String.prototype.match',
  kind: 'instance',
  native: resolveNative('String.prototype.match'),
  receiverKey: "string",
  paramOrder: ["regexp","matcher"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.match\`.
## Original signature(s)
  Overload 1: (regexp: string | RegExp) => RegExpMatchArray
  Overload 2: (matcher: { [Symbol.match](string: string): RegExpMatchArray | null; }) => RegExpMatchArray
## JSDoc
Matches a string with a regular expression, and returns an array containing the results of that search.

## How to respond
- Behave EXACTLY as the original \`match\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"regexp","type":"string | RegExp"}],
};
export function match(input: { string: string; regexp: string | RegExp; prompt?: string }): Promise<RegExpMatchArray>;
export function match(input: { string: string; matcher: { [Symbol.match](string: string): RegExpMatchArray | null; }; prompt?: string }): Promise<RegExpMatchArray>;
export async function match(input: Record<string, unknown>): Promise<unknown> { return runMethod(__match_22_spec, input); }

/** `String.prototype.matchAll` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __matchAll_23_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'matchAll',
  functionId: 'String.prototype.matchAll',
  kind: 'instance',
  native: resolveNative('String.prototype.matchAll'),
  receiverKey: "string",
  paramOrder: ["regexp"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.matchAll\`.
## Original signature(s)
  Overload 1: (regexp: RegExp) => RegExpStringIterator<RegExpExecArray>
## JSDoc
Matches a string with a regular expression, and returns an iterable of matches
containing the results of that search.

## How to respond
- Behave EXACTLY as the original \`matchAll\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"regexp","type":"RegExp"}],
};
export function matchAll(input: { string: string; regexp: RegExp; prompt?: string }): Promise<RegExpStringIterator<RegExpExecArray>>;
export async function matchAll(input: Record<string, unknown>): Promise<unknown> { return runMethod(__matchAll_23_spec, input); }

/** `String.prototype.normalize` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __normalize_24_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'normalize',
  functionId: 'String.prototype.normalize',
  kind: 'instance',
  native: resolveNative('String.prototype.normalize'),
  receiverKey: "string",
  paramOrder: ["form"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.normalize\`.
## Original signature(s)
  Overload 1: (form: "NFC" | "NFD" | "NFKC" | "NFKD") => string
  Overload 2: (form?: string) => string
## JSDoc
Returns the String value result of normalizing the string into the normalization form
named by form as specified in Unicode Standard Annex #15, Unicode Normalization Forms.

## How to respond
- Behave EXACTLY as the original \`normalize\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"form","type":"\"NFC\" | \"NFD\" | \"NFKC\" | \"NFKD\""}],
};
export function normalize(input: { string: string; form: "NFC" | "NFD" | "NFKC" | "NFKD"; prompt?: string }): Promise<string>;
export function normalize(input: { string: string; form?: string; prompt?: string }): Promise<string>;
export async function normalize(input: Record<string, unknown>): Promise<unknown> { return runMethod(__normalize_24_spec, input); }

/** `String.prototype.padEnd` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __padEnd_25_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'padEnd',
  functionId: 'String.prototype.padEnd',
  kind: 'instance',
  native: resolveNative('String.prototype.padEnd'),
  receiverKey: "string",
  paramOrder: ["maxLength","fillString"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.padEnd\`.
## Original signature(s)
  Overload 1: (maxLength: number, fillString?: string) => string
## JSDoc
Pads the current string with a given string (possibly repeated) so that the resulting string reaches a given length.
The padding is applied from the end (right) of the current string.

## How to respond
- Behave EXACTLY as the original \`padEnd\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"maxLength","type":"number"},{"name":"fillString?","type":"string"}],
};
export function padEnd(input: { string: string; maxLength: number; fillString?: string; prompt?: string }): Promise<string>;
export async function padEnd(input: Record<string, unknown>): Promise<unknown> { return runMethod(__padEnd_25_spec, input); }

/** `String.prototype.padStart` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __padStart_26_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'padStart',
  functionId: 'String.prototype.padStart',
  kind: 'instance',
  native: resolveNative('String.prototype.padStart'),
  receiverKey: "string",
  paramOrder: ["maxLength","fillString"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.padStart\`.
## Original signature(s)
  Overload 1: (maxLength: number, fillString?: string) => string
## JSDoc
Pads the current string with a given string (possibly repeated) so that the resulting string reaches a given length.
The padding is applied from the start (left) of the current string.

## How to respond
- Behave EXACTLY as the original \`padStart\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"maxLength","type":"number"},{"name":"fillString?","type":"string"}],
};
export function padStart(input: { string: string; maxLength: number; fillString?: string; prompt?: string }): Promise<string>;
export async function padStart(input: Record<string, unknown>): Promise<unknown> { return runMethod(__padStart_26_spec, input); }

/** `String.raw` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __raw_27_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'raw',
  functionId: 'String.raw',
  kind: 'static',
  native: resolveNative('String.raw'),
  receiverKey: "",
  paramOrder: ["template","substitutions"],
  variadicKey: "substitutions",
  systemPrompt: `You are simulating the JavaScript built-in \`String.raw\`.
## Original signature(s)
  Overload 1: (template: { raw: readonly string[] | ArrayLike<string>; }, ...substitutions: any[]) => string
## JSDoc
String.raw is usually used as a tag function of a Tagged Template String. When called as
such, the first argument will be a well formed template call site object and the rest
parameter will contain the substitution values. It can also be called directly, for example,
to interleave strings and values from your own tag function, and in this case the only thing
it needs from the first argument is the raw property.

## How to respond
- Behave EXACTLY as the original \`raw\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"template","type":"{ raw: readonly string[] | ArrayLike<string>; }"},{"name":"substitutions","type":"any[]"}],
};
export function raw(input: { template: { raw: readonly string[] | ArrayLike<string>; }; substitutions?: (any)[]; prompt?: string }): Promise<string>;
export async function raw(input: Record<string, unknown>): Promise<unknown> { return runMethod(__raw_27_spec, input); }

/** `String.prototype.repeat` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __repeat_28_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'repeat',
  functionId: 'String.prototype.repeat',
  kind: 'instance',
  native: resolveNative('String.prototype.repeat'),
  receiverKey: "string",
  paramOrder: ["count"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.repeat\`.
## Original signature(s)
  Overload 1: (count: number) => string
## JSDoc
Returns a String value that is made from count copies appended together. If count is 0,
the empty string is returned.

## How to respond
- Behave EXACTLY as the original \`repeat\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"count","type":"number"}],
};
export function repeat(input: { string: string; count: number; prompt?: string }): Promise<string>;
export async function repeat(input: Record<string, unknown>): Promise<unknown> { return runMethod(__repeat_28_spec, input); }

/** `String.prototype.replace` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __replace_29_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'replace',
  functionId: 'String.prototype.replace',
  kind: 'instance',
  native: resolveNative('String.prototype.replace'),
  receiverKey: "string",
  paramOrder: ["searchValue","replaceValue","replacer"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.replace\`.
## Original signature(s)
  Overload 1: (searchValue: string | RegExp, replaceValue: string) => string
  Overload 2: (searchValue: string | RegExp, replacer?: (substring: string, ...args: any[]) => string) => string
  Overload 3: (searchValue: { [Symbol.replace](string: string, replaceValue: string): string; }, replaceValue: string) => string
  Overload 4: (searchValue?: { [Symbol.replace](string: string, replacer: (substring: string, ...args: any[]) => string): string; }, replacer?: (substring: string, ...args: any[]) => string) => string
## JSDoc
Replaces text in a string, using a regular expression or search string.

## How to respond
- Behave EXACTLY as the original \`replace\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"searchValue","type":"string | RegExp"},{"name":"replaceValue","type":"string"}],
};
export function replace(input: { string: string; searchValue: string | RegExp; replaceValue: string; prompt?: string }): Promise<string>;
export function replace(input: { string: string; searchValue: string | RegExp; replacer?: (substring: string, ...args: any[]) => string; prompt?: string }): Promise<string>;
export function replace(input: { string: string; searchValue: { [Symbol.replace](string: string, replaceValue: string): string; }; replaceValue: string; prompt?: string }): Promise<string>;
export function replace(input: { string: string; searchValue?: { [Symbol.replace](string: string, replacer: (substring: string, ...args: any[]) => string): string; }; replacer?: (substring: string, ...args: any[]) => string; prompt?: string }): Promise<string>;
export async function replace(input: Record<string, unknown>): Promise<unknown> { return runMethod(__replace_29_spec, input); }

/** `String.prototype.replaceAll` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __replaceAll_30_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'replaceAll',
  functionId: 'String.prototype.replaceAll',
  kind: 'instance',
  native: resolveNative('String.prototype.replaceAll'),
  receiverKey: "string",
  paramOrder: ["searchValue","replaceValue","replacer"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.replaceAll\`.
## Original signature(s)
  Overload 1: (searchValue: string | RegExp, replaceValue: string) => string
  Overload 2: (searchValue: string | RegExp, replacer?: (substring: string, ...args: any[]) => string) => string
## JSDoc
Replace all instances of a substring in a string, using a regular expression or search string.

## How to respond
- Behave EXACTLY as the original \`replaceAll\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"searchValue","type":"string | RegExp"},{"name":"replaceValue","type":"string"}],
};
export function replaceAll(input: { string: string; searchValue: string | RegExp; replaceValue: string; prompt?: string }): Promise<string>;
export function replaceAll(input: { string: string; searchValue: string | RegExp; replacer?: (substring: string, ...args: any[]) => string; prompt?: string }): Promise<string>;
export async function replaceAll(input: Record<string, unknown>): Promise<unknown> { return runMethod(__replaceAll_30_spec, input); }

/** `String.prototype.search` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __search_31_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'search',
  functionId: 'String.prototype.search',
  kind: 'instance',
  native: resolveNative('String.prototype.search'),
  receiverKey: "string",
  paramOrder: ["regexp","searcher"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.search\`.
## Original signature(s)
  Overload 1: (regexp: string | RegExp) => number
  Overload 2: (searcher: { [Symbol.search](string: string): number; }) => number
## JSDoc
Finds the first substring match in a regular expression search.

## How to respond
- Behave EXACTLY as the original \`search\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"regexp","type":"string | RegExp"}],
};
export function search(input: { string: string; regexp: string | RegExp; prompt?: string }): Promise<number>;
export function search(input: { string: string; searcher: { [Symbol.search](string: string): number; }; prompt?: string }): Promise<number>;
export async function search(input: Record<string, unknown>): Promise<unknown> { return runMethod(__search_31_spec, input); }

/** `String.prototype.slice` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __slice_32_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'slice',
  functionId: 'String.prototype.slice',
  kind: 'instance',
  native: resolveNative('String.prototype.slice'),
  receiverKey: "string",
  paramOrder: ["start","end"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.slice\`.
## Original signature(s)
  Overload 1: (start?: number, end?: number) => string
## JSDoc
Returns a section of a string.

## How to respond
- Behave EXACTLY as the original \`slice\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"start?","type":"number"},{"name":"end?","type":"number"}],
};
export function slice(input: { string: string; start?: number; end?: number; prompt?: string }): Promise<string>;
export async function slice(input: Record<string, unknown>): Promise<unknown> { return runMethod(__slice_32_spec, input); }

/** `String.prototype.small` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __small_33_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'small',
  functionId: 'String.prototype.small',
  kind: 'instance',
  native: resolveNative('String.prototype.small'),
  receiverKey: "string",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.small\`.
## Original signature(s)
  Overload 1: () => string
## JSDoc
Returns a \`<small>\` HTML element

## How to respond
- Behave EXACTLY as the original \`small\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function small(input: { string: string; prompt?: string }): Promise<string>;
export async function small(input: Record<string, unknown>): Promise<unknown> { return runMethod(__small_33_spec, input); }

/** `String.prototype.split` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __split_34_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'split',
  functionId: 'String.prototype.split',
  kind: 'instance',
  native: resolveNative('String.prototype.split'),
  receiverKey: "string",
  paramOrder: ["separator","limit","splitter"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.split\`.
## Original signature(s)
  Overload 1: (separator: string | RegExp, limit?: number) => string[]
  Overload 2: (splitter: { [Symbol.split](string: string, limit?: number): string[]; }, limit?: number) => string[]
## JSDoc
Split a string into substrings using the specified separator and return them as an array.

## How to respond
- Behave EXACTLY as the original \`split\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"separator","type":"string | RegExp"},{"name":"limit?","type":"number"}],
};
export function split(input: { string: string; separator: string | RegExp; limit?: number; prompt?: string }): Promise<string[]>;
export function split(input: { string: string; splitter: { [Symbol.split](string: string, limit?: number): string[]; }; limit?: number; prompt?: string }): Promise<string[]>;
export async function split(input: Record<string, unknown>): Promise<unknown> { return runMethod(__split_34_spec, input); }

/** `String.prototype.startsWith` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __startsWith_35_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'startsWith',
  functionId: 'String.prototype.startsWith',
  kind: 'instance',
  native: resolveNative('String.prototype.startsWith'),
  receiverKey: "string",
  paramOrder: ["searchString","position"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.startsWith\`.
## Original signature(s)
  Overload 1: (searchString: string, position?: number) => boolean
## JSDoc
Returns true if the sequence of elements of searchString converted to a String is the
same as the corresponding elements of this object (converted to a String) starting at
position. Otherwise returns false.

## How to respond
- Behave EXACTLY as the original \`startsWith\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"searchString","type":"string"},{"name":"position?","type":"number"}],
};
export function startsWith(input: { string: string; searchString: string; position?: number; prompt?: string }): Promise<boolean>;
export async function startsWith(input: Record<string, unknown>): Promise<unknown> { return runMethod(__startsWith_35_spec, input); }

/** `String.prototype.strike` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __strike_36_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'strike',
  functionId: 'String.prototype.strike',
  kind: 'instance',
  native: resolveNative('String.prototype.strike'),
  receiverKey: "string",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.strike\`.
## Original signature(s)
  Overload 1: () => string
## JSDoc
Returns a \`<strike>\` HTML element

## How to respond
- Behave EXACTLY as the original \`strike\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function strike(input: { string: string; prompt?: string }): Promise<string>;
export async function strike(input: Record<string, unknown>): Promise<unknown> { return runMethod(__strike_36_spec, input); }

/** `String.prototype.sub` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __sub_37_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'sub',
  functionId: 'String.prototype.sub',
  kind: 'instance',
  native: resolveNative('String.prototype.sub'),
  receiverKey: "string",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.sub\`.
## Original signature(s)
  Overload 1: () => string
## JSDoc
Returns a \`<sub>\` HTML element

## How to respond
- Behave EXACTLY as the original \`sub\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function sub(input: { string: string; prompt?: string }): Promise<string>;
export async function sub(input: Record<string, unknown>): Promise<unknown> { return runMethod(__sub_37_spec, input); }

/** `String.prototype.substr` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __substr_38_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'substr',
  functionId: 'String.prototype.substr',
  kind: 'instance',
  native: resolveNative('String.prototype.substr'),
  receiverKey: "string",
  paramOrder: ["from","length"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.substr\`.
## Original signature(s)
  Overload 1: (from: number, length?: number) => string
## JSDoc
Gets a substring beginning at the specified location and having the specified length.

## How to respond
- Behave EXACTLY as the original \`substr\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"from","type":"number"},{"name":"length?","type":"number"}],
};
export function substr(input: { string: string; from: number; length?: number; prompt?: string }): Promise<string>;
export async function substr(input: Record<string, unknown>): Promise<unknown> { return runMethod(__substr_38_spec, input); }

/** `String.prototype.substring` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __substring_39_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'substring',
  functionId: 'String.prototype.substring',
  kind: 'instance',
  native: resolveNative('String.prototype.substring'),
  receiverKey: "string",
  paramOrder: ["start","end"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.substring\`.
## Original signature(s)
  Overload 1: (start: number, end?: number) => string
## JSDoc
Returns the substring at the specified location within a String object.

## How to respond
- Behave EXACTLY as the original \`substring\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"start","type":"number"},{"name":"end?","type":"number"}],
};
export function substring(input: { string: string; start: number; end?: number; prompt?: string }): Promise<string>;
export async function substring(input: Record<string, unknown>): Promise<unknown> { return runMethod(__substring_39_spec, input); }

/** `String.prototype.sup` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __sup_40_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'sup',
  functionId: 'String.prototype.sup',
  kind: 'instance',
  native: resolveNative('String.prototype.sup'),
  receiverKey: "string",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.sup\`.
## Original signature(s)
  Overload 1: () => string
## JSDoc
Returns a \`<sup>\` HTML element

## How to respond
- Behave EXACTLY as the original \`sup\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function sup(input: { string: string; prompt?: string }): Promise<string>;
export async function sup(input: Record<string, unknown>): Promise<unknown> { return runMethod(__sup_40_spec, input); }

/** `String.prototype.toLocaleLowerCase` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toLocaleLowerCase_41_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'toLocaleLowerCase',
  functionId: 'String.prototype.toLocaleLowerCase',
  kind: 'instance',
  native: resolveNative('String.prototype.toLocaleLowerCase'),
  receiverKey: "string",
  paramOrder: ["locales"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.toLocaleLowerCase\`.
## Original signature(s)
  Overload 1: (locales?: string | string[]) => string
  Overload 2: (locales?: Intl.LocalesArgument) => string
## JSDoc
Converts all alphabetic characters to lowercase, taking into account the host environment's current locale.

## How to respond
- Behave EXACTLY as the original \`toLocaleLowerCase\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"locales?","type":"string | string[]"}],
};
export function toLocaleLowerCase(input: { string: string; locales?: string | string[]; prompt?: string }): Promise<string>;
export function toLocaleLowerCase(input: { string: string; locales?: Intl.LocalesArgument; prompt?: string }): Promise<string>;
export async function toLocaleLowerCase(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toLocaleLowerCase_41_spec, input); }

/** `String.prototype.toLocaleUpperCase` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toLocaleUpperCase_42_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'toLocaleUpperCase',
  functionId: 'String.prototype.toLocaleUpperCase',
  kind: 'instance',
  native: resolveNative('String.prototype.toLocaleUpperCase'),
  receiverKey: "string",
  paramOrder: ["locales"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.toLocaleUpperCase\`.
## Original signature(s)
  Overload 1: (locales?: string | string[]) => string
  Overload 2: (locales?: Intl.LocalesArgument) => string
## JSDoc
Returns a string where all alphabetic characters have been converted to uppercase, taking into account the host environment's current locale.

## How to respond
- Behave EXACTLY as the original \`toLocaleUpperCase\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"locales?","type":"string | string[]"}],
};
export function toLocaleUpperCase(input: { string: string; locales?: string | string[]; prompt?: string }): Promise<string>;
export function toLocaleUpperCase(input: { string: string; locales?: Intl.LocalesArgument; prompt?: string }): Promise<string>;
export async function toLocaleUpperCase(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toLocaleUpperCase_42_spec, input); }

/** `String.prototype.toLowerCase` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toLowerCase_43_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'toLowerCase',
  functionId: 'String.prototype.toLowerCase',
  kind: 'instance',
  native: resolveNative('String.prototype.toLowerCase'),
  receiverKey: "string",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.toLowerCase\`.
## Original signature(s)
  Overload 1: () => string
## JSDoc
Converts all the alphabetic characters in a string to lowercase.

## How to respond
- Behave EXACTLY as the original \`toLowerCase\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function toLowerCase(input: { string: string; prompt?: string }): Promise<string>;
export async function toLowerCase(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toLowerCase_43_spec, input); }

/** `String.prototype.toUpperCase` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toUpperCase_44_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'toUpperCase',
  functionId: 'String.prototype.toUpperCase',
  kind: 'instance',
  native: resolveNative('String.prototype.toUpperCase'),
  receiverKey: "string",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.toUpperCase\`.
## Original signature(s)
  Overload 1: () => string
## JSDoc
Converts all the alphabetic characters in a string to uppercase.

## How to respond
- Behave EXACTLY as the original \`toUpperCase\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function toUpperCase(input: { string: string; prompt?: string }): Promise<string>;
export async function toUpperCase(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toUpperCase_44_spec, input); }

/** `String.prototype.toWellFormed` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toWellFormed_45_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'toWellFormed',
  functionId: 'String.prototype.toWellFormed',
  kind: 'instance',
  native: resolveNative('String.prototype.toWellFormed'),
  receiverKey: "string",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.toWellFormed\`.
## Original signature(s)
  Overload 1: () => string
## JSDoc
Returns a string where all lone or out-of-order surrogates have been replaced by the Unicode replacement character (U+FFFD).

## How to respond
- Behave EXACTLY as the original \`toWellFormed\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function toWellFormed(input: { string: string; prompt?: string }): Promise<string>;
export async function toWellFormed(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toWellFormed_45_spec, input); }

/** `String.prototype.trim` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __trim_46_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'trim',
  functionId: 'String.prototype.trim',
  kind: 'instance',
  native: resolveNative('String.prototype.trim'),
  receiverKey: "string",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.trim\`.
## Original signature(s)
  Overload 1: () => string
## JSDoc
Removes the leading and trailing white space and line terminator characters from a string.

## How to respond
- Behave EXACTLY as the original \`trim\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function trim(input: { string: string; prompt?: string }): Promise<string>;
export async function trim(input: Record<string, unknown>): Promise<unknown> { return runMethod(__trim_46_spec, input); }

/** `String.prototype.trimEnd` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __trimEnd_47_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'trimEnd',
  functionId: 'String.prototype.trimEnd',
  kind: 'instance',
  native: resolveNative('String.prototype.trimEnd'),
  receiverKey: "string",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.trimEnd\`.
## Original signature(s)
  Overload 1: () => string
## JSDoc
Removes the trailing white space and line terminator characters from a string.

## How to respond
- Behave EXACTLY as the original \`trimEnd\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function trimEnd(input: { string: string; prompt?: string }): Promise<string>;
export async function trimEnd(input: Record<string, unknown>): Promise<unknown> { return runMethod(__trimEnd_47_spec, input); }

/** `String.prototype.trimLeft` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __trimLeft_48_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'trimLeft',
  functionId: 'String.prototype.trimLeft',
  kind: 'instance',
  native: resolveNative('String.prototype.trimLeft'),
  receiverKey: "string",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.trimLeft\`.
## Original signature(s)
  Overload 1: () => string
## JSDoc
Removes the leading white space and line terminator characters from a string.

## How to respond
- Behave EXACTLY as the original \`trimLeft\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function trimLeft(input: { string: string; prompt?: string }): Promise<string>;
export async function trimLeft(input: Record<string, unknown>): Promise<unknown> { return runMethod(__trimLeft_48_spec, input); }

/** `String.prototype.trimRight` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __trimRight_49_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'trimRight',
  functionId: 'String.prototype.trimRight',
  kind: 'instance',
  native: resolveNative('String.prototype.trimRight'),
  receiverKey: "string",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.trimRight\`.
## Original signature(s)
  Overload 1: () => string
## JSDoc
Removes the trailing white space and line terminator characters from a string.

## How to respond
- Behave EXACTLY as the original \`trimRight\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function trimRight(input: { string: string; prompt?: string }): Promise<string>;
export async function trimRight(input: Record<string, unknown>): Promise<unknown> { return runMethod(__trimRight_49_spec, input); }

/** `String.prototype.trimStart` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __trimStart_50_spec: GeneratedMethodSpec = {
  group: 'string',
  methodName: 'trimStart',
  functionId: 'String.prototype.trimStart',
  kind: 'instance',
  native: resolveNative('String.prototype.trimStart'),
  receiverKey: "string",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`String.prototype.trimStart\`.
## Original signature(s)
  Overload 1: () => string
## JSDoc
Removes the leading white space and line terminator characters from a string.

## How to respond
- Behave EXACTLY as the original \`trimStart\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function trimStart(input: { string: string; prompt?: string }): Promise<string>;
export async function trimStart(input: Record<string, unknown>): Promise<unknown> { return runMethod(__trimStart_50_spec, input); }


