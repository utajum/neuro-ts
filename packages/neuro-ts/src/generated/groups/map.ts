// Auto-generated by scripts/generate-wrappers.ts. Do not edit.
import { runMethod, resolveNative, type GeneratedMethodSpec } from '../../runtime';

/** `Map.prototype.clear` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __clear_0_spec: GeneratedMethodSpec = {
  group: 'map',
  methodName: 'clear',
  functionId: 'Map.prototype.clear',
  kind: 'instance',
  native: resolveNative('Map.prototype.clear'),
  receiverKey: "map",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Map.prototype.clear\`.
## Original signature(s)
  Overload 1: () => void
## JSDoc
Removes all elements from the Map.

## How to respond
- Behave EXACTLY as the original \`clear\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function clear<V, K>(input: { map: Map<K, V>; prompt?: string }): Promise<void>;
export async function clear(input: Record<string, unknown>): Promise<unknown> { return runMethod(__clear_0_spec, input); }

/** `Map.prototype.delete` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __delete_1_spec: GeneratedMethodSpec = {
  group: 'map',
  methodName: 'delete',
  functionId: 'Map.prototype.delete',
  kind: 'instance',
  native: resolveNative('Map.prototype.delete'),
  receiverKey: "map",
  paramOrder: ["key"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Map.prototype.delete\`.
## Original signature(s)
  Overload 1: (key: K) => boolean
## How to respond
- Behave EXACTLY as the original \`delete\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"key","type":"K"}],
};
function _delete<V, K>(input: { map: Map<K, V>; key: K; prompt?: string }): Promise<boolean>;
async function _delete(input: Record<string, unknown>): Promise<unknown> { return runMethod(__delete_1_spec, input); }

/** `Map.prototype.entries` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __entries_2_spec: GeneratedMethodSpec = {
  group: 'map',
  methodName: 'entries',
  functionId: 'Map.prototype.entries',
  kind: 'instance',
  native: resolveNative('Map.prototype.entries'),
  receiverKey: "map",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Map.prototype.entries\`.
## Original signature(s)
  Overload 1: () => MapIterator<[K, V]>
## JSDoc
Returns an iterable of key, value pairs for every entry in the map.

## How to respond
- Behave EXACTLY as the original \`entries\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function entries<V, K>(input: { map: Map<K, V>; prompt?: string }): Promise<MapIterator<[K, V]>>;
export async function entries(input: Record<string, unknown>): Promise<unknown> { return runMethod(__entries_2_spec, input); }

/** `Map.prototype.forEach` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __forEach_3_spec: GeneratedMethodSpec = {
  group: 'map',
  methodName: 'forEach',
  functionId: 'Map.prototype.forEach',
  kind: 'instance',
  native: resolveNative('Map.prototype.forEach'),
  receiverKey: "map",
  paramOrder: ["callbackfn","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Map.prototype.forEach\`.
## Original signature(s)
  Overload 1: (callbackfn?: (value: V, key: K, map: Map<K, V>) => void, thisArg?: any) => void
## JSDoc
Executes a provided function once per each key/value pair in the Map, in insertion order.

## How to respond
- Behave EXACTLY as the original \`forEach\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"callbackfn?","type":"(value: V, key: K, map: Map<K, V>) => void"},{"name":"thisArg?","type":"any"}],
};
export function forEach<V, K>(input: { map: Map<K, V>; callbackfn?: (value: V, key: K, map: Map<K, V>) => void; thisArg?: any; prompt?: string }): Promise<void>;
export async function forEach(input: Record<string, unknown>): Promise<unknown> { return runMethod(__forEach_3_spec, input); }

/** `Map.prototype.get` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __get_4_spec: GeneratedMethodSpec = {
  group: 'map',
  methodName: 'get',
  functionId: 'Map.prototype.get',
  kind: 'instance',
  native: resolveNative('Map.prototype.get'),
  receiverKey: "map",
  paramOrder: ["key"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Map.prototype.get\`.
## Original signature(s)
  Overload 1: (key: K) => V
## JSDoc
Returns a specified element from the Map object. If the value that is associated to the provided key is an object, then you will get a reference to that object and any change made to that object will effectively modify it inside the Map.

## How to respond
- Behave EXACTLY as the original \`get\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"key","type":"K"}],
};
export function get<V, K>(input: { map: Map<K, V>; key: K; prompt?: string }): Promise<V>;
export async function get(input: Record<string, unknown>): Promise<unknown> { return runMethod(__get_4_spec, input); }

/** `Map.prototype.getOrInsert` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getOrInsert_5_spec: GeneratedMethodSpec = {
  group: 'map',
  methodName: 'getOrInsert',
  functionId: 'Map.prototype.getOrInsert',
  kind: 'instance',
  native: resolveNative('Map.prototype.getOrInsert'),
  receiverKey: "map",
  paramOrder: ["key","defaultValue"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Map.prototype.getOrInsert\`.
## Original signature(s)
  Overload 1: (key: K, defaultValue: V) => V
## JSDoc
Returns a specified element from the Map object.
If no element is associated with the specified key, a new element with the value \`defaultValue\` will be inserted into the Map and returned.

## How to respond
- Behave EXACTLY as the original \`getOrInsert\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"key","type":"K"},{"name":"defaultValue","type":"V"}],
};
export function getOrInsert<V, K>(input: { map: Map<K, V>; key: K; defaultValue: V; prompt?: string }): Promise<V>;
export async function getOrInsert(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getOrInsert_5_spec, input); }

/** `Map.prototype.getOrInsertComputed` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getOrInsertComputed_6_spec: GeneratedMethodSpec = {
  group: 'map',
  methodName: 'getOrInsertComputed',
  functionId: 'Map.prototype.getOrInsertComputed',
  kind: 'instance',
  native: resolveNative('Map.prototype.getOrInsertComputed'),
  receiverKey: "map",
  paramOrder: ["key","callback"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Map.prototype.getOrInsertComputed\`.
## Original signature(s)
  Overload 1: (key: K, callback?: (key: K) => V) => V
## JSDoc
Returns a specified element from the Map object.
If no element is associated with the specified key, the result of passing the specified key to the \`callback\` function will be inserted into the Map and returned.

## How to respond
- Behave EXACTLY as the original \`getOrInsertComputed\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"key","type":"K"},{"name":"callback?","type":"(key: K) => V"}],
};
export function getOrInsertComputed<V, K>(input: { map: Map<K, V>; key: K; callback?: (key: K) => V; prompt?: string }): Promise<V>;
export async function getOrInsertComputed(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getOrInsertComputed_6_spec, input); }

/** `Map.groupBy` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __groupBy_7_spec: GeneratedMethodSpec = {
  group: 'map',
  methodName: 'groupBy',
  functionId: 'Map.groupBy',
  kind: 'static',
  native: resolveNative('Map.groupBy'),
  receiverKey: "",
  paramOrder: ["items","keySelector"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Map.groupBy\`.
## Original signature(s)
  Overload 1: (items: Iterable<T>, keySelector?: (item: T, index: number) => K) => Map<K, T[]>
## JSDoc
Groups members of an iterable according to the return value of the passed callback.

## How to respond
- Behave EXACTLY as the original \`groupBy\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"items","type":"Iterable<T>"},{"name":"keySelector?","type":"(item: T, index: number) => K"}],
};
export function groupBy<K, T>(input: { items: Iterable<T>; keySelector?: (item: T, index: number) => K; prompt?: string }): Promise<Map<K, T[]>>;
export async function groupBy(input: Record<string, unknown>): Promise<unknown> { return runMethod(__groupBy_7_spec, input); }

/** `Map.prototype.has` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __has_8_spec: GeneratedMethodSpec = {
  group: 'map',
  methodName: 'has',
  functionId: 'Map.prototype.has',
  kind: 'instance',
  native: resolveNative('Map.prototype.has'),
  receiverKey: "map",
  paramOrder: ["key"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Map.prototype.has\`.
## Original signature(s)
  Overload 1: (key: K) => boolean
## How to respond
- Behave EXACTLY as the original \`has\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"key","type":"K"}],
};
export function has<V, K>(input: { map: Map<K, V>; key: K; prompt?: string }): Promise<boolean>;
export async function has(input: Record<string, unknown>): Promise<unknown> { return runMethod(__has_8_spec, input); }

/** `Map.prototype.keys` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __keys_9_spec: GeneratedMethodSpec = {
  group: 'map',
  methodName: 'keys',
  functionId: 'Map.prototype.keys',
  kind: 'instance',
  native: resolveNative('Map.prototype.keys'),
  receiverKey: "map",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Map.prototype.keys\`.
## Original signature(s)
  Overload 1: () => MapIterator<K>
## JSDoc
Returns an iterable of keys in the map

## How to respond
- Behave EXACTLY as the original \`keys\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function keys<V, K>(input: { map: Map<K, V>; prompt?: string }): Promise<MapIterator<K>>;
export async function keys(input: Record<string, unknown>): Promise<unknown> { return runMethod(__keys_9_spec, input); }

/** `Map.prototype.set` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __set_10_spec: GeneratedMethodSpec = {
  group: 'map',
  methodName: 'set',
  functionId: 'Map.prototype.set',
  kind: 'instance',
  native: resolveNative('Map.prototype.set'),
  receiverKey: "map",
  paramOrder: ["key","value"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Map.prototype.set\`.
## Original signature(s)
  Overload 1: (key: K, value: V) => Map<K, V>
## JSDoc
Adds a new element with a specified key and value to the Map. If an element with the same key already exists, the element will be updated.

## How to respond
- Behave EXACTLY as the original \`set\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"key","type":"K"},{"name":"value","type":"V"}],
};
export function set<V, K>(input: { map: Map<K, V>; key: K; value: V; prompt?: string }): Promise<Map<K, V>>;
export async function set(input: Record<string, unknown>): Promise<unknown> { return runMethod(__set_10_spec, input); }

/** `Map.prototype.values` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __values_11_spec: GeneratedMethodSpec = {
  group: 'map',
  methodName: 'values',
  functionId: 'Map.prototype.values',
  kind: 'instance',
  native: resolveNative('Map.prototype.values'),
  receiverKey: "map",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Map.prototype.values\`.
## Original signature(s)
  Overload 1: () => MapIterator<V>
## JSDoc
Returns an iterable of values in the map

## How to respond
- Behave EXACTLY as the original \`values\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function values<V, K>(input: { map: Map<K, V>; prompt?: string }): Promise<MapIterator<V>>;
export async function values(input: Record<string, unknown>): Promise<unknown> { return runMethod(__values_11_spec, input); }

export { _delete as delete };
