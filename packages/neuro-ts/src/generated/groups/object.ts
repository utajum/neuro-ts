// Auto-generated by scripts/generate-wrappers.ts. Do not edit.
import { runMethod, resolveNative, type GeneratedMethodSpec } from '../../runtime';

/** `Object.assign` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __assign_0_spec: GeneratedMethodSpec = {
  group: 'object',
  methodName: 'assign',
  functionId: 'Object.assign',
  kind: 'static',
  native: resolveNative('Object.assign'),
  receiverKey: "",
  paramOrder: ["target","source","source1","source2","source3","sources"],
  variadicKey: "sources",
  systemPrompt: `You are simulating the JavaScript built-in \`Object.assign\`.
## Original signature(s)
  Overload 1: (target: T, source: U) => T & U
  Overload 2: (target: T, source1: U, source2: V) => T & U & V
  Overload 3: (target: T, source1: U, source2: V, source3: W) => T & U & V & W
  Overload 4: (target: object, ...sources: any[]) => any
## JSDoc
Copy the values of all of the enumerable own properties from one or more source objects to a
target object. Returns the target object.

## How to respond
- Behave EXACTLY as the original \`assign\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"target","type":"T"},{"name":"source","type":"U"}],
};
export function assign<T extends {}, U>(input: { target: T; source: U; prompt?: string }): Promise<T & U>;
export function assign<T extends {}, U, V>(input: { target: T; source1: U; source2: V; prompt?: string }): Promise<T & U & V>;
export function assign<T extends {}, U, V, W>(input: { target: T; source1: U; source2: V; source3: W; prompt?: string }): Promise<T & U & V & W>;
export function assign(input: { target: object; sources?: (any)[]; prompt?: string }): Promise<any>;
export async function assign(input: Record<string, unknown>): Promise<unknown> { return runMethod(__assign_0_spec, input); }

/** `Object.create` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __create_1_spec: GeneratedMethodSpec = {
  group: 'object',
  methodName: 'create',
  functionId: 'Object.create',
  kind: 'static',
  native: resolveNative('Object.create'),
  receiverKey: "",
  paramOrder: ["o","properties"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Object.create\`.
## Original signature(s)
  Overload 1: (o: object) => any
  Overload 2: (o: object, properties: PropertyDescriptorMap & ThisType<any>) => any
## JSDoc
Creates an object that has the specified prototype or that has null prototype.

## How to respond
- Behave EXACTLY as the original \`create\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"o","type":"object"}],
};
export function create(input: { o: object; prompt?: string }): Promise<any>;
export function create(input: { o: object; properties: PropertyDescriptorMap & ThisType<any>; prompt?: string }): Promise<any>;
export async function create(input: Record<string, unknown>): Promise<unknown> { return runMethod(__create_1_spec, input); }

/** `Object.defineProperties` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __defineProperties_2_spec: GeneratedMethodSpec = {
  group: 'object',
  methodName: 'defineProperties',
  functionId: 'Object.defineProperties',
  kind: 'static',
  native: resolveNative('Object.defineProperties'),
  receiverKey: "",
  paramOrder: ["o","properties"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Object.defineProperties\`.
## Original signature(s)
  Overload 1: (o: T, properties: PropertyDescriptorMap & ThisType<any>) => T
## JSDoc
Adds one or more properties to an object, and/or modifies attributes of existing properties.

## How to respond
- Behave EXACTLY as the original \`defineProperties\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"o","type":"T"},{"name":"properties","type":"PropertyDescriptorMap & ThisType<any>"}],
};
export function defineProperties<T>(input: { o: T; properties: PropertyDescriptorMap & ThisType<any>; prompt?: string }): Promise<T>;
export async function defineProperties(input: Record<string, unknown>): Promise<unknown> { return runMethod(__defineProperties_2_spec, input); }

/** `Object.defineProperty` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __defineProperty_3_spec: GeneratedMethodSpec = {
  group: 'object',
  methodName: 'defineProperty',
  functionId: 'Object.defineProperty',
  kind: 'static',
  native: resolveNative('Object.defineProperty'),
  receiverKey: "",
  paramOrder: ["o","p","attributes"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Object.defineProperty\`.
## Original signature(s)
  Overload 1: (o: T, p: PropertyKey, attributes: PropertyDescriptor & ThisType<any>) => T
## JSDoc
Adds a property to an object, or modifies attributes of an existing property.

## How to respond
- Behave EXACTLY as the original \`defineProperty\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"o","type":"T"},{"name":"p","type":"PropertyKey"},{"name":"attributes","type":"PropertyDescriptor & ThisType<any>"}],
};
export function defineProperty<T>(input: { o: T; p: PropertyKey; attributes: PropertyDescriptor & ThisType<any>; prompt?: string }): Promise<T>;
export async function defineProperty(input: Record<string, unknown>): Promise<unknown> { return runMethod(__defineProperty_3_spec, input); }

/** `Object.entries` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __entries_4_spec: GeneratedMethodSpec = {
  group: 'object',
  methodName: 'entries',
  functionId: 'Object.entries',
  kind: 'static',
  native: resolveNative('Object.entries'),
  receiverKey: "",
  paramOrder: ["o"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Object.entries\`.
## Original signature(s)
  Overload 1: (o: { [s: string]: T; } | ArrayLike<T>) => [string, T][]
  Overload 2: (o: {}) => [string, any][]
## JSDoc
Returns an array of key/values of the enumerable own properties of an object

## How to respond
- Behave EXACTLY as the original \`entries\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"o","type":"{ [s: string]: T; } | ArrayLike<T>"}],
};
export function entries<T>(input: { o: { [s: string]: T; } | ArrayLike<T>; prompt?: string }): Promise<[string, T][]>;
export function entries(input: { o: {}; prompt?: string }): Promise<[string, any][]>;
export async function entries(input: Record<string, unknown>): Promise<unknown> { return runMethod(__entries_4_spec, input); }

/** `Object.freeze` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __freeze_5_spec: GeneratedMethodSpec = {
  group: 'object',
  methodName: 'freeze',
  functionId: 'Object.freeze',
  kind: 'static',
  native: resolveNative('Object.freeze'),
  receiverKey: "",
  paramOrder: ["f","o"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Object.freeze\`.
## Original signature(s)
  Overload 1: (f: T) => T
  Overload 2: (o: T) => Readonly<T>
  Overload 3: (o: T) => Readonly<T>
## JSDoc
Prevents the modification of existing property attributes and values, and prevents the addition of new properties.

## How to respond
- Behave EXACTLY as the original \`freeze\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"f","type":"T"}],
};
export function freeze<T extends Function>(input: { f: T; prompt?: string }): Promise<T>;
export function freeze<T extends { [idx: string]: U | null | undefined | object; }, U extends string | bigint | number | boolean | symbol>(input: { o: T; prompt?: string }): Promise<Readonly<T>>;
export function freeze<T>(input: { o: T; prompt?: string }): Promise<Readonly<T>>;
export async function freeze(input: Record<string, unknown>): Promise<unknown> { return runMethod(__freeze_5_spec, input); }

/** `Object.fromEntries` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __fromEntries_6_spec: GeneratedMethodSpec = {
  group: 'object',
  methodName: 'fromEntries',
  functionId: 'Object.fromEntries',
  kind: 'static',
  native: resolveNative('Object.fromEntries'),
  receiverKey: "",
  paramOrder: ["entries"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Object.fromEntries\`.
## Original signature(s)
  Overload 1: (entries: Iterable<readonly [PropertyKey, T]>) => { [k: string]: T; }
  Overload 2: (entries: Iterable<readonly any[]>) => any
## JSDoc
Returns an object created by key-value entries for properties and methods

## How to respond
- Behave EXACTLY as the original \`fromEntries\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"entries","type":"Iterable<readonly [PropertyKey, T]>"}],
};
export function fromEntries<T = any>(input: { entries: Iterable<readonly [PropertyKey, T]>; prompt?: string }): Promise<{ [k: string]: T; }>;
export function fromEntries(input: { entries: Iterable<readonly any[]>; prompt?: string }): Promise<any>;
export async function fromEntries(input: Record<string, unknown>): Promise<unknown> { return runMethod(__fromEntries_6_spec, input); }

/** `Object.getOwnPropertyDescriptor` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getOwnPropertyDescriptor_7_spec: GeneratedMethodSpec = {
  group: 'object',
  methodName: 'getOwnPropertyDescriptor',
  functionId: 'Object.getOwnPropertyDescriptor',
  kind: 'static',
  native: resolveNative('Object.getOwnPropertyDescriptor'),
  receiverKey: "",
  paramOrder: ["o","p"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Object.getOwnPropertyDescriptor\`.
## Original signature(s)
  Overload 1: (o: any, p: PropertyKey) => PropertyDescriptor
## JSDoc
Gets the own property descriptor of the specified object.
An own property descriptor is one that is defined directly on the object and is not inherited from the object's prototype.

## How to respond
- Behave EXACTLY as the original \`getOwnPropertyDescriptor\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"o","type":"any"},{"name":"p","type":"PropertyKey"}],
};
export function getOwnPropertyDescriptor(input: { o: any; p: PropertyKey; prompt?: string }): Promise<PropertyDescriptor>;
export async function getOwnPropertyDescriptor(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getOwnPropertyDescriptor_7_spec, input); }

/** `Object.getOwnPropertyDescriptors` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getOwnPropertyDescriptors_8_spec: GeneratedMethodSpec = {
  group: 'object',
  methodName: 'getOwnPropertyDescriptors',
  functionId: 'Object.getOwnPropertyDescriptors',
  kind: 'static',
  native: resolveNative('Object.getOwnPropertyDescriptors'),
  receiverKey: "",
  paramOrder: ["o"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Object.getOwnPropertyDescriptors\`.
## Original signature(s)
  Overload 1: (o: T) => { [P in keyof T]: TypedPropertyDescriptor<T[P]>; } & { [x: string]: PropertyDescriptor; }
## JSDoc
Returns an object containing all own property descriptors of an object

## How to respond
- Behave EXACTLY as the original \`getOwnPropertyDescriptors\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"o","type":"T"}],
};
export function getOwnPropertyDescriptors<T>(input: { o: T; prompt?: string }): Promise<{ [P in keyof T]: TypedPropertyDescriptor<T[P]>; } & { [x: string]: PropertyDescriptor; }>;
export async function getOwnPropertyDescriptors(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getOwnPropertyDescriptors_8_spec, input); }

/** `Object.getOwnPropertyNames` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getOwnPropertyNames_9_spec: GeneratedMethodSpec = {
  group: 'object',
  methodName: 'getOwnPropertyNames',
  functionId: 'Object.getOwnPropertyNames',
  kind: 'static',
  native: resolveNative('Object.getOwnPropertyNames'),
  receiverKey: "",
  paramOrder: ["o"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Object.getOwnPropertyNames\`.
## Original signature(s)
  Overload 1: (o: any) => string[]
## JSDoc
Returns the names of the own properties of an object. The own properties of an object are those that are defined directly
on that object, and are not inherited from the object's prototype. The properties of an object include both fields (objects) and functions.

## How to respond
- Behave EXACTLY as the original \`getOwnPropertyNames\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"o","type":"any"}],
};
export function getOwnPropertyNames(input: { o: any; prompt?: string }): Promise<string[]>;
export async function getOwnPropertyNames(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getOwnPropertyNames_9_spec, input); }

/** `Object.getOwnPropertySymbols` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getOwnPropertySymbols_10_spec: GeneratedMethodSpec = {
  group: 'object',
  methodName: 'getOwnPropertySymbols',
  functionId: 'Object.getOwnPropertySymbols',
  kind: 'static',
  native: resolveNative('Object.getOwnPropertySymbols'),
  receiverKey: "",
  paramOrder: ["o"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Object.getOwnPropertySymbols\`.
## Original signature(s)
  Overload 1: (o: any) => symbol[]
## JSDoc
Returns an array of all symbol properties found directly on object o.

## How to respond
- Behave EXACTLY as the original \`getOwnPropertySymbols\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"o","type":"any"}],
};
export function getOwnPropertySymbols(input: { o: any; prompt?: string }): Promise<symbol[]>;
export async function getOwnPropertySymbols(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getOwnPropertySymbols_10_spec, input); }

/** `Object.getPrototypeOf` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getPrototypeOf_11_spec: GeneratedMethodSpec = {
  group: 'object',
  methodName: 'getPrototypeOf',
  functionId: 'Object.getPrototypeOf',
  kind: 'static',
  native: resolveNative('Object.getPrototypeOf'),
  receiverKey: "",
  paramOrder: ["o"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Object.getPrototypeOf\`.
## Original signature(s)
  Overload 1: (o: any) => any
## JSDoc
Returns the prototype of an object.

## How to respond
- Behave EXACTLY as the original \`getPrototypeOf\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"o","type":"any"}],
};
export function getPrototypeOf(input: { o: any; prompt?: string }): Promise<any>;
export async function getPrototypeOf(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getPrototypeOf_11_spec, input); }

/** `Object.groupBy` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __groupBy_12_spec: GeneratedMethodSpec = {
  group: 'object',
  methodName: 'groupBy',
  functionId: 'Object.groupBy',
  kind: 'static',
  native: resolveNative('Object.groupBy'),
  receiverKey: "",
  paramOrder: ["items","keySelector"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Object.groupBy\`.
## Original signature(s)
  Overload 1: (items: Iterable<T>, keySelector?: (item: T, index: number) => K) => Partial<Record<K, T[]>>
## JSDoc
Groups members of an iterable according to the return value of the passed callback.

## How to respond
- Behave EXACTLY as the original \`groupBy\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"items","type":"Iterable<T>"},{"name":"keySelector?","type":"(item: T, index: number) => K"}],
};
export function groupBy<K extends PropertyKey, T>(input: { items: Iterable<T>; keySelector?: (item: T, index: number) => K; prompt?: string }): Promise<Partial<Record<K, T[]>>>;
export async function groupBy(input: Record<string, unknown>): Promise<unknown> { return runMethod(__groupBy_12_spec, input); }

/** `Object.hasOwn` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __hasOwn_13_spec: GeneratedMethodSpec = {
  group: 'object',
  methodName: 'hasOwn',
  functionId: 'Object.hasOwn',
  kind: 'static',
  native: resolveNative('Object.hasOwn'),
  receiverKey: "",
  paramOrder: ["o","v"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Object.hasOwn\`.
## Original signature(s)
  Overload 1: (o: object, v: PropertyKey) => boolean
## JSDoc
Determines whether an object has a property with the specified name.

## How to respond
- Behave EXACTLY as the original \`hasOwn\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"o","type":"object"},{"name":"v","type":"PropertyKey"}],
};
export function hasOwn(input: { o: object; v: PropertyKey; prompt?: string }): Promise<boolean>;
export async function hasOwn(input: Record<string, unknown>): Promise<unknown> { return runMethod(__hasOwn_13_spec, input); }

/** `Object.is` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __is_14_spec: GeneratedMethodSpec = {
  group: 'object',
  methodName: 'is',
  functionId: 'Object.is',
  kind: 'static',
  native: resolveNative('Object.is'),
  receiverKey: "",
  paramOrder: ["value1","value2"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Object.is\`.
## Original signature(s)
  Overload 1: (value1: any, value2: any) => boolean
## JSDoc
Returns true if the values are the same value, false otherwise.

## How to respond
- Behave EXACTLY as the original \`is\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"value1","type":"any"},{"name":"value2","type":"any"}],
};
export function is(input: { value1: any; value2: any; prompt?: string }): Promise<boolean>;
export async function is(input: Record<string, unknown>): Promise<unknown> { return runMethod(__is_14_spec, input); }

/** `Object.isExtensible` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __isExtensible_15_spec: GeneratedMethodSpec = {
  group: 'object',
  methodName: 'isExtensible',
  functionId: 'Object.isExtensible',
  kind: 'static',
  native: resolveNative('Object.isExtensible'),
  receiverKey: "",
  paramOrder: ["o"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Object.isExtensible\`.
## Original signature(s)
  Overload 1: (o: any) => boolean
## JSDoc
Returns a value that indicates whether new properties can be added to an object.

## How to respond
- Behave EXACTLY as the original \`isExtensible\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"o","type":"any"}],
};
export function isExtensible(input: { o: any; prompt?: string }): Promise<boolean>;
export async function isExtensible(input: Record<string, unknown>): Promise<unknown> { return runMethod(__isExtensible_15_spec, input); }

/** `Object.isFrozen` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __isFrozen_16_spec: GeneratedMethodSpec = {
  group: 'object',
  methodName: 'isFrozen',
  functionId: 'Object.isFrozen',
  kind: 'static',
  native: resolveNative('Object.isFrozen'),
  receiverKey: "",
  paramOrder: ["o"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Object.isFrozen\`.
## Original signature(s)
  Overload 1: (o: any) => boolean
## JSDoc
Returns true if existing property attributes and values cannot be modified in an object, and new properties cannot be added to the object.

## How to respond
- Behave EXACTLY as the original \`isFrozen\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"o","type":"any"}],
};
export function isFrozen(input: { o: any; prompt?: string }): Promise<boolean>;
export async function isFrozen(input: Record<string, unknown>): Promise<unknown> { return runMethod(__isFrozen_16_spec, input); }

/** `Object.isSealed` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __isSealed_17_spec: GeneratedMethodSpec = {
  group: 'object',
  methodName: 'isSealed',
  functionId: 'Object.isSealed',
  kind: 'static',
  native: resolveNative('Object.isSealed'),
  receiverKey: "",
  paramOrder: ["o"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Object.isSealed\`.
## Original signature(s)
  Overload 1: (o: any) => boolean
## JSDoc
Returns true if existing property attributes cannot be modified in an object and new properties cannot be added to the object.

## How to respond
- Behave EXACTLY as the original \`isSealed\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"o","type":"any"}],
};
export function isSealed(input: { o: any; prompt?: string }): Promise<boolean>;
export async function isSealed(input: Record<string, unknown>): Promise<unknown> { return runMethod(__isSealed_17_spec, input); }

/** `Object.keys` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __keys_18_spec: GeneratedMethodSpec = {
  group: 'object',
  methodName: 'keys',
  functionId: 'Object.keys',
  kind: 'static',
  native: resolveNative('Object.keys'),
  receiverKey: "",
  paramOrder: ["o"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Object.keys\`.
## Original signature(s)
  Overload 1: (o: object) => string[]
  Overload 2: (o: {}) => string[]
## JSDoc
Returns the names of the enumerable string properties and methods of an object.

## How to respond
- Behave EXACTLY as the original \`keys\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"o","type":"object"}],
};
export function keys(input: { o: object; prompt?: string }): Promise<string[]>;
export function keys(input: { o: {}; prompt?: string }): Promise<string[]>;
export async function keys(input: Record<string, unknown>): Promise<unknown> { return runMethod(__keys_18_spec, input); }

/** `Object.preventExtensions` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __preventExtensions_19_spec: GeneratedMethodSpec = {
  group: 'object',
  methodName: 'preventExtensions',
  functionId: 'Object.preventExtensions',
  kind: 'static',
  native: resolveNative('Object.preventExtensions'),
  receiverKey: "",
  paramOrder: ["o"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Object.preventExtensions\`.
## Original signature(s)
  Overload 1: (o: T) => T
## JSDoc
Prevents the addition of new properties to an object.

## How to respond
- Behave EXACTLY as the original \`preventExtensions\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"o","type":"T"}],
};
export function preventExtensions<T>(input: { o: T; prompt?: string }): Promise<T>;
export async function preventExtensions(input: Record<string, unknown>): Promise<unknown> { return runMethod(__preventExtensions_19_spec, input); }

/** `Object.seal` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __seal_20_spec: GeneratedMethodSpec = {
  group: 'object',
  methodName: 'seal',
  functionId: 'Object.seal',
  kind: 'static',
  native: resolveNative('Object.seal'),
  receiverKey: "",
  paramOrder: ["o"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Object.seal\`.
## Original signature(s)
  Overload 1: (o: T) => T
## JSDoc
Prevents the modification of attributes of existing properties, and prevents the addition of new properties.

## How to respond
- Behave EXACTLY as the original \`seal\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"o","type":"T"}],
};
export function seal<T>(input: { o: T; prompt?: string }): Promise<T>;
export async function seal(input: Record<string, unknown>): Promise<unknown> { return runMethod(__seal_20_spec, input); }

/** `Object.setPrototypeOf` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setPrototypeOf_21_spec: GeneratedMethodSpec = {
  group: 'object',
  methodName: 'setPrototypeOf',
  functionId: 'Object.setPrototypeOf',
  kind: 'static',
  native: resolveNative('Object.setPrototypeOf'),
  receiverKey: "",
  paramOrder: ["o","proto"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Object.setPrototypeOf\`.
## Original signature(s)
  Overload 1: (o: any, proto: object) => any
## JSDoc
Sets the prototype of a specified object o to object proto or null. Returns the object o.

## How to respond
- Behave EXACTLY as the original \`setPrototypeOf\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"o","type":"any"},{"name":"proto","type":"object"}],
};
export function setPrototypeOf(input: { o: any; proto: object; prompt?: string }): Promise<any>;
export async function setPrototypeOf(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setPrototypeOf_21_spec, input); }

/** `Object.values` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __values_22_spec: GeneratedMethodSpec = {
  group: 'object',
  methodName: 'values',
  functionId: 'Object.values',
  kind: 'static',
  native: resolveNative('Object.values'),
  receiverKey: "",
  paramOrder: ["o"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Object.values\`.
## Original signature(s)
  Overload 1: (o: { [s: string]: T; } | ArrayLike<T>) => T[]
  Overload 2: (o: {}) => any[]
## JSDoc
Returns an array of values of the enumerable own properties of an object

## How to respond
- Behave EXACTLY as the original \`values\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"o","type":"{ [s: string]: T; } | ArrayLike<T>"}],
};
export function values<T>(input: { o: { [s: string]: T; } | ArrayLike<T>; prompt?: string }): Promise<T[]>;
export function values(input: { o: {}; prompt?: string }): Promise<any[]>;
export async function values(input: Record<string, unknown>): Promise<unknown> { return runMethod(__values_22_spec, input); }


