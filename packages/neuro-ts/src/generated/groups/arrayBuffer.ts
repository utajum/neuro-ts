// Auto-generated by scripts/generate-wrappers.ts. Do not edit.
import { runMethod, resolveNative, type GeneratedMethodSpec } from '../../runtime';

/** `ArrayBuffer.isView` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __isView_0_spec: GeneratedMethodSpec = {
  group: 'arrayBuffer',
  methodName: 'isView',
  functionId: 'ArrayBuffer.isView',
  kind: 'static',
  native: resolveNative('ArrayBuffer.isView'),
  receiverKey: "",
  paramOrder: ["arg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`ArrayBuffer.isView\`.
## Original signature(s)
  Overload 1: (arg: any) => boolean
## How to respond
- Behave EXACTLY as the original \`isView\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"arg","type":"any"}],
};
export function isView(input: { arg: any; prompt?: string }): Promise<boolean>;
export async function isView(input: Record<string, unknown>): Promise<unknown> { return runMethod(__isView_0_spec, input); }

/** `ArrayBuffer.prototype.resize` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __resize_1_spec: GeneratedMethodSpec = {
  group: 'arrayBuffer',
  methodName: 'resize',
  functionId: 'ArrayBuffer.prototype.resize',
  kind: 'instance',
  native: resolveNative('ArrayBuffer.prototype.resize'),
  receiverKey: "arrayBuffer",
  paramOrder: ["newByteLength"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`ArrayBuffer.prototype.resize\`.
## Original signature(s)
  Overload 1: (newByteLength?: number) => void
## JSDoc
Resizes the ArrayBuffer to the specified size (in bytes).

[MDN](https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/ArrayBuffer/resize)

## How to respond
- Behave EXACTLY as the original \`resize\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"newByteLength?","type":"number"}],
};
export function resize(input: { arrayBuffer: ArrayBuffer; newByteLength?: number; prompt?: string }): Promise<void>;
export async function resize(input: Record<string, unknown>): Promise<unknown> { return runMethod(__resize_1_spec, input); }

/** `ArrayBuffer.prototype.slice` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __slice_2_spec: GeneratedMethodSpec = {
  group: 'arrayBuffer',
  methodName: 'slice',
  functionId: 'ArrayBuffer.prototype.slice',
  kind: 'instance',
  native: resolveNative('ArrayBuffer.prototype.slice'),
  receiverKey: "arrayBuffer",
  paramOrder: ["begin","end"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`ArrayBuffer.prototype.slice\`.
## Original signature(s)
  Overload 1: (begin?: number, end?: number) => ArrayBuffer
## JSDoc
Returns a section of an ArrayBuffer.

## How to respond
- Behave EXACTLY as the original \`slice\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"begin?","type":"number"},{"name":"end?","type":"number"}],
};
export function slice(input: { arrayBuffer: ArrayBuffer; begin?: number; end?: number; prompt?: string }): Promise<ArrayBuffer>;
export async function slice(input: Record<string, unknown>): Promise<unknown> { return runMethod(__slice_2_spec, input); }

/** `ArrayBuffer.prototype.transfer` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __transfer_3_spec: GeneratedMethodSpec = {
  group: 'arrayBuffer',
  methodName: 'transfer',
  functionId: 'ArrayBuffer.prototype.transfer',
  kind: 'instance',
  native: resolveNative('ArrayBuffer.prototype.transfer'),
  receiverKey: "arrayBuffer",
  paramOrder: ["newByteLength"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`ArrayBuffer.prototype.transfer\`.
## Original signature(s)
  Overload 1: (newByteLength?: number) => ArrayBuffer
## JSDoc
Creates a new ArrayBuffer with the same byte content as this buffer, then detaches this buffer.

[MDN](https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/ArrayBuffer/transfer)

## How to respond
- Behave EXACTLY as the original \`transfer\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"newByteLength?","type":"number"}],
};
export function transfer(input: { arrayBuffer: ArrayBuffer; newByteLength?: number; prompt?: string }): Promise<ArrayBuffer>;
export async function transfer(input: Record<string, unknown>): Promise<unknown> { return runMethod(__transfer_3_spec, input); }

/** `ArrayBuffer.prototype.transferToFixedLength` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __transferToFixedLength_4_spec: GeneratedMethodSpec = {
  group: 'arrayBuffer',
  methodName: 'transferToFixedLength',
  functionId: 'ArrayBuffer.prototype.transferToFixedLength',
  kind: 'instance',
  native: resolveNative('ArrayBuffer.prototype.transferToFixedLength'),
  receiverKey: "arrayBuffer",
  paramOrder: ["newByteLength"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`ArrayBuffer.prototype.transferToFixedLength\`.
## Original signature(s)
  Overload 1: (newByteLength?: number) => ArrayBuffer
## JSDoc
Creates a new non-resizable ArrayBuffer with the same byte content as this buffer, then detaches this buffer.

[MDN](https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/ArrayBuffer/transferToFixedLength)

## How to respond
- Behave EXACTLY as the original \`transferToFixedLength\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"newByteLength?","type":"number"}],
};
export function transferToFixedLength(input: { arrayBuffer: ArrayBuffer; newByteLength?: number; prompt?: string }): Promise<ArrayBuffer>;
export async function transferToFixedLength(input: Record<string, unknown>): Promise<unknown> { return runMethod(__transferToFixedLength_4_spec, input); }


