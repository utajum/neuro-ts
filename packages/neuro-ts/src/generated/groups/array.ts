// Auto-generated by scripts/generate-wrappers.ts. Do not edit.
import { runMethod, resolveNative, type GeneratedMethodSpec } from '../../runtime';

/** `Array.prototype.at` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __at_0_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'at',
  functionId: 'Array.prototype.at',
  kind: 'instance',
  native: resolveNative('Array.prototype.at'),
  receiverKey: "array",
  paramOrder: ["index"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.at\`.
## Original signature(s)
  Overload 1: (index: number) => T
## JSDoc
Returns the item located at the specified index.

## How to respond
- Behave EXACTLY as the original \`at\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"index","type":"number"}],
};
export function at<T>(input: { array: T[]; index: number; prompt?: string }): Promise<T>;
export async function at(input: Record<string, unknown>): Promise<unknown> { return runMethod(__at_0_spec, input); }

/** `Array.prototype.concat` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __concat_1_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'concat',
  functionId: 'Array.prototype.concat',
  kind: 'instance',
  native: resolveNative('Array.prototype.concat'),
  receiverKey: "array",
  paramOrder: ["items"],
  variadicKey: "items",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.concat\`.
## Original signature(s)
  Overload 1: (...items: ConcatArray<T>[]) => T[]
  Overload 2: (...items: (T | ConcatArray<T>)[]) => T[]
## JSDoc
Combines two or more arrays.
This method returns a new array without modifying any existing arrays.

## How to respond
- Behave EXACTLY as the original \`concat\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"items","type":"ConcatArray<T>[]"}],
};
export function concat<T>(input: { array: T[]; items?: (ConcatArray<T>)[]; prompt?: string }): Promise<T[]>;
export function concat<T>(input: { array: T[]; items?: ((T | ConcatArray<T>))[]; prompt?: string }): Promise<T[]>;
export async function concat(input: Record<string, unknown>): Promise<unknown> { return runMethod(__concat_1_spec, input); }

/** `Array.prototype.copyWithin` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __copyWithin_2_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'copyWithin',
  functionId: 'Array.prototype.copyWithin',
  kind: 'instance',
  native: resolveNative('Array.prototype.copyWithin'),
  receiverKey: "array",
  paramOrder: ["target","start","end"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.copyWithin\`.
## Original signature(s)
  Overload 1: (target: number, start: number, end?: number) => T[]
## JSDoc
Returns the this object after copying a section of the array identified by start and end
to the same array starting at position target

## How to respond
- Behave EXACTLY as the original \`copyWithin\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"target","type":"number"},{"name":"start","type":"number"},{"name":"end?","type":"number"}],
};
export function copyWithin<T>(input: { array: T[]; target: number; start: number; end?: number; prompt?: string }): Promise<T[]>;
export async function copyWithin(input: Record<string, unknown>): Promise<unknown> { return runMethod(__copyWithin_2_spec, input); }

/** `Array.prototype.entries` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __entries_3_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'entries',
  functionId: 'Array.prototype.entries',
  kind: 'instance',
  native: resolveNative('Array.prototype.entries'),
  receiverKey: "array",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.entries\`.
## Original signature(s)
  Overload 1: () => ArrayIterator<[number, T]>
## JSDoc
Returns an iterable of key, value pairs for every entry in the array

## How to respond
- Behave EXACTLY as the original \`entries\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function entries<T>(input: { array: T[]; prompt?: string }): Promise<ArrayIterator<[number, T]>>;
export async function entries(input: Record<string, unknown>): Promise<unknown> { return runMethod(__entries_3_spec, input); }

/** `Array.prototype.every` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __every_4_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'every',
  functionId: 'Array.prototype.every',
  kind: 'instance',
  native: resolveNative('Array.prototype.every'),
  receiverKey: "array",
  paramOrder: ["predicate","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.every\`.
## Original signature(s)
  Overload 1: (predicate?: (value: T, index: number, array: T[]) => boolean, thisArg?: any) => boolean
  Overload 2: (predicate?: (value: T, index: number, array: T[]) => unknown, thisArg?: any) => boolean
## JSDoc
Determines whether all the members of an array satisfy the specified test.

## How to respond
- Behave EXACTLY as the original \`every\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: T, index: number, array: T[]) => boolean"},{"name":"thisArg?","type":"any"}],
};
export function every<T, S extends T>(input: { array: T[]; predicate?: (value: T, index: number, array: T[]) => boolean; thisArg?: any; prompt?: string }): Promise<boolean>;
export function every<T>(input: { array: T[]; predicate?: (value: T, index: number, array: T[]) => unknown; thisArg?: any; prompt?: string }): Promise<boolean>;
export async function every(input: Record<string, unknown>): Promise<unknown> { return runMethod(__every_4_spec, input); }

/** `Array.prototype.fill` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __fill_5_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'fill',
  functionId: 'Array.prototype.fill',
  kind: 'instance',
  native: resolveNative('Array.prototype.fill'),
  receiverKey: "array",
  paramOrder: ["value","start","end"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.fill\`.
## Original signature(s)
  Overload 1: (value: T, start?: number, end?: number) => T[]
## JSDoc
Changes all array elements from \`start\` to \`end\` index to a static \`value\` and returns the modified array

## How to respond
- Behave EXACTLY as the original \`fill\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"value","type":"T"},{"name":"start?","type":"number"},{"name":"end?","type":"number"}],
};
export function fill<T>(input: { array: T[]; value: T; start?: number; end?: number; prompt?: string }): Promise<T[]>;
export async function fill(input: Record<string, unknown>): Promise<unknown> { return runMethod(__fill_5_spec, input); }

/** `Array.prototype.filter` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __filter_6_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'filter',
  functionId: 'Array.prototype.filter',
  kind: 'instance',
  native: resolveNative('Array.prototype.filter'),
  receiverKey: "array",
  paramOrder: ["predicate","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.filter\`.
## Original signature(s)
  Overload 1: (predicate?: (value: T, index: number, array: T[]) => boolean, thisArg?: any) => S[]
  Overload 2: (predicate?: (value: T, index: number, array: T[]) => unknown, thisArg?: any) => T[]
## JSDoc
Returns the elements of an array that meet the condition specified in a callback function.

## How to respond
- Behave EXACTLY as the original \`filter\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: T, index: number, array: T[]) => boolean"},{"name":"thisArg?","type":"any"}],
};
export function filter<T, S extends T>(input: { array: T[]; predicate?: (value: T, index: number, array: T[]) => boolean; thisArg?: any; prompt?: string }): Promise<S[]>;
export function filter<T>(input: { array: T[]; predicate?: (value: T, index: number, array: T[]) => unknown; thisArg?: any; prompt?: string }): Promise<T[]>;
export async function filter(input: Record<string, unknown>): Promise<unknown> { return runMethod(__filter_6_spec, input); }

/** `Array.prototype.find` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __find_7_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'find',
  functionId: 'Array.prototype.find',
  kind: 'instance',
  native: resolveNative('Array.prototype.find'),
  receiverKey: "array",
  paramOrder: ["predicate","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.find\`.
## Original signature(s)
  Overload 1: (predicate?: (value: T, index: number, obj: T[]) => boolean, thisArg?: any) => S
  Overload 2: (predicate?: (value: T, index: number, obj: T[]) => unknown, thisArg?: any) => T
## JSDoc
Returns the value of the first element in the array where predicate is true, and undefined
otherwise.

## How to respond
- Behave EXACTLY as the original \`find\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: T, index: number, obj: T[]) => boolean"},{"name":"thisArg?","type":"any"}],
};
export function find<T, S extends T>(input: { array: T[]; predicate?: (value: T, index: number, obj: T[]) => boolean; thisArg?: any; prompt?: string }): Promise<S>;
export function find<T>(input: { array: T[]; predicate?: (value: T, index: number, obj: T[]) => unknown; thisArg?: any; prompt?: string }): Promise<T>;
export async function find(input: Record<string, unknown>): Promise<unknown> { return runMethod(__find_7_spec, input); }

/** `Array.prototype.findIndex` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __findIndex_8_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'findIndex',
  functionId: 'Array.prototype.findIndex',
  kind: 'instance',
  native: resolveNative('Array.prototype.findIndex'),
  receiverKey: "array",
  paramOrder: ["predicate","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.findIndex\`.
## Original signature(s)
  Overload 1: (predicate?: (value: T, index: number, obj: T[]) => unknown, thisArg?: any) => number
## JSDoc
Returns the index of the first element in the array where predicate is true, and -1
otherwise.

## How to respond
- Behave EXACTLY as the original \`findIndex\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: T, index: number, obj: T[]) => unknown"},{"name":"thisArg?","type":"any"}],
};
export function findIndex<T>(input: { array: T[]; predicate?: (value: T, index: number, obj: T[]) => unknown; thisArg?: any; prompt?: string }): Promise<number>;
export async function findIndex(input: Record<string, unknown>): Promise<unknown> { return runMethod(__findIndex_8_spec, input); }

/** `Array.prototype.findLast` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __findLast_9_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'findLast',
  functionId: 'Array.prototype.findLast',
  kind: 'instance',
  native: resolveNative('Array.prototype.findLast'),
  receiverKey: "array",
  paramOrder: ["predicate","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.findLast\`.
## Original signature(s)
  Overload 1: (predicate?: (value: T, index: number, array: T[]) => boolean, thisArg?: any) => S
  Overload 2: (predicate?: (value: T, index: number, array: T[]) => unknown, thisArg?: any) => T
## JSDoc
Returns the value of the last element in the array where predicate is true, and undefined
otherwise.

## How to respond
- Behave EXACTLY as the original \`findLast\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: T, index: number, array: T[]) => boolean"},{"name":"thisArg?","type":"any"}],
};
export function findLast<T, S extends T>(input: { array: T[]; predicate?: (value: T, index: number, array: T[]) => boolean; thisArg?: any; prompt?: string }): Promise<S>;
export function findLast<T>(input: { array: T[]; predicate?: (value: T, index: number, array: T[]) => unknown; thisArg?: any; prompt?: string }): Promise<T>;
export async function findLast(input: Record<string, unknown>): Promise<unknown> { return runMethod(__findLast_9_spec, input); }

/** `Array.prototype.findLastIndex` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __findLastIndex_10_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'findLastIndex',
  functionId: 'Array.prototype.findLastIndex',
  kind: 'instance',
  native: resolveNative('Array.prototype.findLastIndex'),
  receiverKey: "array",
  paramOrder: ["predicate","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.findLastIndex\`.
## Original signature(s)
  Overload 1: (predicate?: (value: T, index: number, array: T[]) => unknown, thisArg?: any) => number
## JSDoc
Returns the index of the last element in the array where predicate is true, and -1
otherwise.

## How to respond
- Behave EXACTLY as the original \`findLastIndex\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: T, index: number, array: T[]) => unknown"},{"name":"thisArg?","type":"any"}],
};
export function findLastIndex<T>(input: { array: T[]; predicate?: (value: T, index: number, array: T[]) => unknown; thisArg?: any; prompt?: string }): Promise<number>;
export async function findLastIndex(input: Record<string, unknown>): Promise<unknown> { return runMethod(__findLastIndex_10_spec, input); }

/** `Array.prototype.flat` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __flat_11_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'flat',
  functionId: 'Array.prototype.flat',
  kind: 'instance',
  native: resolveNative('Array.prototype.flat'),
  receiverKey: "array",
  paramOrder: ["depth"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.flat\`.
## Original signature(s)
  Overload 1: (depth?: D) => FlatArray<A, D>[]
## JSDoc
Returns a new array with all sub-array elements concatenated into it recursively up to the
specified depth.

## How to respond
- Behave EXACTLY as the original \`flat\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"depth?","type":"D"}],
};
export function flat<T, A, D extends number = 1>(input: { array: T[]; depth?: D; prompt?: string }): Promise<FlatArray<A, D>[]>;
export async function flat(input: Record<string, unknown>): Promise<unknown> { return runMethod(__flat_11_spec, input); }

/** `Array.prototype.flatMap` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __flatMap_12_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'flatMap',
  functionId: 'Array.prototype.flatMap',
  kind: 'instance',
  native: resolveNative('Array.prototype.flatMap'),
  receiverKey: "array",
  paramOrder: ["callback","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.flatMap\`.
## Original signature(s)
  Overload 1: (callback?: (this: This, value: T, index: number, array: T[]) => U | readonly U[], thisArg?: This) => U[]
## JSDoc
Calls a defined callback function on each element of an array. Then, flattens the result into
a new array.
This is identical to a map followed by flat with depth 1.

## How to respond
- Behave EXACTLY as the original \`flatMap\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"callback?","type":"(this: This, value: T, index: number, array: T[]) => U | readonly U[]"},{"name":"thisArg?","type":"This"}],
};
export function flatMap<T, U, This = undefined>(input: { array: T[]; callback?: (this: This, value: T, index: number, array: T[]) => U | readonly U[]; thisArg?: This; prompt?: string }): Promise<U[]>;
export async function flatMap(input: Record<string, unknown>): Promise<unknown> { return runMethod(__flatMap_12_spec, input); }

/** `Array.prototype.forEach` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __forEach_13_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'forEach',
  functionId: 'Array.prototype.forEach',
  kind: 'instance',
  native: resolveNative('Array.prototype.forEach'),
  receiverKey: "array",
  paramOrder: ["callbackfn","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.forEach\`.
## Original signature(s)
  Overload 1: (callbackfn?: (value: T, index: number, array: T[]) => void, thisArg?: any) => void
## JSDoc
Performs the specified action for each element in an array.

## How to respond
- Behave EXACTLY as the original \`forEach\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"callbackfn?","type":"(value: T, index: number, array: T[]) => void"},{"name":"thisArg?","type":"any"}],
};
export function forEach<T>(input: { array: T[]; callbackfn?: (value: T, index: number, array: T[]) => void; thisArg?: any; prompt?: string }): Promise<void>;
export async function forEach(input: Record<string, unknown>): Promise<unknown> { return runMethod(__forEach_13_spec, input); }

/** `Array.from` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __from_14_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'from',
  functionId: 'Array.from',
  kind: 'static',
  native: resolveNative('Array.from'),
  receiverKey: "",
  paramOrder: ["arrayLike","mapfn","thisArg","iterable"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.from\`.
## Original signature(s)
  Overload 1: (arrayLike: ArrayLike<T>) => T[]
  Overload 2: (arrayLike: ArrayLike<T>, mapfn?: (v: T, k: number) => U, thisArg?: any) => U[]
  Overload 3: (iterable: Iterable<T> | ArrayLike<T>) => T[]
  Overload 4: (iterable: Iterable<T> | ArrayLike<T>, mapfn?: (v: T, k: number) => U, thisArg?: any) => U[]
## JSDoc
Creates an array from an array-like object.

## How to respond
- Behave EXACTLY as the original \`from\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"arrayLike","type":"ArrayLike<T>"}],
};
export function from<T>(input: { arrayLike: ArrayLike<T>; prompt?: string }): Promise<T[]>;
export function from<T, U>(input: { arrayLike: ArrayLike<T>; mapfn?: (v: T, k: number) => U; thisArg?: any; prompt?: string }): Promise<U[]>;
export function from<T>(input: { iterable: Iterable<T> | ArrayLike<T>; prompt?: string }): Promise<T[]>;
export function from<T, U>(input: { iterable: Iterable<T> | ArrayLike<T>; mapfn?: (v: T, k: number) => U; thisArg?: any; prompt?: string }): Promise<U[]>;
export async function from(input: Record<string, unknown>): Promise<unknown> { return runMethod(__from_14_spec, input); }

/** `Array.fromAsync` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __fromAsync_15_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'fromAsync',
  functionId: 'Array.fromAsync',
  kind: 'static',
  native: resolveNative('Array.fromAsync'),
  receiverKey: "",
  paramOrder: ["iterableOrArrayLike","mapFn","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.fromAsync\`.
## Original signature(s)
  Overload 1: (iterableOrArrayLike: AsyncIterable<T> | Iterable<T | PromiseLike<T>> | ArrayLike<T | PromiseLike<T>>) => Promise<T[]>
  Overload 2: (iterableOrArrayLike: AsyncIterable<T> | Iterable<T> | ArrayLike<T>, mapFn?: (value: Awaited<T>, index: number) => U, thisArg?: any) => Promise<Awaited<U>[]>
## JSDoc
Creates an array from an async iterator or iterable object.

## How to respond
- Behave EXACTLY as the original \`fromAsync\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"iterableOrArrayLike","type":"AsyncIterable<T> | Iterable<T | PromiseLike<T>> | ArrayLike<T | PromiseLike<T>>"}],
};
export function fromAsync<T>(input: { iterableOrArrayLike: AsyncIterable<T> | Iterable<T | PromiseLike<T>> | ArrayLike<T | PromiseLike<T>>; prompt?: string }): Promise<T[]>;
export function fromAsync<T, U>(input: { iterableOrArrayLike: AsyncIterable<T> | Iterable<T> | ArrayLike<T>; mapFn?: (value: Awaited<T>, index: number) => U; thisArg?: any; prompt?: string }): Promise<Awaited<U>[]>;
export async function fromAsync(input: Record<string, unknown>): Promise<unknown> { return runMethod(__fromAsync_15_spec, input); }

/** `Array.prototype.includes` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __includes_16_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'includes',
  functionId: 'Array.prototype.includes',
  kind: 'instance',
  native: resolveNative('Array.prototype.includes'),
  receiverKey: "array",
  paramOrder: ["searchElement","fromIndex"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.includes\`.
## Original signature(s)
  Overload 1: (searchElement: T, fromIndex?: number) => boolean
## JSDoc
Determines whether an array includes a certain element, returning true or false as appropriate.

## How to respond
- Behave EXACTLY as the original \`includes\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"searchElement","type":"T"},{"name":"fromIndex?","type":"number"}],
};
export function includes<T>(input: { array: T[]; searchElement: T; fromIndex?: number; prompt?: string }): Promise<boolean>;
export async function includes(input: Record<string, unknown>): Promise<unknown> { return runMethod(__includes_16_spec, input); }

/** `Array.prototype.indexOf` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __indexOf_17_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'indexOf',
  functionId: 'Array.prototype.indexOf',
  kind: 'instance',
  native: resolveNative('Array.prototype.indexOf'),
  receiverKey: "array",
  paramOrder: ["searchElement","fromIndex"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.indexOf\`.
## Original signature(s)
  Overload 1: (searchElement: T, fromIndex?: number) => number
## JSDoc
Returns the index of the first occurrence of a value in an array, or -1 if it is not present.

## How to respond
- Behave EXACTLY as the original \`indexOf\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"searchElement","type":"T"},{"name":"fromIndex?","type":"number"}],
};
export function indexOf<T>(input: { array: T[]; searchElement: T; fromIndex?: number; prompt?: string }): Promise<number>;
export async function indexOf(input: Record<string, unknown>): Promise<unknown> { return runMethod(__indexOf_17_spec, input); }

/** `Array.isArray` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __isArray_18_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'isArray',
  functionId: 'Array.isArray',
  kind: 'static',
  native: resolveNative('Array.isArray'),
  receiverKey: "",
  paramOrder: ["arg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.isArray\`.
## Original signature(s)
  Overload 1: (arg: any) => boolean
## How to respond
- Behave EXACTLY as the original \`isArray\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"arg","type":"any"}],
};
export function isArray(input: { arg: any; prompt?: string }): Promise<boolean>;
export async function isArray(input: Record<string, unknown>): Promise<unknown> { return runMethod(__isArray_18_spec, input); }

/** `Array.prototype.join` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __join_19_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'join',
  functionId: 'Array.prototype.join',
  kind: 'instance',
  native: resolveNative('Array.prototype.join'),
  receiverKey: "array",
  paramOrder: ["separator"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.join\`.
## Original signature(s)
  Overload 1: (separator?: string) => string
## JSDoc
Adds all the elements of an array into a string, separated by the specified separator string.

## How to respond
- Behave EXACTLY as the original \`join\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"separator?","type":"string"}],
};
export function join<T>(input: { array: T[]; separator?: string; prompt?: string }): Promise<string>;
export async function join(input: Record<string, unknown>): Promise<unknown> { return runMethod(__join_19_spec, input); }

/** `Array.prototype.keys` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __keys_20_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'keys',
  functionId: 'Array.prototype.keys',
  kind: 'instance',
  native: resolveNative('Array.prototype.keys'),
  receiverKey: "array",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.keys\`.
## Original signature(s)
  Overload 1: () => ArrayIterator<number>
## JSDoc
Returns an iterable of keys in the array

## How to respond
- Behave EXACTLY as the original \`keys\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function keys<T>(input: { array: T[]; prompt?: string }): Promise<ArrayIterator<number>>;
export async function keys(input: Record<string, unknown>): Promise<unknown> { return runMethod(__keys_20_spec, input); }

/** `Array.prototype.lastIndexOf` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __lastIndexOf_21_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'lastIndexOf',
  functionId: 'Array.prototype.lastIndexOf',
  kind: 'instance',
  native: resolveNative('Array.prototype.lastIndexOf'),
  receiverKey: "array",
  paramOrder: ["searchElement","fromIndex"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.lastIndexOf\`.
## Original signature(s)
  Overload 1: (searchElement: T, fromIndex?: number) => number
## JSDoc
Returns the index of the last occurrence of a specified value in an array, or -1 if it is not present.

## How to respond
- Behave EXACTLY as the original \`lastIndexOf\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"searchElement","type":"T"},{"name":"fromIndex?","type":"number"}],
};
export function lastIndexOf<T>(input: { array: T[]; searchElement: T; fromIndex?: number; prompt?: string }): Promise<number>;
export async function lastIndexOf(input: Record<string, unknown>): Promise<unknown> { return runMethod(__lastIndexOf_21_spec, input); }

/** `Array.prototype.map` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __map_22_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'map',
  functionId: 'Array.prototype.map',
  kind: 'instance',
  native: resolveNative('Array.prototype.map'),
  receiverKey: "array",
  paramOrder: ["callbackfn","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.map\`.
## Original signature(s)
  Overload 1: (callbackfn?: (value: T, index: number, array: T[]) => U, thisArg?: any) => U[]
## JSDoc
Calls a defined callback function on each element of an array, and returns an array that contains the results.

## How to respond
- Behave EXACTLY as the original \`map\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"callbackfn?","type":"(value: T, index: number, array: T[]) => U"},{"name":"thisArg?","type":"any"}],
};
export function map<T, U>(input: { array: T[]; callbackfn?: (value: T, index: number, array: T[]) => U; thisArg?: any; prompt?: string }): Promise<U[]>;
export async function map(input: Record<string, unknown>): Promise<unknown> { return runMethod(__map_22_spec, input); }

/** `Array.of` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __of_23_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'of',
  functionId: 'Array.of',
  kind: 'static',
  native: resolveNative('Array.of'),
  receiverKey: "",
  paramOrder: ["items"],
  variadicKey: "items",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.of\`.
## Original signature(s)
  Overload 1: (...items: T[]) => T[]
## JSDoc
Returns a new array from a set of elements.

## How to respond
- Behave EXACTLY as the original \`of\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"items","type":"T[]"}],
};
export function of<T>(input: { items?: (T)[]; prompt?: string }): Promise<T[]>;
export async function of(input: Record<string, unknown>): Promise<unknown> { return runMethod(__of_23_spec, input); }

/** `Array.prototype.pop` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __pop_24_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'pop',
  functionId: 'Array.prototype.pop',
  kind: 'instance',
  native: resolveNative('Array.prototype.pop'),
  receiverKey: "array",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.pop\`.
## Original signature(s)
  Overload 1: () => T
## JSDoc
Removes the last element from an array and returns it.
If the array is empty, undefined is returned and the array is not modified.

## How to respond
- Behave EXACTLY as the original \`pop\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function pop<T>(input: { array: T[]; prompt?: string }): Promise<T>;
export async function pop(input: Record<string, unknown>): Promise<unknown> { return runMethod(__pop_24_spec, input); }

/** `Array.prototype.push` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __push_25_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'push',
  functionId: 'Array.prototype.push',
  kind: 'instance',
  native: resolveNative('Array.prototype.push'),
  receiverKey: "array",
  paramOrder: ["items"],
  variadicKey: "items",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.push\`.
## Original signature(s)
  Overload 1: (...items: T[]) => number
## JSDoc
Appends new elements to the end of an array, and returns the new length of the array.

## How to respond
- Behave EXACTLY as the original \`push\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"items","type":"T[]"}],
};
export function push<T>(input: { array: T[]; items?: (T)[]; prompt?: string }): Promise<number>;
export async function push(input: Record<string, unknown>): Promise<unknown> { return runMethod(__push_25_spec, input); }

/** `Array.prototype.reduce` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __reduce_26_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'reduce',
  functionId: 'Array.prototype.reduce',
  kind: 'instance',
  native: resolveNative('Array.prototype.reduce'),
  receiverKey: "array",
  paramOrder: ["callbackfn","initialValue"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.reduce\`.
## Original signature(s)
  Overload 1: (callbackfn?: (previousValue: T, currentValue: T, currentIndex: number, array: T[]) => T) => T
  Overload 2: (callbackfn?: (previousValue: T, currentValue: T, currentIndex: number, array: T[]) => T, initialValue?: T) => T
  Overload 3: (callbackfn?: (previousValue: U, currentValue: T, currentIndex: number, array: T[]) => U, initialValue?: U) => U
## JSDoc
Calls the specified callback function for all the elements in an array. The return value of the callback function is the accumulated result, and is provided as an argument in the next call to the callback function.

## How to respond
- Behave EXACTLY as the original \`reduce\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"callbackfn?","type":"(previousValue: T, currentValue: T, currentIndex: number, array: T[]) => T"}],
};
export function reduce<T>(input: { array: T[]; callbackfn?: (previousValue: T, currentValue: T, currentIndex: number, array: T[]) => T; prompt?: string }): Promise<T>;
export function reduce<T>(input: { array: T[]; callbackfn?: (previousValue: T, currentValue: T, currentIndex: number, array: T[]) => T; initialValue?: T; prompt?: string }): Promise<T>;
export function reduce<T, U>(input: { array: T[]; callbackfn?: (previousValue: U, currentValue: T, currentIndex: number, array: T[]) => U; initialValue?: U; prompt?: string }): Promise<U>;
export async function reduce(input: Record<string, unknown>): Promise<unknown> { return runMethod(__reduce_26_spec, input); }

/** `Array.prototype.reduceRight` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __reduceRight_27_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'reduceRight',
  functionId: 'Array.prototype.reduceRight',
  kind: 'instance',
  native: resolveNative('Array.prototype.reduceRight'),
  receiverKey: "array",
  paramOrder: ["callbackfn","initialValue"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.reduceRight\`.
## Original signature(s)
  Overload 1: (callbackfn?: (previousValue: T, currentValue: T, currentIndex: number, array: T[]) => T) => T
  Overload 2: (callbackfn?: (previousValue: T, currentValue: T, currentIndex: number, array: T[]) => T, initialValue?: T) => T
  Overload 3: (callbackfn?: (previousValue: U, currentValue: T, currentIndex: number, array: T[]) => U, initialValue?: U) => U
## JSDoc
Calls the specified callback function for all the elements in an array, in descending order. The return value of the callback function is the accumulated result, and is provided as an argument in the next call to the callback function.

## How to respond
- Behave EXACTLY as the original \`reduceRight\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"callbackfn?","type":"(previousValue: T, currentValue: T, currentIndex: number, array: T[]) => T"}],
};
export function reduceRight<T>(input: { array: T[]; callbackfn?: (previousValue: T, currentValue: T, currentIndex: number, array: T[]) => T; prompt?: string }): Promise<T>;
export function reduceRight<T>(input: { array: T[]; callbackfn?: (previousValue: T, currentValue: T, currentIndex: number, array: T[]) => T; initialValue?: T; prompt?: string }): Promise<T>;
export function reduceRight<T, U>(input: { array: T[]; callbackfn?: (previousValue: U, currentValue: T, currentIndex: number, array: T[]) => U; initialValue?: U; prompt?: string }): Promise<U>;
export async function reduceRight(input: Record<string, unknown>): Promise<unknown> { return runMethod(__reduceRight_27_spec, input); }

/** `Array.prototype.reverse` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __reverse_28_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'reverse',
  functionId: 'Array.prototype.reverse',
  kind: 'instance',
  native: resolveNative('Array.prototype.reverse'),
  receiverKey: "array",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.reverse\`.
## Original signature(s)
  Overload 1: () => T[]
## JSDoc
Reverses the elements in an array in place.
This method mutates the array and returns a reference to the same array.

## How to respond
- Behave EXACTLY as the original \`reverse\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function reverse<T>(input: { array: T[]; prompt?: string }): Promise<T[]>;
export async function reverse(input: Record<string, unknown>): Promise<unknown> { return runMethod(__reverse_28_spec, input); }

/** `Array.prototype.shift` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __shift_29_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'shift',
  functionId: 'Array.prototype.shift',
  kind: 'instance',
  native: resolveNative('Array.prototype.shift'),
  receiverKey: "array",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.shift\`.
## Original signature(s)
  Overload 1: () => T
## JSDoc
Removes the first element from an array and returns it.
If the array is empty, undefined is returned and the array is not modified.

## How to respond
- Behave EXACTLY as the original \`shift\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function shift<T>(input: { array: T[]; prompt?: string }): Promise<T>;
export async function shift(input: Record<string, unknown>): Promise<unknown> { return runMethod(__shift_29_spec, input); }

/** `Array.prototype.slice` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __slice_30_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'slice',
  functionId: 'Array.prototype.slice',
  kind: 'instance',
  native: resolveNative('Array.prototype.slice'),
  receiverKey: "array",
  paramOrder: ["start","end"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.slice\`.
## Original signature(s)
  Overload 1: (start?: number, end?: number) => T[]
## JSDoc
Returns a copy of a section of an array.
For both start and end, a negative index can be used to indicate an offset from the end of the array.
For example, -2 refers to the second to last element of the array.

## How to respond
- Behave EXACTLY as the original \`slice\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"start?","type":"number"},{"name":"end?","type":"number"}],
};
export function slice<T>(input: { array: T[]; start?: number; end?: number; prompt?: string }): Promise<T[]>;
export async function slice(input: Record<string, unknown>): Promise<unknown> { return runMethod(__slice_30_spec, input); }

/** `Array.prototype.some` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __some_31_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'some',
  functionId: 'Array.prototype.some',
  kind: 'instance',
  native: resolveNative('Array.prototype.some'),
  receiverKey: "array",
  paramOrder: ["predicate","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.some\`.
## Original signature(s)
  Overload 1: (predicate?: (value: T, index: number, array: T[]) => unknown, thisArg?: any) => boolean
## JSDoc
Determines whether the specified callback function returns true for any element of an array.

## How to respond
- Behave EXACTLY as the original \`some\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: T, index: number, array: T[]) => unknown"},{"name":"thisArg?","type":"any"}],
};
export function some<T>(input: { array: T[]; predicate?: (value: T, index: number, array: T[]) => unknown; thisArg?: any; prompt?: string }): Promise<boolean>;
export async function some(input: Record<string, unknown>): Promise<unknown> { return runMethod(__some_31_spec, input); }

/** `Array.prototype.sort` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __sort_32_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'sort',
  functionId: 'Array.prototype.sort',
  kind: 'instance',
  native: resolveNative('Array.prototype.sort'),
  receiverKey: "array",
  paramOrder: ["compareFn"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.sort\`.
## Original signature(s)
  Overload 1: (compareFn?: (a: T, b: T) => number) => T[]
## JSDoc
Sorts an array in place.
This method mutates the array and returns a reference to the same array.

## How to respond
- Behave EXACTLY as the original \`sort\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"compareFn?","type":"(a: T, b: T) => number"}],
};
export function sort<T>(input: { array: T[]; compareFn?: (a: T, b: T) => number; prompt?: string }): Promise<T[]>;
export async function sort(input: Record<string, unknown>): Promise<unknown> { return runMethod(__sort_32_spec, input); }

/** `Array.prototype.splice` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __splice_33_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'splice',
  functionId: 'Array.prototype.splice',
  kind: 'instance',
  native: resolveNative('Array.prototype.splice'),
  receiverKey: "array",
  paramOrder: ["start","deleteCount","items"],
  variadicKey: "items",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.splice\`.
## Original signature(s)
  Overload 1: (start: number, deleteCount?: number) => T[]
  Overload 2: (start: number, deleteCount: number, ...items: T[]) => T[]
## JSDoc
Removes elements from an array and, if necessary, inserts new elements in their place, returning the deleted elements.

## How to respond
- Behave EXACTLY as the original \`splice\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"start","type":"number"},{"name":"deleteCount?","type":"number"}],
};
export function splice<T>(input: { array: T[]; start: number; deleteCount?: number; prompt?: string }): Promise<T[]>;
export function splice<T>(input: { array: T[]; start: number; deleteCount: number; items?: (T)[]; prompt?: string }): Promise<T[]>;
export async function splice(input: Record<string, unknown>): Promise<unknown> { return runMethod(__splice_33_spec, input); }

/** `Array.prototype.toReversed` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toReversed_34_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'toReversed',
  functionId: 'Array.prototype.toReversed',
  kind: 'instance',
  native: resolveNative('Array.prototype.toReversed'),
  receiverKey: "array",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.toReversed\`.
## Original signature(s)
  Overload 1: () => T[]
## JSDoc
Returns a copy of an array with its elements reversed.

## How to respond
- Behave EXACTLY as the original \`toReversed\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function toReversed<T>(input: { array: T[]; prompt?: string }): Promise<T[]>;
export async function toReversed(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toReversed_34_spec, input); }

/** `Array.prototype.toSorted` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toSorted_35_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'toSorted',
  functionId: 'Array.prototype.toSorted',
  kind: 'instance',
  native: resolveNative('Array.prototype.toSorted'),
  receiverKey: "array",
  paramOrder: ["compareFn"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.toSorted\`.
## Original signature(s)
  Overload 1: (compareFn?: (a: T, b: T) => number) => T[]
## JSDoc
Returns a copy of an array with its elements sorted.

## How to respond
- Behave EXACTLY as the original \`toSorted\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"compareFn?","type":"(a: T, b: T) => number"}],
};
export function toSorted<T>(input: { array: T[]; compareFn?: (a: T, b: T) => number; prompt?: string }): Promise<T[]>;
export async function toSorted(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toSorted_35_spec, input); }

/** `Array.prototype.toSpliced` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toSpliced_36_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'toSpliced',
  functionId: 'Array.prototype.toSpliced',
  kind: 'instance',
  native: resolveNative('Array.prototype.toSpliced'),
  receiverKey: "array",
  paramOrder: ["start","deleteCount","items"],
  variadicKey: "items",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.toSpliced\`.
## Original signature(s)
  Overload 1: (start: number, deleteCount: number, ...items: T[]) => T[]
  Overload 2: (start: number, deleteCount?: number) => T[]
## JSDoc
Copies an array and removes elements and, if necessary, inserts new elements in their place. Returns the copied array.

## How to respond
- Behave EXACTLY as the original \`toSpliced\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"start","type":"number"},{"name":"deleteCount","type":"number"},{"name":"items","type":"T[]"}],
};
export function toSpliced<T>(input: { array: T[]; start: number; deleteCount: number; items?: (T)[]; prompt?: string }): Promise<T[]>;
export function toSpliced<T>(input: { array: T[]; start: number; deleteCount?: number; prompt?: string }): Promise<T[]>;
export async function toSpliced(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toSpliced_36_spec, input); }

/** `Array.prototype.unshift` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __unshift_37_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'unshift',
  functionId: 'Array.prototype.unshift',
  kind: 'instance',
  native: resolveNative('Array.prototype.unshift'),
  receiverKey: "array",
  paramOrder: ["items"],
  variadicKey: "items",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.unshift\`.
## Original signature(s)
  Overload 1: (...items: T[]) => number
## JSDoc
Inserts new elements at the start of an array, and returns the new length of the array.

## How to respond
- Behave EXACTLY as the original \`unshift\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"items","type":"T[]"}],
};
export function unshift<T>(input: { array: T[]; items?: (T)[]; prompt?: string }): Promise<number>;
export async function unshift(input: Record<string, unknown>): Promise<unknown> { return runMethod(__unshift_37_spec, input); }

/** `Array.prototype.values` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __values_38_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'values',
  functionId: 'Array.prototype.values',
  kind: 'instance',
  native: resolveNative('Array.prototype.values'),
  receiverKey: "array",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.values\`.
## Original signature(s)
  Overload 1: () => ArrayIterator<T>
## JSDoc
Returns an iterable of values in the array

## How to respond
- Behave EXACTLY as the original \`values\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function values<T>(input: { array: T[]; prompt?: string }): Promise<ArrayIterator<T>>;
export async function values(input: Record<string, unknown>): Promise<unknown> { return runMethod(__values_38_spec, input); }

/** `Array.prototype.with` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __with_39_spec: GeneratedMethodSpec = {
  group: 'array',
  methodName: 'with',
  functionId: 'Array.prototype.with',
  kind: 'instance',
  native: resolveNative('Array.prototype.with'),
  receiverKey: "array",
  paramOrder: ["index","value"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Array.prototype.with\`.
## Original signature(s)
  Overload 1: (index: number, value: T) => T[]
## JSDoc
Copies an array, then overwrites the value at the provided index with the
given value. If the index is negative, then it replaces from the end
of the array.

## How to respond
- Behave EXACTLY as the original \`with\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"index","type":"number"},{"name":"value","type":"T"}],
};
function _with<T>(input: { array: T[]; index: number; value: T; prompt?: string }): Promise<T[]>;
async function _with(input: Record<string, unknown>): Promise<unknown> { return runMethod(__with_39_spec, input); }

export { _with as with };
