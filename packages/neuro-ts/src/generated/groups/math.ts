// Auto-generated by scripts/generate-wrappers.ts. Do not edit.
import { runMethod, resolveNative, type GeneratedMethodSpec } from '../../runtime';

/** `Math.abs` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __abs_0_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'abs',
  functionId: 'Math.abs',
  kind: 'static',
  native: resolveNative('Math.abs'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.abs\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the absolute value of a number (the value without regard to whether it is positive or negative).
For example, the absolute value of -5 is the same as the absolute value of 5.

## How to respond
- Behave EXACTLY as the original \`abs\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function abs(input: { x: number; prompt?: string }): Promise<number>;
export async function abs(input: Record<string, unknown>): Promise<unknown> { return runMethod(__abs_0_spec, input); }

/** `Math.acos` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __acos_1_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'acos',
  functionId: 'Math.acos',
  kind: 'static',
  native: resolveNative('Math.acos'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.acos\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the arc cosine (or inverse cosine) of a number.

## How to respond
- Behave EXACTLY as the original \`acos\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function acos(input: { x: number; prompt?: string }): Promise<number>;
export async function acos(input: Record<string, unknown>): Promise<unknown> { return runMethod(__acos_1_spec, input); }

/** `Math.acosh` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __acosh_2_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'acosh',
  functionId: 'Math.acosh',
  kind: 'static',
  native: resolveNative('Math.acosh'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.acosh\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the inverse hyperbolic cosine of a number.

## How to respond
- Behave EXACTLY as the original \`acosh\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function acosh(input: { x: number; prompt?: string }): Promise<number>;
export async function acosh(input: Record<string, unknown>): Promise<unknown> { return runMethod(__acosh_2_spec, input); }

/** `Math.asin` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __asin_3_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'asin',
  functionId: 'Math.asin',
  kind: 'static',
  native: resolveNative('Math.asin'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.asin\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the arcsine of a number.

## How to respond
- Behave EXACTLY as the original \`asin\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function asin(input: { x: number; prompt?: string }): Promise<number>;
export async function asin(input: Record<string, unknown>): Promise<unknown> { return runMethod(__asin_3_spec, input); }

/** `Math.asinh` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __asinh_4_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'asinh',
  functionId: 'Math.asinh',
  kind: 'static',
  native: resolveNative('Math.asinh'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.asinh\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the inverse hyperbolic sine of a number.

## How to respond
- Behave EXACTLY as the original \`asinh\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function asinh(input: { x: number; prompt?: string }): Promise<number>;
export async function asinh(input: Record<string, unknown>): Promise<unknown> { return runMethod(__asinh_4_spec, input); }

/** `Math.atan` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __atan_5_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'atan',
  functionId: 'Math.atan',
  kind: 'static',
  native: resolveNative('Math.atan'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.atan\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the arctangent of a number.

## How to respond
- Behave EXACTLY as the original \`atan\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function atan(input: { x: number; prompt?: string }): Promise<number>;
export async function atan(input: Record<string, unknown>): Promise<unknown> { return runMethod(__atan_5_spec, input); }

/** `Math.atan2` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __atan2_6_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'atan2',
  functionId: 'Math.atan2',
  kind: 'static',
  native: resolveNative('Math.atan2'),
  receiverKey: "",
  paramOrder: ["y","x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.atan2\`.
## Original signature(s)
  Overload 1: (y: number, x: number) => number
## JSDoc
Returns the angle (in radians) between the X axis and the line going through both the origin and the given point.

## How to respond
- Behave EXACTLY as the original \`atan2\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"y","type":"number"},{"name":"x","type":"number"}],
};
export function atan2(input: { y: number; x: number; prompt?: string }): Promise<number>;
export async function atan2(input: Record<string, unknown>): Promise<unknown> { return runMethod(__atan2_6_spec, input); }

/** `Math.atanh` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __atanh_7_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'atanh',
  functionId: 'Math.atanh',
  kind: 'static',
  native: resolveNative('Math.atanh'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.atanh\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the inverse hyperbolic tangent of a number.

## How to respond
- Behave EXACTLY as the original \`atanh\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function atanh(input: { x: number; prompt?: string }): Promise<number>;
export async function atanh(input: Record<string, unknown>): Promise<unknown> { return runMethod(__atanh_7_spec, input); }

/** `Math.cbrt` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __cbrt_8_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'cbrt',
  functionId: 'Math.cbrt',
  kind: 'static',
  native: resolveNative('Math.cbrt'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.cbrt\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns an implementation-dependent approximation to the cube root of number.

## How to respond
- Behave EXACTLY as the original \`cbrt\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function cbrt(input: { x: number; prompt?: string }): Promise<number>;
export async function cbrt(input: Record<string, unknown>): Promise<unknown> { return runMethod(__cbrt_8_spec, input); }

/** `Math.ceil` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __ceil_9_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'ceil',
  functionId: 'Math.ceil',
  kind: 'static',
  native: resolveNative('Math.ceil'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.ceil\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the smallest integer greater than or equal to its numeric argument.

## How to respond
- Behave EXACTLY as the original \`ceil\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function ceil(input: { x: number; prompt?: string }): Promise<number>;
export async function ceil(input: Record<string, unknown>): Promise<unknown> { return runMethod(__ceil_9_spec, input); }

/** `Math.clz32` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __clz32_10_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'clz32',
  functionId: 'Math.clz32',
  kind: 'static',
  native: resolveNative('Math.clz32'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.clz32\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the number of leading zero bits in the 32-bit binary representation of a number.

## How to respond
- Behave EXACTLY as the original \`clz32\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function clz32(input: { x: number; prompt?: string }): Promise<number>;
export async function clz32(input: Record<string, unknown>): Promise<unknown> { return runMethod(__clz32_10_spec, input); }

/** `Math.cos` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __cos_11_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'cos',
  functionId: 'Math.cos',
  kind: 'static',
  native: resolveNative('Math.cos'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.cos\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the cosine of a number.

## How to respond
- Behave EXACTLY as the original \`cos\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function cos(input: { x: number; prompt?: string }): Promise<number>;
export async function cos(input: Record<string, unknown>): Promise<unknown> { return runMethod(__cos_11_spec, input); }

/** `Math.cosh` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __cosh_12_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'cosh',
  functionId: 'Math.cosh',
  kind: 'static',
  native: resolveNative('Math.cosh'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.cosh\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the hyperbolic cosine of a number.

## How to respond
- Behave EXACTLY as the original \`cosh\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function cosh(input: { x: number; prompt?: string }): Promise<number>;
export async function cosh(input: Record<string, unknown>): Promise<unknown> { return runMethod(__cosh_12_spec, input); }

/** `Math.exp` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __exp_13_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'exp',
  functionId: 'Math.exp',
  kind: 'static',
  native: resolveNative('Math.exp'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.exp\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns e (the base of natural logarithms) raised to a power.

## How to respond
- Behave EXACTLY as the original \`exp\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function exp(input: { x: number; prompt?: string }): Promise<number>;
export async function exp(input: Record<string, unknown>): Promise<unknown> { return runMethod(__exp_13_spec, input); }

/** `Math.expm1` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __expm1_14_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'expm1',
  functionId: 'Math.expm1',
  kind: 'static',
  native: resolveNative('Math.expm1'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.expm1\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the result of (e^x - 1), which is an implementation-dependent approximation to
subtracting 1 from the exponential function of x (e raised to the power of x, where e
is the base of the natural logarithms).

## How to respond
- Behave EXACTLY as the original \`expm1\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function expm1(input: { x: number; prompt?: string }): Promise<number>;
export async function expm1(input: Record<string, unknown>): Promise<unknown> { return runMethod(__expm1_14_spec, input); }

/** `Math.f16round` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __f16round_15_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'f16round',
  functionId: 'Math.f16round',
  kind: 'static',
  native: resolveNative('Math.f16round'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.f16round\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the nearest half precision float representation of a number.

## How to respond
- Behave EXACTLY as the original \`f16round\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function f16round(input: { x: number; prompt?: string }): Promise<number>;
export async function f16round(input: Record<string, unknown>): Promise<unknown> { return runMethod(__f16round_15_spec, input); }

/** `Math.floor` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __floor_16_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'floor',
  functionId: 'Math.floor',
  kind: 'static',
  native: resolveNative('Math.floor'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.floor\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the greatest integer less than or equal to its numeric argument.

## How to respond
- Behave EXACTLY as the original \`floor\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function floor(input: { x: number; prompt?: string }): Promise<number>;
export async function floor(input: Record<string, unknown>): Promise<unknown> { return runMethod(__floor_16_spec, input); }

/** `Math.fround` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __fround_17_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'fround',
  functionId: 'Math.fround',
  kind: 'static',
  native: resolveNative('Math.fround'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.fround\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the nearest single precision float representation of a number.

## How to respond
- Behave EXACTLY as the original \`fround\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function fround(input: { x: number; prompt?: string }): Promise<number>;
export async function fround(input: Record<string, unknown>): Promise<unknown> { return runMethod(__fround_17_spec, input); }

/** `Math.hypot` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __hypot_18_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'hypot',
  functionId: 'Math.hypot',
  kind: 'static',
  native: resolveNative('Math.hypot'),
  receiverKey: "",
  paramOrder: ["values"],
  variadicKey: "values",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.hypot\`.
## Original signature(s)
  Overload 1: (...values: number[]) => number
## JSDoc
Returns the square root of the sum of squares of its arguments.

## How to respond
- Behave EXACTLY as the original \`hypot\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"values","type":"number[]"}],
};
export function hypot(input: { values?: (number)[]; prompt?: string }): Promise<number>;
export async function hypot(input: Record<string, unknown>): Promise<unknown> { return runMethod(__hypot_18_spec, input); }

/** `Math.imul` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __imul_19_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'imul',
  functionId: 'Math.imul',
  kind: 'static',
  native: resolveNative('Math.imul'),
  receiverKey: "",
  paramOrder: ["x","y"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.imul\`.
## Original signature(s)
  Overload 1: (x: number, y: number) => number
## JSDoc
Returns the result of 32-bit multiplication of two numbers.

## How to respond
- Behave EXACTLY as the original \`imul\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"},{"name":"y","type":"number"}],
};
export function imul(input: { x: number; y: number; prompt?: string }): Promise<number>;
export async function imul(input: Record<string, unknown>): Promise<unknown> { return runMethod(__imul_19_spec, input); }

/** `Math.log` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __log_20_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'log',
  functionId: 'Math.log',
  kind: 'static',
  native: resolveNative('Math.log'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.log\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the natural logarithm (base e) of a number.

## How to respond
- Behave EXACTLY as the original \`log\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function log(input: { x: number; prompt?: string }): Promise<number>;
export async function log(input: Record<string, unknown>): Promise<unknown> { return runMethod(__log_20_spec, input); }

/** `Math.log10` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __log10_21_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'log10',
  functionId: 'Math.log10',
  kind: 'static',
  native: resolveNative('Math.log10'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.log10\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the base 10 logarithm of a number.

## How to respond
- Behave EXACTLY as the original \`log10\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function log10(input: { x: number; prompt?: string }): Promise<number>;
export async function log10(input: Record<string, unknown>): Promise<unknown> { return runMethod(__log10_21_spec, input); }

/** `Math.log1p` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __log1p_22_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'log1p',
  functionId: 'Math.log1p',
  kind: 'static',
  native: resolveNative('Math.log1p'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.log1p\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the natural logarithm of 1 + x.

## How to respond
- Behave EXACTLY as the original \`log1p\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function log1p(input: { x: number; prompt?: string }): Promise<number>;
export async function log1p(input: Record<string, unknown>): Promise<unknown> { return runMethod(__log1p_22_spec, input); }

/** `Math.log2` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __log2_23_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'log2',
  functionId: 'Math.log2',
  kind: 'static',
  native: resolveNative('Math.log2'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.log2\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the base 2 logarithm of a number.

## How to respond
- Behave EXACTLY as the original \`log2\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function log2(input: { x: number; prompt?: string }): Promise<number>;
export async function log2(input: Record<string, unknown>): Promise<unknown> { return runMethod(__log2_23_spec, input); }

/** `Math.max` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __max_24_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'max',
  functionId: 'Math.max',
  kind: 'static',
  native: resolveNative('Math.max'),
  receiverKey: "",
  paramOrder: ["values"],
  variadicKey: "values",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.max\`.
## Original signature(s)
  Overload 1: (...values: number[]) => number
## JSDoc
Returns the larger of a set of supplied numeric expressions.

## How to respond
- Behave EXACTLY as the original \`max\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"values","type":"number[]"}],
};
export function max(input: { values?: (number)[]; prompt?: string }): Promise<number>;
export async function max(input: Record<string, unknown>): Promise<unknown> { return runMethod(__max_24_spec, input); }

/** `Math.min` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __min_25_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'min',
  functionId: 'Math.min',
  kind: 'static',
  native: resolveNative('Math.min'),
  receiverKey: "",
  paramOrder: ["values"],
  variadicKey: "values",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.min\`.
## Original signature(s)
  Overload 1: (...values: number[]) => number
## JSDoc
Returns the smaller of a set of supplied numeric expressions.

## How to respond
- Behave EXACTLY as the original \`min\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"values","type":"number[]"}],
};
export function min(input: { values?: (number)[]; prompt?: string }): Promise<number>;
export async function min(input: Record<string, unknown>): Promise<unknown> { return runMethod(__min_25_spec, input); }

/** `Math.pow` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __pow_26_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'pow',
  functionId: 'Math.pow',
  kind: 'static',
  native: resolveNative('Math.pow'),
  receiverKey: "",
  paramOrder: ["x","y"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.pow\`.
## Original signature(s)
  Overload 1: (x: number, y: number) => number
## JSDoc
Returns the value of a base expression taken to a specified power.

## How to respond
- Behave EXACTLY as the original \`pow\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"},{"name":"y","type":"number"}],
};
export function pow(input: { x: number; y: number; prompt?: string }): Promise<number>;
export async function pow(input: Record<string, unknown>): Promise<unknown> { return runMethod(__pow_26_spec, input); }

/** `Math.random` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __random_27_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'random',
  functionId: 'Math.random',
  kind: 'static',
  native: resolveNative('Math.random'),
  receiverKey: "",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.random\`.
## Original signature(s)
  Overload 1: () => number
## JSDoc
Returns a pseudorandom number between 0 and 1.

## How to respond
- Behave EXACTLY as the original \`random\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function random(input: { prompt?: string }): Promise<number>;
export async function random(input: Record<string, unknown>): Promise<unknown> { return runMethod(__random_27_spec, input); }

/** `Math.round` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __round_28_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'round',
  functionId: 'Math.round',
  kind: 'static',
  native: resolveNative('Math.round'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.round\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns a supplied numeric expression rounded to the nearest integer.

## How to respond
- Behave EXACTLY as the original \`round\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function round(input: { x: number; prompt?: string }): Promise<number>;
export async function round(input: Record<string, unknown>): Promise<unknown> { return runMethod(__round_28_spec, input); }

/** `Math.sign` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __sign_29_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'sign',
  functionId: 'Math.sign',
  kind: 'static',
  native: resolveNative('Math.sign'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.sign\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the sign of the x, indicating whether x is positive, negative or zero.

## How to respond
- Behave EXACTLY as the original \`sign\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function sign(input: { x: number; prompt?: string }): Promise<number>;
export async function sign(input: Record<string, unknown>): Promise<unknown> { return runMethod(__sign_29_spec, input); }

/** `Math.sin` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __sin_30_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'sin',
  functionId: 'Math.sin',
  kind: 'static',
  native: resolveNative('Math.sin'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.sin\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the sine of a number.

## How to respond
- Behave EXACTLY as the original \`sin\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function sin(input: { x: number; prompt?: string }): Promise<number>;
export async function sin(input: Record<string, unknown>): Promise<unknown> { return runMethod(__sin_30_spec, input); }

/** `Math.sinh` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __sinh_31_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'sinh',
  functionId: 'Math.sinh',
  kind: 'static',
  native: resolveNative('Math.sinh'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.sinh\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the hyperbolic sine of a number.

## How to respond
- Behave EXACTLY as the original \`sinh\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function sinh(input: { x: number; prompt?: string }): Promise<number>;
export async function sinh(input: Record<string, unknown>): Promise<unknown> { return runMethod(__sinh_31_spec, input); }

/** `Math.sqrt` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __sqrt_32_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'sqrt',
  functionId: 'Math.sqrt',
  kind: 'static',
  native: resolveNative('Math.sqrt'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.sqrt\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the square root of a number.

## How to respond
- Behave EXACTLY as the original \`sqrt\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function sqrt(input: { x: number; prompt?: string }): Promise<number>;
export async function sqrt(input: Record<string, unknown>): Promise<unknown> { return runMethod(__sqrt_32_spec, input); }

/** `Math.tan` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __tan_33_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'tan',
  functionId: 'Math.tan',
  kind: 'static',
  native: resolveNative('Math.tan'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.tan\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the tangent of a number.

## How to respond
- Behave EXACTLY as the original \`tan\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function tan(input: { x: number; prompt?: string }): Promise<number>;
export async function tan(input: Record<string, unknown>): Promise<unknown> { return runMethod(__tan_33_spec, input); }

/** `Math.tanh` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __tanh_34_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'tanh',
  functionId: 'Math.tanh',
  kind: 'static',
  native: resolveNative('Math.tanh'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.tanh\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the hyperbolic tangent of a number.

## How to respond
- Behave EXACTLY as the original \`tanh\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function tanh(input: { x: number; prompt?: string }): Promise<number>;
export async function tanh(input: Record<string, unknown>): Promise<unknown> { return runMethod(__tanh_34_spec, input); }

/** `Math.trunc` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __trunc_35_spec: GeneratedMethodSpec = {
  group: 'math',
  methodName: 'trunc',
  functionId: 'Math.trunc',
  kind: 'static',
  native: resolveNative('Math.trunc'),
  receiverKey: "",
  paramOrder: ["x"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Math.trunc\`.
## Original signature(s)
  Overload 1: (x: number) => number
## JSDoc
Returns the integral part of the numeric expression x, removing any fractional digits.
If x is already an integer, the result is x.

## How to respond
- Behave EXACTLY as the original \`trunc\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"x","type":"number"}],
};
export function trunc(input: { x: number; prompt?: string }): Promise<number>;
export async function trunc(input: Record<string, unknown>): Promise<unknown> { return runMethod(__trunc_35_spec, input); }


