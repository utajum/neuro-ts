// Auto-generated by scripts/generate-wrappers.ts. Do not edit.
import { runMethod, resolveNative, type GeneratedMethodSpec } from '../../runtime';

/** `Uint8Array.prototype.at` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __at_0_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'at',
  functionId: 'Uint8Array.prototype.at',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.at'),
  receiverKey: "uint8Array",
  paramOrder: ["index"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.at\`.
## Original signature(s)
  Overload 1: (index: number) => number
## JSDoc
Returns the item located at the specified index.

## How to respond
- Behave EXACTLY as the original \`at\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"index","type":"number"}],
};
export function at(input: { uint8Array: Uint8Array; index: number; prompt?: string }): Promise<number>;
export async function at(input: Record<string, unknown>): Promise<unknown> { return runMethod(__at_0_spec, input); }

/** `Uint8Array.prototype.copyWithin` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __copyWithin_1_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'copyWithin',
  functionId: 'Uint8Array.prototype.copyWithin',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.copyWithin'),
  receiverKey: "uint8Array",
  paramOrder: ["target","start","end"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.copyWithin\`.
## Original signature(s)
  Overload 1: (target: number, start: number, end?: number) => Uint8Array
## JSDoc
Returns the this object after copying a section of the array identified by start and end
to the same array starting at position target

## How to respond
- Behave EXACTLY as the original \`copyWithin\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"target","type":"number"},{"name":"start","type":"number"},{"name":"end?","type":"number"}],
};
export function copyWithin(input: { uint8Array: Uint8Array; target: number; start: number; end?: number; prompt?: string }): Promise<Uint8Array>;
export async function copyWithin(input: Record<string, unknown>): Promise<unknown> { return runMethod(__copyWithin_1_spec, input); }

/** `Uint8Array.prototype.entries` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __entries_2_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'entries',
  functionId: 'Uint8Array.prototype.entries',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.entries'),
  receiverKey: "uint8Array",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.entries\`.
## Original signature(s)
  Overload 1: () => ArrayIterator<[number, number]>
## JSDoc
Returns an array of key, value pairs for every entry in the array

## How to respond
- Behave EXACTLY as the original \`entries\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function entries(input: { uint8Array: Uint8Array; prompt?: string }): Promise<ArrayIterator<[number, number]>>;
export async function entries(input: Record<string, unknown>): Promise<unknown> { return runMethod(__entries_2_spec, input); }

/** `Uint8Array.prototype.every` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __every_3_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'every',
  functionId: 'Uint8Array.prototype.every',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.every'),
  receiverKey: "uint8Array",
  paramOrder: ["predicate","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.every\`.
## Original signature(s)
  Overload 1: (predicate?: (value: number, index: number, array: Uint8Array) => unknown, thisArg?: any) => boolean
## JSDoc
Determines whether all the members of an array satisfy the specified test.

## How to respond
- Behave EXACTLY as the original \`every\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: number, index: number, array: Uint8Array) => unknown"},{"name":"thisArg?","type":"any"}],
};
export function every(input: { uint8Array: Uint8Array; predicate?: (value: number, index: number, array: Uint8Array) => unknown; thisArg?: any; prompt?: string }): Promise<boolean>;
export async function every(input: Record<string, unknown>): Promise<unknown> { return runMethod(__every_3_spec, input); }

/** `Uint8Array.prototype.fill` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __fill_4_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'fill',
  functionId: 'Uint8Array.prototype.fill',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.fill'),
  receiverKey: "uint8Array",
  paramOrder: ["value","start","end"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.fill\`.
## Original signature(s)
  Overload 1: (value: number, start?: number, end?: number) => Uint8Array
## JSDoc
Changes all array elements from \`start\` to \`end\` index to a static \`value\` and returns the modified array

## How to respond
- Behave EXACTLY as the original \`fill\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"value","type":"number"},{"name":"start?","type":"number"},{"name":"end?","type":"number"}],
};
export function fill(input: { uint8Array: Uint8Array; value: number; start?: number; end?: number; prompt?: string }): Promise<Uint8Array>;
export async function fill(input: Record<string, unknown>): Promise<unknown> { return runMethod(__fill_4_spec, input); }

/** `Uint8Array.prototype.filter` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __filter_5_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'filter',
  functionId: 'Uint8Array.prototype.filter',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.filter'),
  receiverKey: "uint8Array",
  paramOrder: ["predicate","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.filter\`.
## Original signature(s)
  Overload 1: (predicate?: (value: number, index: number, array: Uint8Array) => any, thisArg?: any) => Uint8Array
## JSDoc
Returns the elements of an array that meet the condition specified in a callback function.

## How to respond
- Behave EXACTLY as the original \`filter\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: number, index: number, array: Uint8Array) => any"},{"name":"thisArg?","type":"any"}],
};
export function filter(input: { uint8Array: Uint8Array; predicate?: (value: number, index: number, array: Uint8Array) => any; thisArg?: any; prompt?: string }): Promise<Uint8Array>;
export async function filter(input: Record<string, unknown>): Promise<unknown> { return runMethod(__filter_5_spec, input); }

/** `Uint8Array.prototype.find` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __find_6_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'find',
  functionId: 'Uint8Array.prototype.find',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.find'),
  receiverKey: "uint8Array",
  paramOrder: ["predicate","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.find\`.
## Original signature(s)
  Overload 1: (predicate?: (value: number, index: number, obj: Uint8Array) => boolean, thisArg?: any) => number
## JSDoc
Returns the value of the first element in the array where predicate is true, and undefined
otherwise.

## How to respond
- Behave EXACTLY as the original \`find\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: number, index: number, obj: Uint8Array) => boolean"},{"name":"thisArg?","type":"any"}],
};
export function find(input: { uint8Array: Uint8Array; predicate?: (value: number, index: number, obj: Uint8Array) => boolean; thisArg?: any; prompt?: string }): Promise<number>;
export async function find(input: Record<string, unknown>): Promise<unknown> { return runMethod(__find_6_spec, input); }

/** `Uint8Array.prototype.findIndex` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __findIndex_7_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'findIndex',
  functionId: 'Uint8Array.prototype.findIndex',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.findIndex'),
  receiverKey: "uint8Array",
  paramOrder: ["predicate","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.findIndex\`.
## Original signature(s)
  Overload 1: (predicate?: (value: number, index: number, obj: Uint8Array) => boolean, thisArg?: any) => number
## JSDoc
Returns the index of the first element in the array where predicate is true, and -1
otherwise.

## How to respond
- Behave EXACTLY as the original \`findIndex\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: number, index: number, obj: Uint8Array) => boolean"},{"name":"thisArg?","type":"any"}],
};
export function findIndex(input: { uint8Array: Uint8Array; predicate?: (value: number, index: number, obj: Uint8Array) => boolean; thisArg?: any; prompt?: string }): Promise<number>;
export async function findIndex(input: Record<string, unknown>): Promise<unknown> { return runMethod(__findIndex_7_spec, input); }

/** `Uint8Array.prototype.findLast` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __findLast_8_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'findLast',
  functionId: 'Uint8Array.prototype.findLast',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.findLast'),
  receiverKey: "uint8Array",
  paramOrder: ["predicate","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.findLast\`.
## Original signature(s)
  Overload 1: (predicate?: (value: number, index: number, array: Uint8Array) => boolean, thisArg?: any) => S
  Overload 2: (predicate?: (value: number, index: number, array: Uint8Array) => unknown, thisArg?: any) => number
## JSDoc
Returns the value of the last element in the array where predicate is true, and undefined
otherwise.

## How to respond
- Behave EXACTLY as the original \`findLast\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: number, index: number, array: Uint8Array) => boolean"},{"name":"thisArg?","type":"any"}],
};
export function findLast<S extends number>(input: { uint8Array: Uint8Array; predicate?: (value: number, index: number, array: Uint8Array) => boolean; thisArg?: any; prompt?: string }): Promise<S>;
export function findLast(input: { uint8Array: Uint8Array; predicate?: (value: number, index: number, array: Uint8Array) => unknown; thisArg?: any; prompt?: string }): Promise<number>;
export async function findLast(input: Record<string, unknown>): Promise<unknown> { return runMethod(__findLast_8_spec, input); }

/** `Uint8Array.prototype.findLastIndex` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __findLastIndex_9_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'findLastIndex',
  functionId: 'Uint8Array.prototype.findLastIndex',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.findLastIndex'),
  receiverKey: "uint8Array",
  paramOrder: ["predicate","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.findLastIndex\`.
## Original signature(s)
  Overload 1: (predicate?: (value: number, index: number, array: Uint8Array) => unknown, thisArg?: any) => number
## JSDoc
Returns the index of the last element in the array where predicate is true, and -1
otherwise.

## How to respond
- Behave EXACTLY as the original \`findLastIndex\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: number, index: number, array: Uint8Array) => unknown"},{"name":"thisArg?","type":"any"}],
};
export function findLastIndex(input: { uint8Array: Uint8Array; predicate?: (value: number, index: number, array: Uint8Array) => unknown; thisArg?: any; prompt?: string }): Promise<number>;
export async function findLastIndex(input: Record<string, unknown>): Promise<unknown> { return runMethod(__findLastIndex_9_spec, input); }

/** `Uint8Array.prototype.forEach` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __forEach_10_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'forEach',
  functionId: 'Uint8Array.prototype.forEach',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.forEach'),
  receiverKey: "uint8Array",
  paramOrder: ["callbackfn","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.forEach\`.
## Original signature(s)
  Overload 1: (callbackfn?: (value: number, index: number, array: Uint8Array) => void, thisArg?: any) => void
## JSDoc
Performs the specified action for each element in an array.

## How to respond
- Behave EXACTLY as the original \`forEach\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"callbackfn?","type":"(value: number, index: number, array: Uint8Array) => void"},{"name":"thisArg?","type":"any"}],
};
export function forEach(input: { uint8Array: Uint8Array; callbackfn?: (value: number, index: number, array: Uint8Array) => void; thisArg?: any; prompt?: string }): Promise<void>;
export async function forEach(input: Record<string, unknown>): Promise<unknown> { return runMethod(__forEach_10_spec, input); }

/** `Uint8Array.from` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __from_11_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'from',
  functionId: 'Uint8Array.from',
  kind: 'static',
  native: resolveNative('Uint8Array.from'),
  receiverKey: "",
  paramOrder: ["arrayLike","mapfn","thisArg","elements"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.from\`.
## Original signature(s)
  Overload 1: (arrayLike: ArrayLike<number>) => Uint8Array
  Overload 2: (arrayLike: ArrayLike<T>, mapfn?: (v: T, k: number) => number, thisArg?: any) => Uint8Array
  Overload 3: (elements: Iterable<number>) => Uint8Array
  Overload 4: (elements: Iterable<T>, mapfn?: (v: T, k: number) => number, thisArg?: any) => Uint8Array
## JSDoc
Creates an array from an array-like or iterable object.

## How to respond
- Behave EXACTLY as the original \`from\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"arrayLike","type":"ArrayLike<number>"}],
};
export function from(input: { arrayLike: ArrayLike<number>; prompt?: string }): Promise<Uint8Array>;
export function from<T>(input: { arrayLike: ArrayLike<T>; mapfn?: (v: T, k: number) => number; thisArg?: any; prompt?: string }): Promise<Uint8Array>;
export function from(input: { elements: Iterable<number>; prompt?: string }): Promise<Uint8Array>;
export function from<T>(input: { elements: Iterable<T>; mapfn?: (v: T, k: number) => number; thisArg?: any; prompt?: string }): Promise<Uint8Array>;
export async function from(input: Record<string, unknown>): Promise<unknown> { return runMethod(__from_11_spec, input); }

/** `Uint8Array.fromBase64` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __fromBase64_12_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'fromBase64',
  functionId: 'Uint8Array.fromBase64',
  kind: 'static',
  native: resolveNative('Uint8Array.fromBase64'),
  receiverKey: "",
  paramOrder: ["string","options"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.fromBase64\`.
## Original signature(s)
  Overload 1: (string: string, options?: { alphabet?: "base64" | "base64url" | undefined; lastChunkHandling?: "loose" | "strict" | "stop-before-partial" | undefined; }) => Uint8Array
## JSDoc
Creates a new \`Uint8Array\` from a base64-encoded string.

## How to respond
- Behave EXACTLY as the original \`fromBase64\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"string","type":"string"},{"name":"options?","type":"{ alphabet?: \"base64\" | \"base64url\" | undefined; lastChunkHandling?: \"loose\" | \"strict\" | \"stop-before-partial\" | undefined; }"}],
};
export function fromBase64(input: { string: string; options?: { alphabet?: "base64" | "base64url" | undefined; lastChunkHandling?: "loose" | "strict" | "stop-before-partial" | undefined; }; prompt?: string }): Promise<Uint8Array>;
export async function fromBase64(input: Record<string, unknown>): Promise<unknown> { return runMethod(__fromBase64_12_spec, input); }

/** `Uint8Array.fromHex` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __fromHex_13_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'fromHex',
  functionId: 'Uint8Array.fromHex',
  kind: 'static',
  native: resolveNative('Uint8Array.fromHex'),
  receiverKey: "",
  paramOrder: ["string"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.fromHex\`.
## Original signature(s)
  Overload 1: (string: string) => Uint8Array
## JSDoc
Creates a new \`Uint8Array\` from a base16-encoded string.

## How to respond
- Behave EXACTLY as the original \`fromHex\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"string","type":"string"}],
};
export function fromHex(input: { string: string; prompt?: string }): Promise<Uint8Array>;
export async function fromHex(input: Record<string, unknown>): Promise<unknown> { return runMethod(__fromHex_13_spec, input); }

/** `Uint8Array.prototype.includes` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __includes_14_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'includes',
  functionId: 'Uint8Array.prototype.includes',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.includes'),
  receiverKey: "uint8Array",
  paramOrder: ["searchElement","fromIndex"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.includes\`.
## Original signature(s)
  Overload 1: (searchElement: number, fromIndex?: number) => boolean
## JSDoc
Determines whether an array includes a certain element, returning true or false as appropriate.

## How to respond
- Behave EXACTLY as the original \`includes\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"searchElement","type":"number"},{"name":"fromIndex?","type":"number"}],
};
export function includes(input: { uint8Array: Uint8Array; searchElement: number; fromIndex?: number; prompt?: string }): Promise<boolean>;
export async function includes(input: Record<string, unknown>): Promise<unknown> { return runMethod(__includes_14_spec, input); }

/** `Uint8Array.prototype.indexOf` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __indexOf_15_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'indexOf',
  functionId: 'Uint8Array.prototype.indexOf',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.indexOf'),
  receiverKey: "uint8Array",
  paramOrder: ["searchElement","fromIndex"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.indexOf\`.
## Original signature(s)
  Overload 1: (searchElement: number, fromIndex?: number) => number
## JSDoc
Returns the index of the first occurrence of a value in an array, or -1 if it is not present.

## How to respond
- Behave EXACTLY as the original \`indexOf\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"searchElement","type":"number"},{"name":"fromIndex?","type":"number"}],
};
export function indexOf(input: { uint8Array: Uint8Array; searchElement: number; fromIndex?: number; prompt?: string }): Promise<number>;
export async function indexOf(input: Record<string, unknown>): Promise<unknown> { return runMethod(__indexOf_15_spec, input); }

/** `Uint8Array.prototype.join` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __join_16_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'join',
  functionId: 'Uint8Array.prototype.join',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.join'),
  receiverKey: "uint8Array",
  paramOrder: ["separator"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.join\`.
## Original signature(s)
  Overload 1: (separator?: string) => string
## JSDoc
Adds all the elements of an array separated by the specified separator string.

## How to respond
- Behave EXACTLY as the original \`join\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"separator?","type":"string"}],
};
export function join(input: { uint8Array: Uint8Array; separator?: string; prompt?: string }): Promise<string>;
export async function join(input: Record<string, unknown>): Promise<unknown> { return runMethod(__join_16_spec, input); }

/** `Uint8Array.prototype.keys` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __keys_17_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'keys',
  functionId: 'Uint8Array.prototype.keys',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.keys'),
  receiverKey: "uint8Array",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.keys\`.
## Original signature(s)
  Overload 1: () => ArrayIterator<number>
## JSDoc
Returns an list of keys in the array

## How to respond
- Behave EXACTLY as the original \`keys\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function keys(input: { uint8Array: Uint8Array; prompt?: string }): Promise<ArrayIterator<number>>;
export async function keys(input: Record<string, unknown>): Promise<unknown> { return runMethod(__keys_17_spec, input); }

/** `Uint8Array.prototype.lastIndexOf` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __lastIndexOf_18_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'lastIndexOf',
  functionId: 'Uint8Array.prototype.lastIndexOf',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.lastIndexOf'),
  receiverKey: "uint8Array",
  paramOrder: ["searchElement","fromIndex"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.lastIndexOf\`.
## Original signature(s)
  Overload 1: (searchElement: number, fromIndex?: number) => number
## JSDoc
Returns the index of the last occurrence of a value in an array, or -1 if it is not present.

## How to respond
- Behave EXACTLY as the original \`lastIndexOf\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"searchElement","type":"number"},{"name":"fromIndex?","type":"number"}],
};
export function lastIndexOf(input: { uint8Array: Uint8Array; searchElement: number; fromIndex?: number; prompt?: string }): Promise<number>;
export async function lastIndexOf(input: Record<string, unknown>): Promise<unknown> { return runMethod(__lastIndexOf_18_spec, input); }

/** `Uint8Array.prototype.map` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __map_19_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'map',
  functionId: 'Uint8Array.prototype.map',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.map'),
  receiverKey: "uint8Array",
  paramOrder: ["callbackfn","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.map\`.
## Original signature(s)
  Overload 1: (callbackfn?: (value: number, index: number, array: Uint8Array) => number, thisArg?: any) => Uint8Array
## JSDoc
Calls a defined callback function on each element of an array, and returns an array that
contains the results.

## How to respond
- Behave EXACTLY as the original \`map\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"callbackfn?","type":"(value: number, index: number, array: Uint8Array) => number"},{"name":"thisArg?","type":"any"}],
};
export function map(input: { uint8Array: Uint8Array; callbackfn?: (value: number, index: number, array: Uint8Array) => number; thisArg?: any; prompt?: string }): Promise<Uint8Array>;
export async function map(input: Record<string, unknown>): Promise<unknown> { return runMethod(__map_19_spec, input); }

/** `Uint8Array.of` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __of_20_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'of',
  functionId: 'Uint8Array.of',
  kind: 'static',
  native: resolveNative('Uint8Array.of'),
  receiverKey: "",
  paramOrder: ["items"],
  variadicKey: "items",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.of\`.
## Original signature(s)
  Overload 1: (...items: number[]) => Uint8Array
## JSDoc
Returns a new array from a set of elements.

## How to respond
- Behave EXACTLY as the original \`of\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"items","type":"number[]"}],
};
export function of(input: { items?: (number)[]; prompt?: string }): Promise<Uint8Array>;
export async function of(input: Record<string, unknown>): Promise<unknown> { return runMethod(__of_20_spec, input); }

/** `Uint8Array.prototype.reduce` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __reduce_21_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'reduce',
  functionId: 'Uint8Array.prototype.reduce',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.reduce'),
  receiverKey: "uint8Array",
  paramOrder: ["callbackfn","initialValue"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.reduce\`.
## Original signature(s)
  Overload 1: (callbackfn?: (previousValue: number, currentValue: number, currentIndex: number, array: Uint8Array) => number) => number
  Overload 2: (callbackfn?: (previousValue: number, currentValue: number, currentIndex: number, array: Uint8Array) => number, initialValue?: number) => number
  Overload 3: (callbackfn?: (previousValue: U, currentValue: number, currentIndex: number, array: Uint8Array) => U, initialValue?: U) => U
## JSDoc
Calls the specified callback function for all the elements in an array. The return value of
the callback function is the accumulated result, and is provided as an argument in the next
call to the callback function.

## How to respond
- Behave EXACTLY as the original \`reduce\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"callbackfn?","type":"(previousValue: number, currentValue: number, currentIndex: number, array: Uint8Array) => number"}],
};
export function reduce(input: { uint8Array: Uint8Array; callbackfn?: (previousValue: number, currentValue: number, currentIndex: number, array: Uint8Array) => number; prompt?: string }): Promise<number>;
export function reduce(input: { uint8Array: Uint8Array; callbackfn?: (previousValue: number, currentValue: number, currentIndex: number, array: Uint8Array) => number; initialValue?: number; prompt?: string }): Promise<number>;
export function reduce<U>(input: { uint8Array: Uint8Array; callbackfn?: (previousValue: U, currentValue: number, currentIndex: number, array: Uint8Array) => U; initialValue?: U; prompt?: string }): Promise<U>;
export async function reduce(input: Record<string, unknown>): Promise<unknown> { return runMethod(__reduce_21_spec, input); }

/** `Uint8Array.prototype.reduceRight` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __reduceRight_22_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'reduceRight',
  functionId: 'Uint8Array.prototype.reduceRight',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.reduceRight'),
  receiverKey: "uint8Array",
  paramOrder: ["callbackfn","initialValue"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.reduceRight\`.
## Original signature(s)
  Overload 1: (callbackfn?: (previousValue: number, currentValue: number, currentIndex: number, array: Uint8Array) => number) => number
  Overload 2: (callbackfn?: (previousValue: number, currentValue: number, currentIndex: number, array: Uint8Array) => number, initialValue?: number) => number
  Overload 3: (callbackfn?: (previousValue: U, currentValue: number, currentIndex: number, array: Uint8Array) => U, initialValue?: U) => U
## JSDoc
Calls the specified callback function for all the elements in an array, in descending order.
The return value of the callback function is the accumulated result, and is provided as an
argument in the next call to the callback function.

## How to respond
- Behave EXACTLY as the original \`reduceRight\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"callbackfn?","type":"(previousValue: number, currentValue: number, currentIndex: number, array: Uint8Array) => number"}],
};
export function reduceRight(input: { uint8Array: Uint8Array; callbackfn?: (previousValue: number, currentValue: number, currentIndex: number, array: Uint8Array) => number; prompt?: string }): Promise<number>;
export function reduceRight(input: { uint8Array: Uint8Array; callbackfn?: (previousValue: number, currentValue: number, currentIndex: number, array: Uint8Array) => number; initialValue?: number; prompt?: string }): Promise<number>;
export function reduceRight<U>(input: { uint8Array: Uint8Array; callbackfn?: (previousValue: U, currentValue: number, currentIndex: number, array: Uint8Array) => U; initialValue?: U; prompt?: string }): Promise<U>;
export async function reduceRight(input: Record<string, unknown>): Promise<unknown> { return runMethod(__reduceRight_22_spec, input); }

/** `Uint8Array.prototype.reverse` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __reverse_23_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'reverse',
  functionId: 'Uint8Array.prototype.reverse',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.reverse'),
  receiverKey: "uint8Array",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.reverse\`.
## Original signature(s)
  Overload 1: () => Uint8Array
## JSDoc
Reverses the elements in an Array.

## How to respond
- Behave EXACTLY as the original \`reverse\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function reverse(input: { uint8Array: Uint8Array; prompt?: string }): Promise<Uint8Array>;
export async function reverse(input: Record<string, unknown>): Promise<unknown> { return runMethod(__reverse_23_spec, input); }

/** `Uint8Array.prototype.set` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __set_24_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'set',
  functionId: 'Uint8Array.prototype.set',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.set'),
  receiverKey: "uint8Array",
  paramOrder: ["array","offset"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.set\`.
## Original signature(s)
  Overload 1: (array: ArrayLike<number>, offset?: number) => void
## JSDoc
Sets a value or an array of values.

## How to respond
- Behave EXACTLY as the original \`set\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"array","type":"ArrayLike<number>"},{"name":"offset?","type":"number"}],
};
export function set(input: { uint8Array: Uint8Array; array: ArrayLike<number>; offset?: number; prompt?: string }): Promise<void>;
export async function set(input: Record<string, unknown>): Promise<unknown> { return runMethod(__set_24_spec, input); }

/** `Uint8Array.prototype.setFromBase64` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setFromBase64_25_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'setFromBase64',
  functionId: 'Uint8Array.prototype.setFromBase64',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.setFromBase64'),
  receiverKey: "uint8Array",
  paramOrder: ["string","options"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.setFromBase64\`.
## Original signature(s)
  Overload 1: (string: string, options?: { alphabet?: "base64" | "base64url" | undefined; lastChunkHandling?: "loose" | "strict" | "stop-before-partial" | undefined; }) => { read: number; written: number; }
## JSDoc
Sets the \`Uint8Array\` from a base64-encoded string.

## How to respond
- Behave EXACTLY as the original \`setFromBase64\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"string","type":"string"},{"name":"options?","type":"{ alphabet?: \"base64\" | \"base64url\" | undefined; lastChunkHandling?: \"loose\" | \"strict\" | \"stop-before-partial\" | undefined; }"}],
};
export function setFromBase64(input: { uint8Array: Uint8Array; string: string; options?: { alphabet?: "base64" | "base64url" | undefined; lastChunkHandling?: "loose" | "strict" | "stop-before-partial" | undefined; }; prompt?: string }): Promise<{ read: number; written: number; }>;
export async function setFromBase64(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setFromBase64_25_spec, input); }

/** `Uint8Array.prototype.setFromHex` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setFromHex_26_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'setFromHex',
  functionId: 'Uint8Array.prototype.setFromHex',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.setFromHex'),
  receiverKey: "uint8Array",
  paramOrder: ["string"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.setFromHex\`.
## Original signature(s)
  Overload 1: (string: string) => { read: number; written: number; }
## JSDoc
Sets the \`Uint8Array\` from a base16-encoded string.

## How to respond
- Behave EXACTLY as the original \`setFromHex\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"string","type":"string"}],
};
export function setFromHex(input: { uint8Array: Uint8Array; string: string; prompt?: string }): Promise<{ read: number; written: number; }>;
export async function setFromHex(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setFromHex_26_spec, input); }

/** `Uint8Array.prototype.slice` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __slice_27_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'slice',
  functionId: 'Uint8Array.prototype.slice',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.slice'),
  receiverKey: "uint8Array",
  paramOrder: ["start","end"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.slice\`.
## Original signature(s)
  Overload 1: (start?: number, end?: number) => Uint8Array
## JSDoc
Returns a section of an array.

## How to respond
- Behave EXACTLY as the original \`slice\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"start?","type":"number"},{"name":"end?","type":"number"}],
};
export function slice(input: { uint8Array: Uint8Array; start?: number; end?: number; prompt?: string }): Promise<Uint8Array>;
export async function slice(input: Record<string, unknown>): Promise<unknown> { return runMethod(__slice_27_spec, input); }

/** `Uint8Array.prototype.some` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __some_28_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'some',
  functionId: 'Uint8Array.prototype.some',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.some'),
  receiverKey: "uint8Array",
  paramOrder: ["predicate","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.some\`.
## Original signature(s)
  Overload 1: (predicate?: (value: number, index: number, array: Uint8Array) => unknown, thisArg?: any) => boolean
## JSDoc
Determines whether the specified callback function returns true for any element of an array.

## How to respond
- Behave EXACTLY as the original \`some\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: number, index: number, array: Uint8Array) => unknown"},{"name":"thisArg?","type":"any"}],
};
export function some(input: { uint8Array: Uint8Array; predicate?: (value: number, index: number, array: Uint8Array) => unknown; thisArg?: any; prompt?: string }): Promise<boolean>;
export async function some(input: Record<string, unknown>): Promise<unknown> { return runMethod(__some_28_spec, input); }

/** `Uint8Array.prototype.sort` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __sort_29_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'sort',
  functionId: 'Uint8Array.prototype.sort',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.sort'),
  receiverKey: "uint8Array",
  paramOrder: ["compareFn"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.sort\`.
## Original signature(s)
  Overload 1: (compareFn?: (a: number, b: number) => number) => Uint8Array
## JSDoc
Sorts an array.

## How to respond
- Behave EXACTLY as the original \`sort\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"compareFn?","type":"(a: number, b: number) => number"}],
};
export function sort(input: { uint8Array: Uint8Array; compareFn?: (a: number, b: number) => number; prompt?: string }): Promise<Uint8Array>;
export async function sort(input: Record<string, unknown>): Promise<unknown> { return runMethod(__sort_29_spec, input); }

/** `Uint8Array.prototype.subarray` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __subarray_30_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'subarray',
  functionId: 'Uint8Array.prototype.subarray',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.subarray'),
  receiverKey: "uint8Array",
  paramOrder: ["begin","end"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.subarray\`.
## Original signature(s)
  Overload 1: (begin?: number, end?: number) => Uint8Array
## JSDoc
Gets a new Uint8Array view of the ArrayBuffer store for this array, referencing the elements
at begin, inclusive, up to end, exclusive.

## How to respond
- Behave EXACTLY as the original \`subarray\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"begin?","type":"number"},{"name":"end?","type":"number"}],
};
export function subarray(input: { uint8Array: Uint8Array; begin?: number; end?: number; prompt?: string }): Promise<Uint8Array>;
export async function subarray(input: Record<string, unknown>): Promise<unknown> { return runMethod(__subarray_30_spec, input); }

/** `Uint8Array.prototype.toBase64` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toBase64_31_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'toBase64',
  functionId: 'Uint8Array.prototype.toBase64',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.toBase64'),
  receiverKey: "uint8Array",
  paramOrder: ["options"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.toBase64\`.
## Original signature(s)
  Overload 1: (options?: { alphabet?: "base64" | "base64url" | undefined; omitPadding?: boolean | undefined; }) => string
## JSDoc
Converts the \`Uint8Array\` to a base64-encoded string.

## How to respond
- Behave EXACTLY as the original \`toBase64\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"options?","type":"{ alphabet?: \"base64\" | \"base64url\" | undefined; omitPadding?: boolean | undefined; }"}],
};
export function toBase64(input: { uint8Array: Uint8Array; options?: { alphabet?: "base64" | "base64url" | undefined; omitPadding?: boolean | undefined; }; prompt?: string }): Promise<string>;
export async function toBase64(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toBase64_31_spec, input); }

/** `Uint8Array.prototype.toHex` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toHex_32_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'toHex',
  functionId: 'Uint8Array.prototype.toHex',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.toHex'),
  receiverKey: "uint8Array",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.toHex\`.
## Original signature(s)
  Overload 1: () => string
## JSDoc
Converts the \`Uint8Array\` to a base16-encoded string.

## How to respond
- Behave EXACTLY as the original \`toHex\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function toHex(input: { uint8Array: Uint8Array; prompt?: string }): Promise<string>;
export async function toHex(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toHex_32_spec, input); }

/** `Uint8Array.prototype.toReversed` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toReversed_33_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'toReversed',
  functionId: 'Uint8Array.prototype.toReversed',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.toReversed'),
  receiverKey: "uint8Array",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.toReversed\`.
## Original signature(s)
  Overload 1: () => Uint8Array
## JSDoc
Copies the array and returns the copy with the elements in reverse order.

## How to respond
- Behave EXACTLY as the original \`toReversed\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function toReversed(input: { uint8Array: Uint8Array; prompt?: string }): Promise<Uint8Array>;
export async function toReversed(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toReversed_33_spec, input); }

/** `Uint8Array.prototype.toSorted` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toSorted_34_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'toSorted',
  functionId: 'Uint8Array.prototype.toSorted',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.toSorted'),
  receiverKey: "uint8Array",
  paramOrder: ["compareFn"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.toSorted\`.
## Original signature(s)
  Overload 1: (compareFn?: (a: number, b: number) => number) => Uint8Array
## JSDoc
Copies and sorts the array.

## How to respond
- Behave EXACTLY as the original \`toSorted\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"compareFn?","type":"(a: number, b: number) => number"}],
};
export function toSorted(input: { uint8Array: Uint8Array; compareFn?: (a: number, b: number) => number; prompt?: string }): Promise<Uint8Array>;
export async function toSorted(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toSorted_34_spec, input); }

/** `Uint8Array.prototype.values` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __values_35_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'values',
  functionId: 'Uint8Array.prototype.values',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.values'),
  receiverKey: "uint8Array",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.values\`.
## Original signature(s)
  Overload 1: () => ArrayIterator<number>
## JSDoc
Returns an list of values in the array

## How to respond
- Behave EXACTLY as the original \`values\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function values(input: { uint8Array: Uint8Array; prompt?: string }): Promise<ArrayIterator<number>>;
export async function values(input: Record<string, unknown>): Promise<unknown> { return runMethod(__values_35_spec, input); }

/** `Uint8Array.prototype.with` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __with_36_spec: GeneratedMethodSpec = {
  group: 'uint8Array',
  methodName: 'with',
  functionId: 'Uint8Array.prototype.with',
  kind: 'instance',
  native: resolveNative('Uint8Array.prototype.with'),
  receiverKey: "uint8Array",
  paramOrder: ["index","value"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint8Array.prototype.with\`.
## Original signature(s)
  Overload 1: (index: number, value: number) => Uint8Array
## JSDoc
Copies the array and inserts the given number at the provided index.

## How to respond
- Behave EXACTLY as the original \`with\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"index","type":"number"},{"name":"value","type":"number"}],
};
function _with(input: { uint8Array: Uint8Array; index: number; value: number; prompt?: string }): Promise<Uint8Array>;
async function _with(input: Record<string, unknown>): Promise<unknown> { return runMethod(__with_36_spec, input); }

export { _with as with };
