// Auto-generated by scripts/generate-wrappers.ts. Do not edit.
import { runMethod, resolveNative, type GeneratedMethodSpec } from '../../runtime';

/** `Iterator.prototype.drop` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __drop_0_spec: GeneratedMethodSpec = {
  group: 'iterator',
  methodName: 'drop',
  functionId: 'Iterator.prototype.drop',
  kind: 'instance',
  native: resolveNative('Iterator.prototype.drop'),
  receiverKey: "iterator",
  paramOrder: ["count"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Iterator.prototype.drop\`.
## Original signature(s)
  Overload 1: (count: number) => IteratorObject<T, undefined, unknown>
## JSDoc
Creates an iterator whose values are the values from this iterator after skipping the provided count.

## How to respond
- Behave EXACTLY as the original \`drop\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"count","type":"number"}],
};
export function drop<TNext, TReturn, T>(input: { iterator: IteratorObject<T, TReturn, TNext>; count: number; prompt?: string }): Promise<IteratorObject<T, undefined, unknown>>;
export async function drop(input: Record<string, unknown>): Promise<unknown> { return runMethod(__drop_0_spec, input); }

/** `Iterator.prototype.every` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __every_1_spec: GeneratedMethodSpec = {
  group: 'iterator',
  methodName: 'every',
  functionId: 'Iterator.prototype.every',
  kind: 'instance',
  native: resolveNative('Iterator.prototype.every'),
  receiverKey: "iterator",
  paramOrder: ["predicate"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Iterator.prototype.every\`.
## Original signature(s)
  Overload 1: (predicate?: (value: T, index: number) => unknown) => boolean
## JSDoc
Determines whether all the members of this iterator satisfy the specified test.

## How to respond
- Behave EXACTLY as the original \`every\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: T, index: number) => unknown"}],
};
export function every<TNext, TReturn, T>(input: { iterator: IteratorObject<T, TReturn, TNext>; predicate?: (value: T, index: number) => unknown; prompt?: string }): Promise<boolean>;
export async function every(input: Record<string, unknown>): Promise<unknown> { return runMethod(__every_1_spec, input); }

/** `Iterator.prototype.filter` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __filter_2_spec: GeneratedMethodSpec = {
  group: 'iterator',
  methodName: 'filter',
  functionId: 'Iterator.prototype.filter',
  kind: 'instance',
  native: resolveNative('Iterator.prototype.filter'),
  receiverKey: "iterator",
  paramOrder: ["predicate"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Iterator.prototype.filter\`.
## Original signature(s)
  Overload 1: (predicate?: (value: T, index: number) => boolean) => IteratorObject<S, undefined, unknown>
  Overload 2: (predicate?: (value: T, index: number) => unknown) => IteratorObject<T, undefined, unknown>
## JSDoc
Creates an iterator whose values are those from this iterator for which the provided predicate returns true.

## How to respond
- Behave EXACTLY as the original \`filter\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: T, index: number) => boolean"}],
};
export function filter<TNext, TReturn, T, S extends T>(input: { iterator: IteratorObject<T, TReturn, TNext>; predicate?: (value: T, index: number) => boolean; prompt?: string }): Promise<IteratorObject<S, undefined, unknown>>;
export function filter<TNext, TReturn, T>(input: { iterator: IteratorObject<T, TReturn, TNext>; predicate?: (value: T, index: number) => unknown; prompt?: string }): Promise<IteratorObject<T, undefined, unknown>>;
export async function filter(input: Record<string, unknown>): Promise<unknown> { return runMethod(__filter_2_spec, input); }

/** `Iterator.prototype.find` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __find_3_spec: GeneratedMethodSpec = {
  group: 'iterator',
  methodName: 'find',
  functionId: 'Iterator.prototype.find',
  kind: 'instance',
  native: resolveNative('Iterator.prototype.find'),
  receiverKey: "iterator",
  paramOrder: ["predicate"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Iterator.prototype.find\`.
## Original signature(s)
  Overload 1: (predicate?: (value: T, index: number) => boolean) => S
  Overload 2: (predicate?: (value: T, index: number) => unknown) => T
## JSDoc
Returns the value of the first element in this iterator where predicate is true, and undefined
otherwise.

## How to respond
- Behave EXACTLY as the original \`find\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: T, index: number) => boolean"}],
};
export function find<TNext, TReturn, T, S extends T>(input: { iterator: IteratorObject<T, TReturn, TNext>; predicate?: (value: T, index: number) => boolean; prompt?: string }): Promise<S>;
export function find<TNext, TReturn, T>(input: { iterator: IteratorObject<T, TReturn, TNext>; predicate?: (value: T, index: number) => unknown; prompt?: string }): Promise<T>;
export async function find(input: Record<string, unknown>): Promise<unknown> { return runMethod(__find_3_spec, input); }

/** `Iterator.prototype.flatMap` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __flatMap_4_spec: GeneratedMethodSpec = {
  group: 'iterator',
  methodName: 'flatMap',
  functionId: 'Iterator.prototype.flatMap',
  kind: 'instance',
  native: resolveNative('Iterator.prototype.flatMap'),
  receiverKey: "iterator",
  paramOrder: ["callback"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Iterator.prototype.flatMap\`.
## Original signature(s)
  Overload 1: (callback?: (value: T, index: number) => Iterator<U, unknown, undefined> | Iterable<U, unknown, undefined>) => IteratorObject<U, undefined, unknown>
## JSDoc
Creates an iterator whose values are the result of applying the callback to the values from this iterator and then flattening the resulting iterators or iterables.

## How to respond
- Behave EXACTLY as the original \`flatMap\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"callback?","type":"(value: T, index: number) => Iterator<U, unknown, undefined> | Iterable<U, unknown, undefined>"}],
};
export function flatMap<TNext, TReturn, T, U>(input: { iterator: IteratorObject<T, TReturn, TNext>; callback?: (value: T, index: number) => Iterator<U, unknown, undefined> | Iterable<U, unknown, undefined>; prompt?: string }): Promise<IteratorObject<U, undefined, unknown>>;
export async function flatMap(input: Record<string, unknown>): Promise<unknown> { return runMethod(__flatMap_4_spec, input); }

/** `Iterator.prototype.forEach` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __forEach_5_spec: GeneratedMethodSpec = {
  group: 'iterator',
  methodName: 'forEach',
  functionId: 'Iterator.prototype.forEach',
  kind: 'instance',
  native: resolveNative('Iterator.prototype.forEach'),
  receiverKey: "iterator",
  paramOrder: ["callbackfn"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Iterator.prototype.forEach\`.
## Original signature(s)
  Overload 1: (callbackfn?: (value: T, index: number) => void) => void
## JSDoc
Performs the specified action for each element in the iterator.

## How to respond
- Behave EXACTLY as the original \`forEach\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"callbackfn?","type":"(value: T, index: number) => void"}],
};
export function forEach<TNext, TReturn, T>(input: { iterator: IteratorObject<T, TReturn, TNext>; callbackfn?: (value: T, index: number) => void; prompt?: string }): Promise<void>;
export async function forEach(input: Record<string, unknown>): Promise<unknown> { return runMethod(__forEach_5_spec, input); }

/** `Iterator.from` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __from_6_spec: GeneratedMethodSpec = {
  group: 'iterator',
  methodName: 'from',
  functionId: 'Iterator.from',
  kind: 'static',
  native: resolveNative('Iterator.from'),
  receiverKey: "",
  paramOrder: ["value"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Iterator.from\`.
## Original signature(s)
  Overload 1: (value: Iterator<T, unknown, undefined> | Iterable<T, unknown, undefined>) => IteratorObject<T, undefined, unknown>
## JSDoc
Creates a native iterator from an iterator or iterable object.
Returns its input if the input already inherits from the built-in Iterator class.

## How to respond
- Behave EXACTLY as the original \`from\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"value","type":"Iterator<T, unknown, undefined> | Iterable<T, unknown, undefined>"}],
};
export function from<T>(input: { value: Iterator<T, unknown, undefined> | Iterable<T, unknown, undefined>; prompt?: string }): Promise<IteratorObject<T, undefined, unknown>>;
export async function from(input: Record<string, unknown>): Promise<unknown> { return runMethod(__from_6_spec, input); }

/** `Iterator.prototype.map` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __map_7_spec: GeneratedMethodSpec = {
  group: 'iterator',
  methodName: 'map',
  functionId: 'Iterator.prototype.map',
  kind: 'instance',
  native: resolveNative('Iterator.prototype.map'),
  receiverKey: "iterator",
  paramOrder: ["callbackfn"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Iterator.prototype.map\`.
## Original signature(s)
  Overload 1: (callbackfn?: (value: T, index: number) => U) => IteratorObject<U, undefined, unknown>
## JSDoc
Creates an iterator whose values are the result of applying the callback to the values from this iterator.

## How to respond
- Behave EXACTLY as the original \`map\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"callbackfn?","type":"(value: T, index: number) => U"}],
};
export function map<TNext, TReturn, T, U>(input: { iterator: IteratorObject<T, TReturn, TNext>; callbackfn?: (value: T, index: number) => U; prompt?: string }): Promise<IteratorObject<U, undefined, unknown>>;
export async function map(input: Record<string, unknown>): Promise<unknown> { return runMethod(__map_7_spec, input); }

/** `Iterator.prototype.next` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __next_8_spec: GeneratedMethodSpec = {
  group: 'iterator',
  methodName: 'next',
  functionId: 'Iterator.prototype.next',
  kind: 'instance',
  native: resolveNative('Iterator.prototype.next'),
  receiverKey: "iterator",
  paramOrder: ["__0"],
  variadicKey: "__0",
  systemPrompt: `You are simulating the JavaScript built-in \`Iterator.prototype.next\`.
## Original signature(s)
  Overload 1: (...__0: [] | [TNext]) => IteratorResult<T, TReturn>
## How to respond
- Behave EXACTLY as the original \`next\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"__0","type":"[] | [TNext]"}],
};
export function next<TNext, TReturn, T>(input: { iterator: IteratorObject<T, TReturn, TNext>; __0?: ([] | [TNext])[]; prompt?: string }): Promise<IteratorResult<T, TReturn>>;
export async function next(input: Record<string, unknown>): Promise<unknown> { return runMethod(__next_8_spec, input); }

/** `Iterator.prototype.reduce` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __reduce_9_spec: GeneratedMethodSpec = {
  group: 'iterator',
  methodName: 'reduce',
  functionId: 'Iterator.prototype.reduce',
  kind: 'instance',
  native: resolveNative('Iterator.prototype.reduce'),
  receiverKey: "iterator",
  paramOrder: ["callbackfn","initialValue"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Iterator.prototype.reduce\`.
## Original signature(s)
  Overload 1: (callbackfn?: (previousValue: T, currentValue: T, currentIndex: number) => T) => T
  Overload 2: (callbackfn?: (previousValue: T, currentValue: T, currentIndex: number) => T, initialValue?: T) => T
  Overload 3: (callbackfn?: (previousValue: U, currentValue: T, currentIndex: number) => U, initialValue?: U) => U
## JSDoc
Calls the specified callback function for all the elements in this iterator. The return value of the callback function is the accumulated result, and is provided as an argument in the next call to the callback function.

## How to respond
- Behave EXACTLY as the original \`reduce\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"callbackfn?","type":"(previousValue: T, currentValue: T, currentIndex: number) => T"}],
};
export function reduce<TNext, TReturn, T>(input: { iterator: IteratorObject<T, TReturn, TNext>; callbackfn?: (previousValue: T, currentValue: T, currentIndex: number) => T; prompt?: string }): Promise<T>;
export function reduce<TNext, TReturn, T>(input: { iterator: IteratorObject<T, TReturn, TNext>; callbackfn?: (previousValue: T, currentValue: T, currentIndex: number) => T; initialValue?: T; prompt?: string }): Promise<T>;
export function reduce<TNext, TReturn, T, U>(input: { iterator: IteratorObject<T, TReturn, TNext>; callbackfn?: (previousValue: U, currentValue: T, currentIndex: number) => U; initialValue?: U; prompt?: string }): Promise<U>;
export async function reduce(input: Record<string, unknown>): Promise<unknown> { return runMethod(__reduce_9_spec, input); }

/** `Iterator.prototype.return` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __return_10_spec: GeneratedMethodSpec = {
  group: 'iterator',
  methodName: 'return',
  functionId: 'Iterator.prototype.return',
  kind: 'instance',
  native: resolveNative('Iterator.prototype.return'),
  receiverKey: "iterator",
  paramOrder: ["value"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Iterator.prototype.return\`.
## Original signature(s)
  Overload 1: (value?: TReturn) => IteratorResult<T, TReturn>
## How to respond
- Behave EXACTLY as the original \`return\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"value?","type":"TReturn"}],
};
function _return<TNext, TReturn, T>(input: { iterator: IteratorObject<T, TReturn, TNext>; value?: TReturn; prompt?: string }): Promise<IteratorResult<T, TReturn>>;
async function _return(input: Record<string, unknown>): Promise<unknown> { return runMethod(__return_10_spec, input); }

/** `Iterator.prototype.some` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __some_11_spec: GeneratedMethodSpec = {
  group: 'iterator',
  methodName: 'some',
  functionId: 'Iterator.prototype.some',
  kind: 'instance',
  native: resolveNative('Iterator.prototype.some'),
  receiverKey: "iterator",
  paramOrder: ["predicate"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Iterator.prototype.some\`.
## Original signature(s)
  Overload 1: (predicate?: (value: T, index: number) => unknown) => boolean
## JSDoc
Determines whether the specified callback function returns true for any element of this iterator.

## How to respond
- Behave EXACTLY as the original \`some\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: T, index: number) => unknown"}],
};
export function some<TNext, TReturn, T>(input: { iterator: IteratorObject<T, TReturn, TNext>; predicate?: (value: T, index: number) => unknown; prompt?: string }): Promise<boolean>;
export async function some(input: Record<string, unknown>): Promise<unknown> { return runMethod(__some_11_spec, input); }

/** `Iterator.prototype.take` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __take_12_spec: GeneratedMethodSpec = {
  group: 'iterator',
  methodName: 'take',
  functionId: 'Iterator.prototype.take',
  kind: 'instance',
  native: resolveNative('Iterator.prototype.take'),
  receiverKey: "iterator",
  paramOrder: ["limit"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Iterator.prototype.take\`.
## Original signature(s)
  Overload 1: (limit: number) => IteratorObject<T, undefined, unknown>
## JSDoc
Creates an iterator whose values are the values from this iterator, stopping once the provided limit is reached.

## How to respond
- Behave EXACTLY as the original \`take\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"limit","type":"number"}],
};
export function take<TNext, TReturn, T>(input: { iterator: IteratorObject<T, TReturn, TNext>; limit: number; prompt?: string }): Promise<IteratorObject<T, undefined, unknown>>;
export async function take(input: Record<string, unknown>): Promise<unknown> { return runMethod(__take_12_spec, input); }

/** `Iterator.prototype.throw` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __throw_13_spec: GeneratedMethodSpec = {
  group: 'iterator',
  methodName: 'throw',
  functionId: 'Iterator.prototype.throw',
  kind: 'instance',
  native: resolveNative('Iterator.prototype.throw'),
  receiverKey: "iterator",
  paramOrder: ["e"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Iterator.prototype.throw\`.
## Original signature(s)
  Overload 1: (e?: any) => IteratorResult<T, TReturn>
## How to respond
- Behave EXACTLY as the original \`throw\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"e?","type":"any"}],
};
function _throw<TNext, TReturn, T>(input: { iterator: IteratorObject<T, TReturn, TNext>; e?: any; prompt?: string }): Promise<IteratorResult<T, TReturn>>;
async function _throw(input: Record<string, unknown>): Promise<unknown> { return runMethod(__throw_13_spec, input); }

/** `Iterator.prototype.toArray` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toArray_14_spec: GeneratedMethodSpec = {
  group: 'iterator',
  methodName: 'toArray',
  functionId: 'Iterator.prototype.toArray',
  kind: 'instance',
  native: resolveNative('Iterator.prototype.toArray'),
  receiverKey: "iterator",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Iterator.prototype.toArray\`.
## Original signature(s)
  Overload 1: () => T[]
## JSDoc
Creates a new array from the values yielded by this iterator.

## How to respond
- Behave EXACTLY as the original \`toArray\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function toArray<TNext, TReturn, T>(input: { iterator: IteratorObject<T, TReturn, TNext>; prompt?: string }): Promise<T[]>;
export async function toArray(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toArray_14_spec, input); }

export { _return as return };
export { _throw as throw };
