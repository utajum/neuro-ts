// Auto-generated by scripts/generate-wrappers.ts. Do not edit.
import { runMethod, resolveNative, type GeneratedMethodSpec } from '../../runtime';

/** `Symbol.for` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __for_0_spec: GeneratedMethodSpec = {
  group: 'symbol',
  methodName: 'for',
  functionId: 'Symbol.for',
  kind: 'static',
  native: resolveNative('Symbol.for'),
  receiverKey: "",
  paramOrder: ["key"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Symbol.for\`.
## Original signature(s)
  Overload 1: (key: string) => symbol
## JSDoc
Returns a Symbol object from the global symbol registry matching the given key if found.
Otherwise, returns a new symbol with this key.

## How to respond
- Behave EXACTLY as the original \`for\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"key","type":"string"}],
};
function _for(input: { key: string; prompt?: string }): Promise<symbol>;
async function _for(input: Record<string, unknown>): Promise<unknown> { return runMethod(__for_0_spec, input); }

/** `Symbol.keyFor` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __keyFor_1_spec: GeneratedMethodSpec = {
  group: 'symbol',
  methodName: 'keyFor',
  functionId: 'Symbol.keyFor',
  kind: 'static',
  native: resolveNative('Symbol.keyFor'),
  receiverKey: "",
  paramOrder: ["sym"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Symbol.keyFor\`.
## Original signature(s)
  Overload 1: (sym: symbol) => string
## JSDoc
Returns a key from the global symbol registry matching the given Symbol if found.
Otherwise, returns a undefined.

## How to respond
- Behave EXACTLY as the original \`keyFor\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"sym","type":"symbol"}],
};
export function keyFor(input: { sym: symbol; prompt?: string }): Promise<string>;
export async function keyFor(input: Record<string, unknown>): Promise<unknown> { return runMethod(__keyFor_1_spec, input); }

export { _for as for };
