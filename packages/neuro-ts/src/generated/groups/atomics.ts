// Auto-generated by scripts/generate-wrappers.ts. Do not edit.
import { runMethod, resolveNative, type GeneratedMethodSpec } from '../../runtime';

/** `Atomics.add` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __add_0_spec: GeneratedMethodSpec = {
  group: 'atomics',
  methodName: 'add',
  functionId: 'Atomics.add',
  kind: 'static',
  native: resolveNative('Atomics.add'),
  receiverKey: "",
  paramOrder: ["typedArray","index","value"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Atomics.add\`.
## Original signature(s)
  Overload 1: (typedArray: Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array, index: number, value: number) => number
  Overload 2: (typedArray: BigInt64Array | BigUint64Array, index: number, value: bigint) => bigint
## JSDoc
Adds a value to the value at the given position in the array, returning the original value.
Until this atomic operation completes, any other read or write operation against the array
will block.

## How to respond
- Behave EXACTLY as the original \`add\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"typedArray","type":"Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array"},{"name":"index","type":"number"},{"name":"value","type":"number"}],
};
export function add(input: { typedArray: Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array; index: number; value: number; prompt?: string }): Promise<number>;
export function add(input: { typedArray: BigInt64Array | BigUint64Array; index: number; value: bigint; prompt?: string }): Promise<bigint>;
export async function add(input: Record<string, unknown>): Promise<unknown> { return runMethod(__add_0_spec, input); }

/** `Atomics.and` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __and_1_spec: GeneratedMethodSpec = {
  group: 'atomics',
  methodName: 'and',
  functionId: 'Atomics.and',
  kind: 'static',
  native: resolveNative('Atomics.and'),
  receiverKey: "",
  paramOrder: ["typedArray","index","value"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Atomics.and\`.
## Original signature(s)
  Overload 1: (typedArray: Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array, index: number, value: number) => number
  Overload 2: (typedArray: BigInt64Array | BigUint64Array, index: number, value: bigint) => bigint
## JSDoc
Stores the bitwise AND of a value with the value at the given position in the array,
returning the original value. Until this atomic operation completes, any other read or
write operation against the array will block.

## How to respond
- Behave EXACTLY as the original \`and\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"typedArray","type":"Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array"},{"name":"index","type":"number"},{"name":"value","type":"number"}],
};
export function and(input: { typedArray: Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array; index: number; value: number; prompt?: string }): Promise<number>;
export function and(input: { typedArray: BigInt64Array | BigUint64Array; index: number; value: bigint; prompt?: string }): Promise<bigint>;
export async function and(input: Record<string, unknown>): Promise<unknown> { return runMethod(__and_1_spec, input); }

/** `Atomics.compareExchange` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __compareExchange_2_spec: GeneratedMethodSpec = {
  group: 'atomics',
  methodName: 'compareExchange',
  functionId: 'Atomics.compareExchange',
  kind: 'static',
  native: resolveNative('Atomics.compareExchange'),
  receiverKey: "",
  paramOrder: ["typedArray","index","expectedValue","replacementValue"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Atomics.compareExchange\`.
## Original signature(s)
  Overload 1: (typedArray: Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array, index: number, expectedValue: number, replacementValue: number) => number
  Overload 2: (typedArray: BigInt64Array | BigUint64Array, index: number, expectedValue: bigint, replacementValue: bigint) => bigint
## JSDoc
Replaces the value at the given position in the array if the original value equals the given
expected value, returning the original value. Until this atomic operation completes, any
other read or write operation against the array will block.

## How to respond
- Behave EXACTLY as the original \`compareExchange\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"typedArray","type":"Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array"},{"name":"index","type":"number"},{"name":"expectedValue","type":"number"},{"name":"replacementValue","type":"number"}],
};
export function compareExchange(input: { typedArray: Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array; index: number; expectedValue: number; replacementValue: number; prompt?: string }): Promise<number>;
export function compareExchange(input: { typedArray: BigInt64Array | BigUint64Array; index: number; expectedValue: bigint; replacementValue: bigint; prompt?: string }): Promise<bigint>;
export async function compareExchange(input: Record<string, unknown>): Promise<unknown> { return runMethod(__compareExchange_2_spec, input); }

/** `Atomics.exchange` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __exchange_3_spec: GeneratedMethodSpec = {
  group: 'atomics',
  methodName: 'exchange',
  functionId: 'Atomics.exchange',
  kind: 'static',
  native: resolveNative('Atomics.exchange'),
  receiverKey: "",
  paramOrder: ["typedArray","index","value"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Atomics.exchange\`.
## Original signature(s)
  Overload 1: (typedArray: Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array, index: number, value: number) => number
  Overload 2: (typedArray: BigInt64Array | BigUint64Array, index: number, value: bigint) => bigint
## JSDoc
Replaces the value at the given position in the array, returning the original value. Until
this atomic operation completes, any other read or write operation against the array will
block.

## How to respond
- Behave EXACTLY as the original \`exchange\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"typedArray","type":"Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array"},{"name":"index","type":"number"},{"name":"value","type":"number"}],
};
export function exchange(input: { typedArray: Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array; index: number; value: number; prompt?: string }): Promise<number>;
export function exchange(input: { typedArray: BigInt64Array | BigUint64Array; index: number; value: bigint; prompt?: string }): Promise<bigint>;
export async function exchange(input: Record<string, unknown>): Promise<unknown> { return runMethod(__exchange_3_spec, input); }

/** `Atomics.isLockFree` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __isLockFree_4_spec: GeneratedMethodSpec = {
  group: 'atomics',
  methodName: 'isLockFree',
  functionId: 'Atomics.isLockFree',
  kind: 'static',
  native: resolveNative('Atomics.isLockFree'),
  receiverKey: "",
  paramOrder: ["size"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Atomics.isLockFree\`.
## Original signature(s)
  Overload 1: (size: number) => boolean
## JSDoc
Returns a value indicating whether high-performance algorithms can use atomic operations
(\`true\`) or must use locks (\`false\`) for the given number of bytes-per-element of a typed
array.

## How to respond
- Behave EXACTLY as the original \`isLockFree\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"size","type":"number"}],
};
export function isLockFree(input: { size: number; prompt?: string }): Promise<boolean>;
export async function isLockFree(input: Record<string, unknown>): Promise<unknown> { return runMethod(__isLockFree_4_spec, input); }

/** `Atomics.load` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __load_5_spec: GeneratedMethodSpec = {
  group: 'atomics',
  methodName: 'load',
  functionId: 'Atomics.load',
  kind: 'static',
  native: resolveNative('Atomics.load'),
  receiverKey: "",
  paramOrder: ["typedArray","index"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Atomics.load\`.
## Original signature(s)
  Overload 1: (typedArray: Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array, index: number) => number
  Overload 2: (typedArray: BigInt64Array | BigUint64Array, index: number) => bigint
## JSDoc
Returns the value at the given position in the array. Until this atomic operation completes,
any other read or write operation against the array will block.

## How to respond
- Behave EXACTLY as the original \`load\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"typedArray","type":"Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array"},{"name":"index","type":"number"}],
};
export function load(input: { typedArray: Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array; index: number; prompt?: string }): Promise<number>;
export function load(input: { typedArray: BigInt64Array | BigUint64Array; index: number; prompt?: string }): Promise<bigint>;
export async function load(input: Record<string, unknown>): Promise<unknown> { return runMethod(__load_5_spec, input); }

/** `Atomics.notify` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __notify_6_spec: GeneratedMethodSpec = {
  group: 'atomics',
  methodName: 'notify',
  functionId: 'Atomics.notify',
  kind: 'static',
  native: resolveNative('Atomics.notify'),
  receiverKey: "",
  paramOrder: ["typedArray","index","count"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Atomics.notify\`.
## Original signature(s)
  Overload 1: (typedArray: Int32Array, index: number, count?: number) => number
  Overload 2: (typedArray: BigInt64Array, index: number, count?: number) => number
## JSDoc
Wakes up sleeping agents that are waiting on the given index of the array, returning the
number of agents that were awoken.

## How to respond
- Behave EXACTLY as the original \`notify\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"typedArray","type":"Int32Array"},{"name":"index","type":"number"},{"name":"count?","type":"number"}],
};
export function notify(input: { typedArray: Int32Array; index: number; count?: number; prompt?: string }): Promise<number>;
export function notify(input: { typedArray: BigInt64Array; index: number; count?: number; prompt?: string }): Promise<number>;
export async function notify(input: Record<string, unknown>): Promise<unknown> { return runMethod(__notify_6_spec, input); }

/** `Atomics.or` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __or_7_spec: GeneratedMethodSpec = {
  group: 'atomics',
  methodName: 'or',
  functionId: 'Atomics.or',
  kind: 'static',
  native: resolveNative('Atomics.or'),
  receiverKey: "",
  paramOrder: ["typedArray","index","value"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Atomics.or\`.
## Original signature(s)
  Overload 1: (typedArray: Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array, index: number, value: number) => number
  Overload 2: (typedArray: BigInt64Array | BigUint64Array, index: number, value: bigint) => bigint
## JSDoc
Stores the bitwise OR of a value with the value at the given position in the array,
returning the original value. Until this atomic operation completes, any other read or write
operation against the array will block.

## How to respond
- Behave EXACTLY as the original \`or\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"typedArray","type":"Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array"},{"name":"index","type":"number"},{"name":"value","type":"number"}],
};
export function or(input: { typedArray: Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array; index: number; value: number; prompt?: string }): Promise<number>;
export function or(input: { typedArray: BigInt64Array | BigUint64Array; index: number; value: bigint; prompt?: string }): Promise<bigint>;
export async function or(input: Record<string, unknown>): Promise<unknown> { return runMethod(__or_7_spec, input); }

/** `Atomics.pause` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __pause_8_spec: GeneratedMethodSpec = {
  group: 'atomics',
  methodName: 'pause',
  functionId: 'Atomics.pause',
  kind: 'static',
  native: resolveNative('Atomics.pause'),
  receiverKey: "",
  paramOrder: ["n"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Atomics.pause\`.
## Original signature(s)
  Overload 1: (n?: number) => void
## JSDoc
Performs a finite-time microwait by signaling to the operating system or
CPU that the current executing code is in a spin-wait loop.

## How to respond
- Behave EXACTLY as the original \`pause\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"n?","type":"number"}],
};
export function pause(input: { n?: number; prompt?: string }): Promise<void>;
export async function pause(input: Record<string, unknown>): Promise<unknown> { return runMethod(__pause_8_spec, input); }

/** `Atomics.store` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __store_9_spec: GeneratedMethodSpec = {
  group: 'atomics',
  methodName: 'store',
  functionId: 'Atomics.store',
  kind: 'static',
  native: resolveNative('Atomics.store'),
  receiverKey: "",
  paramOrder: ["typedArray","index","value"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Atomics.store\`.
## Original signature(s)
  Overload 1: (typedArray: Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array, index: number, value: number) => number
  Overload 2: (typedArray: BigInt64Array | BigUint64Array, index: number, value: bigint) => bigint
## JSDoc
Stores a value at the given position in the array, returning the new value. Until this
atomic operation completes, any other read or write operation against the array will block.

## How to respond
- Behave EXACTLY as the original \`store\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"typedArray","type":"Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array"},{"name":"index","type":"number"},{"name":"value","type":"number"}],
};
export function store(input: { typedArray: Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array; index: number; value: number; prompt?: string }): Promise<number>;
export function store(input: { typedArray: BigInt64Array | BigUint64Array; index: number; value: bigint; prompt?: string }): Promise<bigint>;
export async function store(input: Record<string, unknown>): Promise<unknown> { return runMethod(__store_9_spec, input); }

/** `Atomics.sub` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __sub_10_spec: GeneratedMethodSpec = {
  group: 'atomics',
  methodName: 'sub',
  functionId: 'Atomics.sub',
  kind: 'static',
  native: resolveNative('Atomics.sub'),
  receiverKey: "",
  paramOrder: ["typedArray","index","value"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Atomics.sub\`.
## Original signature(s)
  Overload 1: (typedArray: Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array, index: number, value: number) => number
  Overload 2: (typedArray: BigInt64Array | BigUint64Array, index: number, value: bigint) => bigint
## JSDoc
Subtracts a value from the value at the given position in the array, returning the original
value. Until this atomic operation completes, any other read or write operation against the
array will block.

## How to respond
- Behave EXACTLY as the original \`sub\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"typedArray","type":"Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array"},{"name":"index","type":"number"},{"name":"value","type":"number"}],
};
export function sub(input: { typedArray: Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array; index: number; value: number; prompt?: string }): Promise<number>;
export function sub(input: { typedArray: BigInt64Array | BigUint64Array; index: number; value: bigint; prompt?: string }): Promise<bigint>;
export async function sub(input: Record<string, unknown>): Promise<unknown> { return runMethod(__sub_10_spec, input); }

/** `Atomics.wait` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __wait_11_spec: GeneratedMethodSpec = {
  group: 'atomics',
  methodName: 'wait',
  functionId: 'Atomics.wait',
  kind: 'static',
  native: resolveNative('Atomics.wait'),
  receiverKey: "",
  paramOrder: ["typedArray","index","value","timeout"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Atomics.wait\`.
## Original signature(s)
  Overload 1: (typedArray: Int32Array, index: number, value: number, timeout?: number) => "ok" | "not-equal" | "timed-out"
  Overload 2: (typedArray: BigInt64Array, index: number, value: bigint, timeout?: number) => "ok" | "not-equal" | "timed-out"
## JSDoc
If the value at the given position in the array is equal to the provided value, the current
agent is put to sleep causing execution to suspend until the timeout expires (returning
\`"timed-out"\`) or until the agent is awoken (returning \`"ok"\`); otherwise, returns
\`"not-equal"\`.

## How to respond
- Behave EXACTLY as the original \`wait\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"typedArray","type":"Int32Array"},{"name":"index","type":"number"},{"name":"value","type":"number"},{"name":"timeout?","type":"number"}],
};
export function wait(input: { typedArray: Int32Array; index: number; value: number; timeout?: number; prompt?: string }): Promise<"ok" | "not-equal" | "timed-out">;
export function wait(input: { typedArray: BigInt64Array; index: number; value: bigint; timeout?: number; prompt?: string }): Promise<"ok" | "not-equal" | "timed-out">;
export async function wait(input: Record<string, unknown>): Promise<unknown> { return runMethod(__wait_11_spec, input); }

/** `Atomics.waitAsync` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __waitAsync_12_spec: GeneratedMethodSpec = {
  group: 'atomics',
  methodName: 'waitAsync',
  functionId: 'Atomics.waitAsync',
  kind: 'static',
  native: resolveNative('Atomics.waitAsync'),
  receiverKey: "",
  paramOrder: ["typedArray","index","value","timeout"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Atomics.waitAsync\`.
## Original signature(s)
  Overload 1: (typedArray: Int32Array, index: number, value: number, timeout?: number) => { async: false; value: "not-equal" | "timed-out"; } | { async: true; value: Promise<"ok" | "timed-out">; }
  Overload 2: (typedArray: BigInt64Array, index: number, value: bigint, timeout?: number) => { async: false; value: "not-equal" | "timed-out"; } | { async: true; value: Promise<"ok" | "timed-out">; }
## JSDoc
A non-blocking, asynchronous version of wait which is usable on the main thread.
Waits asynchronously on a shared memory location and returns a Promise

## How to respond
- Behave EXACTLY as the original \`waitAsync\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"typedArray","type":"Int32Array"},{"name":"index","type":"number"},{"name":"value","type":"number"},{"name":"timeout?","type":"number"}],
};
export function waitAsync(input: { typedArray: Int32Array; index: number; value: number; timeout?: number; prompt?: string }): Promise<{ async: false; value: "not-equal" | "timed-out"; } | { async: true; value: Promise<"ok" | "timed-out">; }>;
export function waitAsync(input: { typedArray: BigInt64Array; index: number; value: bigint; timeout?: number; prompt?: string }): Promise<{ async: false; value: "not-equal" | "timed-out"; } | { async: true; value: Promise<"ok" | "timed-out">; }>;
export async function waitAsync(input: Record<string, unknown>): Promise<unknown> { return runMethod(__waitAsync_12_spec, input); }

/** `Atomics.xor` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __xor_13_spec: GeneratedMethodSpec = {
  group: 'atomics',
  methodName: 'xor',
  functionId: 'Atomics.xor',
  kind: 'static',
  native: resolveNative('Atomics.xor'),
  receiverKey: "",
  paramOrder: ["typedArray","index","value"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Atomics.xor\`.
## Original signature(s)
  Overload 1: (typedArray: Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array, index: number, value: number) => number
  Overload 2: (typedArray: BigInt64Array | BigUint64Array, index: number, value: bigint) => bigint
## JSDoc
Stores the bitwise XOR of a value with the value at the given position in the array,
returning the original value. Until this atomic operation completes, any other read or write
operation against the array will block.

## How to respond
- Behave EXACTLY as the original \`xor\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"typedArray","type":"Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array"},{"name":"index","type":"number"},{"name":"value","type":"number"}],
};
export function xor(input: { typedArray: Int8Array | Uint8Array | Int16Array | Uint16Array | Int32Array | Uint32Array; index: number; value: number; prompt?: string }): Promise<number>;
export function xor(input: { typedArray: BigInt64Array | BigUint64Array; index: number; value: bigint; prompt?: string }): Promise<bigint>;
export async function xor(input: Record<string, unknown>): Promise<unknown> { return runMethod(__xor_13_spec, input); }


