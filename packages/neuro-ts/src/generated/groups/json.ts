// Auto-generated by scripts/generate-wrappers.ts. Do not edit.
import { runMethod, resolveNative, type GeneratedMethodSpec } from '../../runtime';

/** `JSON.parse` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __parse_0_spec: GeneratedMethodSpec = {
  group: 'json',
  methodName: 'parse',
  functionId: 'JSON.parse',
  kind: 'static',
  native: resolveNative('JSON.parse'),
  receiverKey: "",
  paramOrder: ["text","reviver"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`JSON.parse\`.
## Original signature(s)
  Overload 1: (text: string, reviver?: (this: any, key: string, value: any) => any) => any
## JSDoc
Converts a JavaScript Object Notation (JSON) string into an object.

## How to respond
- Behave EXACTLY as the original \`parse\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"text","type":"string"},{"name":"reviver?","type":"(this: any, key: string, value: any) => any"}],
};
export function parse(input: { text: string; reviver?: (this: any, key: string, value: any) => any; prompt?: string }): Promise<any>;
export async function parse(input: Record<string, unknown>): Promise<unknown> { return runMethod(__parse_0_spec, input); }

/** `JSON.stringify` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __stringify_1_spec: GeneratedMethodSpec = {
  group: 'json',
  methodName: 'stringify',
  functionId: 'JSON.stringify',
  kind: 'static',
  native: resolveNative('JSON.stringify'),
  receiverKey: "",
  paramOrder: ["value","replacer","space"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`JSON.stringify\`.
## Original signature(s)
  Overload 1: (value: any, replacer?: (this: any, key: string, value: any) => any, space?: string | number) => string
  Overload 2: (value: any, replacer?: (string | number)[], space?: string | number) => string
## JSDoc
Converts a JavaScript value to a JavaScript Object Notation (JSON) string.

## How to respond
- Behave EXACTLY as the original \`stringify\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"value","type":"any"},{"name":"replacer?","type":"(this: any, key: string, value: any) => any"},{"name":"space?","type":"string | number"}],
};
export function stringify(input: { value: any; replacer?: (this: any, key: string, value: any) => any; space?: string | number; prompt?: string }): Promise<string>;
export function stringify(input: { value: any; replacer?: (string | number)[]; space?: string | number; prompt?: string }): Promise<string>;
export async function stringify(input: Record<string, unknown>): Promise<unknown> { return runMethod(__stringify_1_spec, input); }


