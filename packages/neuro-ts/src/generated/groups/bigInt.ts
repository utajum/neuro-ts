// Auto-generated by scripts/generate-wrappers.ts. Do not edit.
import { runMethod, resolveNative, type GeneratedMethodSpec } from '../../runtime';

/** `BigInt.asIntN` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __asIntN_0_spec: GeneratedMethodSpec = {
  group: 'bigInt',
  methodName: 'asIntN',
  functionId: 'BigInt.asIntN',
  kind: 'static',
  native: resolveNative('BigInt.asIntN'),
  receiverKey: "",
  paramOrder: ["bits","int"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`BigInt.asIntN\`.
## Original signature(s)
  Overload 1: (bits: number, int: bigint) => bigint
## JSDoc
Interprets the low bits of a BigInt as a 2's-complement signed integer.
All higher bits are discarded.

## How to respond
- Behave EXACTLY as the original \`asIntN\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"bits","type":"number"},{"name":"int","type":"bigint"}],
};
export function asIntN(input: { bits: number; int: bigint; prompt?: string }): Promise<bigint>;
export async function asIntN(input: Record<string, unknown>): Promise<unknown> { return runMethod(__asIntN_0_spec, input); }

/** `BigInt.asUintN` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __asUintN_1_spec: GeneratedMethodSpec = {
  group: 'bigInt',
  methodName: 'asUintN',
  functionId: 'BigInt.asUintN',
  kind: 'static',
  native: resolveNative('BigInt.asUintN'),
  receiverKey: "",
  paramOrder: ["bits","int"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`BigInt.asUintN\`.
## Original signature(s)
  Overload 1: (bits: number, int: bigint) => bigint
## JSDoc
Interprets the low bits of a BigInt as an unsigned integer.
All higher bits are discarded.

## How to respond
- Behave EXACTLY as the original \`asUintN\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"bits","type":"number"},{"name":"int","type":"bigint"}],
};
export function asUintN(input: { bits: number; int: bigint; prompt?: string }): Promise<bigint>;
export async function asUintN(input: Record<string, unknown>): Promise<unknown> { return runMethod(__asUintN_1_spec, input); }


