// Auto-generated by scripts/generate-wrappers.ts. Do not edit.
import { runMethod, resolveNative, type GeneratedMethodSpec } from '../../runtime';

/** `Promise.all` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __all_0_spec: GeneratedMethodSpec = {
  group: 'promise',
  methodName: 'all',
  functionId: 'Promise.all',
  kind: 'static',
  native: resolveNative('Promise.all'),
  receiverKey: "",
  paramOrder: ["values"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Promise.all\`.
## Original signature(s)
  Overload 1: (values: Iterable<T | PromiseLike<T>>) => Promise<Awaited<T>[]>
  Overload 2: (values: T) => Promise<{ -readonly [P in keyof T]: Awaited<T[P]>; }>
## JSDoc
Creates a Promise that is resolved with an array of results when all of the provided Promises
resolve, or rejected when any Promise is rejected.

## How to respond
- Behave EXACTLY as the original \`all\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"values","type":"Iterable<T | PromiseLike<T>>"}],
};
export function all<T>(input: { values: Iterable<T | PromiseLike<T>>; prompt?: string }): Promise<Awaited<T>[]>;
export function all<T extends readonly unknown[] | []>(input: { values: T; prompt?: string }): Promise<{ -readonly [P in keyof T]: Awaited<T[P]>; }>;
export async function all(input: Record<string, unknown>): Promise<unknown> { return runMethod(__all_0_spec, input); }

/** `Promise.allSettled` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __allSettled_1_spec: GeneratedMethodSpec = {
  group: 'promise',
  methodName: 'allSettled',
  functionId: 'Promise.allSettled',
  kind: 'static',
  native: resolveNative('Promise.allSettled'),
  receiverKey: "",
  paramOrder: ["values"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Promise.allSettled\`.
## Original signature(s)
  Overload 1: (values: T) => Promise<{ -readonly [P in keyof T]: PromiseSettledResult<Awaited<T[P]>>; }>
  Overload 2: (values: Iterable<T | PromiseLike<T>>) => Promise<PromiseSettledResult<Awaited<T>>[]>
## JSDoc
Creates a Promise that is resolved with an array of results when all
of the provided Promises resolve or reject.

## How to respond
- Behave EXACTLY as the original \`allSettled\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"values","type":"T"}],
};
export function allSettled<T extends readonly unknown[] | []>(input: { values: T; prompt?: string }): Promise<{ -readonly [P in keyof T]: PromiseSettledResult<Awaited<T[P]>>; }>;
export function allSettled<T>(input: { values: Iterable<T | PromiseLike<T>>; prompt?: string }): Promise<PromiseSettledResult<Awaited<T>>[]>;
export async function allSettled(input: Record<string, unknown>): Promise<unknown> { return runMethod(__allSettled_1_spec, input); }

/** `Promise.any` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __any_2_spec: GeneratedMethodSpec = {
  group: 'promise',
  methodName: 'any',
  functionId: 'Promise.any',
  kind: 'static',
  native: resolveNative('Promise.any'),
  receiverKey: "",
  paramOrder: ["values"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Promise.any\`.
## Original signature(s)
  Overload 1: (values: T) => Promise<Awaited<T[number]>>
  Overload 2: (values: Iterable<T | PromiseLike<T>>) => Promise<Awaited<T>>
## JSDoc
The any function returns a promise that is fulfilled by the first given promise to be fulfilled, or rejected with an AggregateError containing an array of rejection reasons if all of the given promises are rejected. It resolves all elements of the passed iterable to promises as it runs this algorithm.

## How to respond
- Behave EXACTLY as the original \`any\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"values","type":"T"}],
};
export function any<T extends readonly unknown[] | []>(input: { values: T; prompt?: string }): Promise<Awaited<T[number]>>;
export function any<T>(input: { values: Iterable<T | PromiseLike<T>>; prompt?: string }): Promise<Awaited<T>>;
export async function any(input: Record<string, unknown>): Promise<unknown> { return runMethod(__any_2_spec, input); }

/** `Promise.prototype.catch` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __catch_3_spec: GeneratedMethodSpec = {
  group: 'promise',
  methodName: 'catch',
  functionId: 'Promise.prototype.catch',
  kind: 'instance',
  native: resolveNative('Promise.prototype.catch'),
  receiverKey: "promise",
  paramOrder: ["onrejected"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Promise.prototype.catch\`.
## Original signature(s)
  Overload 1: (onrejected?: (reason: any) => TResult | PromiseLike<TResult>) => Promise<T | TResult>
## JSDoc
Attaches a callback for only the rejection of the Promise.

## How to respond
- Behave EXACTLY as the original \`catch\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"onrejected?","type":"(reason: any) => TResult | PromiseLike<TResult>"}],
};
function _catch<T, TResult = never>(input: { promise: Promise<T>; onrejected?: (reason: any) => TResult | PromiseLike<TResult>; prompt?: string }): Promise<T | TResult>;
async function _catch(input: Record<string, unknown>): Promise<unknown> { return runMethod(__catch_3_spec, input); }

/** `Promise.prototype.finally` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __finally_4_spec: GeneratedMethodSpec = {
  group: 'promise',
  methodName: 'finally',
  functionId: 'Promise.prototype.finally',
  kind: 'instance',
  native: resolveNative('Promise.prototype.finally'),
  receiverKey: "promise",
  paramOrder: ["onfinally"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Promise.prototype.finally\`.
## Original signature(s)
  Overload 1: (onfinally?: () => void) => Promise<T>
## JSDoc
Attaches a callback that is invoked when the Promise is settled (fulfilled or rejected). The
resolved value cannot be modified from the callback.

## How to respond
- Behave EXACTLY as the original \`finally\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"onfinally?","type":"() => void"}],
};
function _finally<T>(input: { promise: Promise<T>; onfinally?: () => void; prompt?: string }): Promise<T>;
async function _finally(input: Record<string, unknown>): Promise<unknown> { return runMethod(__finally_4_spec, input); }

/** `Promise.race` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __race_5_spec: GeneratedMethodSpec = {
  group: 'promise',
  methodName: 'race',
  functionId: 'Promise.race',
  kind: 'static',
  native: resolveNative('Promise.race'),
  receiverKey: "",
  paramOrder: ["values"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Promise.race\`.
## Original signature(s)
  Overload 1: (values: Iterable<T | PromiseLike<T>>) => Promise<Awaited<T>>
  Overload 2: (values: T) => Promise<Awaited<T[number]>>
## JSDoc
Creates a Promise that is resolved or rejected when any of the provided Promises are resolved
or rejected.

## How to respond
- Behave EXACTLY as the original \`race\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"values","type":"Iterable<T | PromiseLike<T>>"}],
};
export function race<T>(input: { values: Iterable<T | PromiseLike<T>>; prompt?: string }): Promise<Awaited<T>>;
export function race<T extends readonly unknown[] | []>(input: { values: T; prompt?: string }): Promise<Awaited<T[number]>>;
export async function race(input: Record<string, unknown>): Promise<unknown> { return runMethod(__race_5_spec, input); }

/** `Promise.reject` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __reject_6_spec: GeneratedMethodSpec = {
  group: 'promise',
  methodName: 'reject',
  functionId: 'Promise.reject',
  kind: 'static',
  native: resolveNative('Promise.reject'),
  receiverKey: "",
  paramOrder: ["reason"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Promise.reject\`.
## Original signature(s)
  Overload 1: (reason?: any) => Promise<T>
## JSDoc
Creates a new rejected promise for the provided reason.

## How to respond
- Behave EXACTLY as the original \`reject\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"reason?","type":"any"}],
};
export function reject<T = never>(input: { reason?: any; prompt?: string }): Promise<T>;
export async function reject(input: Record<string, unknown>): Promise<unknown> { return runMethod(__reject_6_spec, input); }

/** `Promise.resolve` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __resolve_7_spec: GeneratedMethodSpec = {
  group: 'promise',
  methodName: 'resolve',
  functionId: 'Promise.resolve',
  kind: 'static',
  native: resolveNative('Promise.resolve'),
  receiverKey: "",
  paramOrder: ["value"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Promise.resolve\`.
## Original signature(s)
  Overload 1: () => Promise<void>
  Overload 2: (value: T) => Promise<Awaited<T>>
  Overload 3: (value: T | PromiseLike<T>) => Promise<Awaited<T>>
## JSDoc
Creates a new resolved promise.

## How to respond
- Behave EXACTLY as the original \`resolve\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function resolve(input: { prompt?: string }): Promise<void>;
export function resolve<T>(input: { value: T; prompt?: string }): Promise<Awaited<T>>;
export function resolve<T>(input: { value: T | PromiseLike<T>; prompt?: string }): Promise<Awaited<T>>;
export async function resolve(input: Record<string, unknown>): Promise<unknown> { return runMethod(__resolve_7_spec, input); }

/** `Promise.prototype.then` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __then_8_spec: GeneratedMethodSpec = {
  group: 'promise',
  methodName: 'then',
  functionId: 'Promise.prototype.then',
  kind: 'instance',
  native: resolveNative('Promise.prototype.then'),
  receiverKey: "promise",
  paramOrder: ["onfulfilled","onrejected"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Promise.prototype.then\`.
## Original signature(s)
  Overload 1: (onfulfilled?: (value: T) => TResult1 | PromiseLike<TResult1>, onrejected?: (reason: any) => TResult2 | PromiseLike<TResult2>) => Promise<TResult1 | TResult2>
## JSDoc
Attaches callbacks for the resolution and/or rejection of the Promise.

## How to respond
- Behave EXACTLY as the original \`then\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"onfulfilled?","type":"(value: T) => TResult1 | PromiseLike<TResult1>"},{"name":"onrejected?","type":"(reason: any) => TResult2 | PromiseLike<TResult2>"}],
};
export function then<T, TResult1 = T, TResult2 = never>(input: { promise: Promise<T>; onfulfilled?: (value: T) => TResult1 | PromiseLike<TResult1>; onrejected?: (reason: any) => TResult2 | PromiseLike<TResult2>; prompt?: string }): Promise<TResult1 | TResult2>;
export async function then(input: Record<string, unknown>): Promise<unknown> { return runMethod(__then_8_spec, input); }

/** `Promise.try` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __try_9_spec: GeneratedMethodSpec = {
  group: 'promise',
  methodName: 'try',
  functionId: 'Promise.try',
  kind: 'static',
  native: resolveNative('Promise.try'),
  receiverKey: "",
  paramOrder: ["callbackFn","args"],
  variadicKey: "args",
  systemPrompt: `You are simulating the JavaScript built-in \`Promise.try\`.
## Original signature(s)
  Overload 1: (callbackFn?: (...args: U) => T | PromiseLike<T>, ...args: U) => Promise<Awaited<T>>
## JSDoc
Takes a callback of any kind (returns or throws, synchronously or asynchronously) and wraps its result
in a Promise.

## How to respond
- Behave EXACTLY as the original \`try\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"callbackFn?","type":"(...args: U) => T | PromiseLike<T>"},{"name":"args","type":"U"}],
};
function _try<T, U extends unknown[]>(input: { callbackFn?: (...args: U) => T | PromiseLike<T>; args?: (U)[]; prompt?: string }): Promise<Awaited<T>>;
async function _try(input: Record<string, unknown>): Promise<unknown> { return runMethod(__try_9_spec, input); }

/** `Promise.withResolvers` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __withResolvers_10_spec: GeneratedMethodSpec = {
  group: 'promise',
  methodName: 'withResolvers',
  functionId: 'Promise.withResolvers',
  kind: 'static',
  native: resolveNative('Promise.withResolvers'),
  receiverKey: "",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Promise.withResolvers\`.
## Original signature(s)
  Overload 1: () => PromiseWithResolvers<T>
## JSDoc
Creates a new Promise and returns it in an object, along with its resolve and reject functions.

## How to respond
- Behave EXACTLY as the original \`withResolvers\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function withResolvers<T>(input: { prompt?: string }): Promise<PromiseWithResolvers<T>>;
export async function withResolvers(input: Record<string, unknown>): Promise<unknown> { return runMethod(__withResolvers_10_spec, input); }

export { _catch as catch };
export { _finally as finally };
export { _try as try };
