// Auto-generated by scripts/generate-wrappers.ts. Do not edit.
import { runMethod, resolveNative, type GeneratedMethodSpec } from '../../runtime';

/** `WeakMap.prototype.delete` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __delete_0_spec: GeneratedMethodSpec = {
  group: 'weakMap',
  methodName: 'delete',
  functionId: 'WeakMap.prototype.delete',
  kind: 'instance',
  native: resolveNative('WeakMap.prototype.delete'),
  receiverKey: "weakMap",
  paramOrder: ["key"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`WeakMap.prototype.delete\`.
## Original signature(s)
  Overload 1: (key: K) => boolean
## JSDoc
Removes the specified element from the WeakMap.

## How to respond
- Behave EXACTLY as the original \`delete\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"key","type":"K"}],
};
function _delete<V, K extends WeakKey>(input: { weakMap: WeakMap<K, V>; key: K; prompt?: string }): Promise<boolean>;
async function _delete(input: Record<string, unknown>): Promise<unknown> { return runMethod(__delete_0_spec, input); }

/** `WeakMap.prototype.get` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __get_1_spec: GeneratedMethodSpec = {
  group: 'weakMap',
  methodName: 'get',
  functionId: 'WeakMap.prototype.get',
  kind: 'instance',
  native: resolveNative('WeakMap.prototype.get'),
  receiverKey: "weakMap",
  paramOrder: ["key"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`WeakMap.prototype.get\`.
## Original signature(s)
  Overload 1: (key: K) => V
## How to respond
- Behave EXACTLY as the original \`get\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"key","type":"K"}],
};
export function get<V, K extends WeakKey>(input: { weakMap: WeakMap<K, V>; key: K; prompt?: string }): Promise<V>;
export async function get(input: Record<string, unknown>): Promise<unknown> { return runMethod(__get_1_spec, input); }

/** `WeakMap.prototype.getOrInsert` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getOrInsert_2_spec: GeneratedMethodSpec = {
  group: 'weakMap',
  methodName: 'getOrInsert',
  functionId: 'WeakMap.prototype.getOrInsert',
  kind: 'instance',
  native: resolveNative('WeakMap.prototype.getOrInsert'),
  receiverKey: "weakMap",
  paramOrder: ["key","defaultValue"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`WeakMap.prototype.getOrInsert\`.
## Original signature(s)
  Overload 1: (key: K, defaultValue: V) => V
## JSDoc
Returns a specified element from the WeakMap object.
If no element is associated with the specified key, a new element with the value \`defaultValue\` will be inserted into the WeakMap and returned.

## How to respond
- Behave EXACTLY as the original \`getOrInsert\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"key","type":"K"},{"name":"defaultValue","type":"V"}],
};
export function getOrInsert<V, K extends WeakKey>(input: { weakMap: WeakMap<K, V>; key: K; defaultValue: V; prompt?: string }): Promise<V>;
export async function getOrInsert(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getOrInsert_2_spec, input); }

/** `WeakMap.prototype.getOrInsertComputed` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getOrInsertComputed_3_spec: GeneratedMethodSpec = {
  group: 'weakMap',
  methodName: 'getOrInsertComputed',
  functionId: 'WeakMap.prototype.getOrInsertComputed',
  kind: 'instance',
  native: resolveNative('WeakMap.prototype.getOrInsertComputed'),
  receiverKey: "weakMap",
  paramOrder: ["key","callback"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`WeakMap.prototype.getOrInsertComputed\`.
## Original signature(s)
  Overload 1: (key: K, callback?: (key: K) => V) => V
## JSDoc
Returns a specified element from the WeakMap object.
If no element is associated with the specified key, the result of passing the specified key to the \`callback\` function will be inserted into the WeakMap and returned.

## How to respond
- Behave EXACTLY as the original \`getOrInsertComputed\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"key","type":"K"},{"name":"callback?","type":"(key: K) => V"}],
};
export function getOrInsertComputed<V, K extends WeakKey>(input: { weakMap: WeakMap<K, V>; key: K; callback?: (key: K) => V; prompt?: string }): Promise<V>;
export async function getOrInsertComputed(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getOrInsertComputed_3_spec, input); }

/** `WeakMap.prototype.has` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __has_4_spec: GeneratedMethodSpec = {
  group: 'weakMap',
  methodName: 'has',
  functionId: 'WeakMap.prototype.has',
  kind: 'instance',
  native: resolveNative('WeakMap.prototype.has'),
  receiverKey: "weakMap",
  paramOrder: ["key"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`WeakMap.prototype.has\`.
## Original signature(s)
  Overload 1: (key: K) => boolean
## How to respond
- Behave EXACTLY as the original \`has\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"key","type":"K"}],
};
export function has<V, K extends WeakKey>(input: { weakMap: WeakMap<K, V>; key: K; prompt?: string }): Promise<boolean>;
export async function has(input: Record<string, unknown>): Promise<unknown> { return runMethod(__has_4_spec, input); }

/** `WeakMap.prototype.set` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __set_5_spec: GeneratedMethodSpec = {
  group: 'weakMap',
  methodName: 'set',
  functionId: 'WeakMap.prototype.set',
  kind: 'instance',
  native: resolveNative('WeakMap.prototype.set'),
  receiverKey: "weakMap",
  paramOrder: ["key","value"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`WeakMap.prototype.set\`.
## Original signature(s)
  Overload 1: (key: K, value: V) => WeakMap<K, V>
## JSDoc
Adds a new element with a specified key and value.

## How to respond
- Behave EXACTLY as the original \`set\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"key","type":"K"},{"name":"value","type":"V"}],
};
export function set<V, K extends WeakKey>(input: { weakMap: WeakMap<K, V>; key: K; value: V; prompt?: string }): Promise<WeakMap<K, V>>;
export async function set(input: Record<string, unknown>): Promise<unknown> { return runMethod(__set_5_spec, input); }

export { _delete as delete };
