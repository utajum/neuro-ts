// Auto-generated by scripts/generate-wrappers.ts. Do not edit.
import { runMethod, resolveNative, type GeneratedMethodSpec } from '../../runtime';

/** `Set.prototype.add` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __add_0_spec: GeneratedMethodSpec = {
  group: 'set',
  methodName: 'add',
  functionId: 'Set.prototype.add',
  kind: 'instance',
  native: resolveNative('Set.prototype.add'),
  receiverKey: "set",
  paramOrder: ["value"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Set.prototype.add\`.
## Original signature(s)
  Overload 1: (value: T) => Set<T>
## JSDoc
Appends a new element with a specified value to the end of the Set.

## How to respond
- Behave EXACTLY as the original \`add\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"value","type":"T"}],
};
export function add<T>(input: { set: Set<T>; value: T; prompt?: string }): Promise<Set<T>>;
export async function add(input: Record<string, unknown>): Promise<unknown> { return runMethod(__add_0_spec, input); }

/** `Set.prototype.clear` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __clear_1_spec: GeneratedMethodSpec = {
  group: 'set',
  methodName: 'clear',
  functionId: 'Set.prototype.clear',
  kind: 'instance',
  native: resolveNative('Set.prototype.clear'),
  receiverKey: "set",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Set.prototype.clear\`.
## Original signature(s)
  Overload 1: () => void
## JSDoc
Removes all elements from the Set.

## How to respond
- Behave EXACTLY as the original \`clear\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function clear<T>(input: { set: Set<T>; prompt?: string }): Promise<void>;
export async function clear(input: Record<string, unknown>): Promise<unknown> { return runMethod(__clear_1_spec, input); }

/** `Set.prototype.delete` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __delete_2_spec: GeneratedMethodSpec = {
  group: 'set',
  methodName: 'delete',
  functionId: 'Set.prototype.delete',
  kind: 'instance',
  native: resolveNative('Set.prototype.delete'),
  receiverKey: "set",
  paramOrder: ["value"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Set.prototype.delete\`.
## Original signature(s)
  Overload 1: (value: T) => boolean
## JSDoc
Removes a specified value from the Set.

## How to respond
- Behave EXACTLY as the original \`delete\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"value","type":"T"}],
};
function _delete<T>(input: { set: Set<T>; value: T; prompt?: string }): Promise<boolean>;
async function _delete(input: Record<string, unknown>): Promise<unknown> { return runMethod(__delete_2_spec, input); }

/** `Set.prototype.difference` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __difference_3_spec: GeneratedMethodSpec = {
  group: 'set',
  methodName: 'difference',
  functionId: 'Set.prototype.difference',
  kind: 'instance',
  native: resolveNative('Set.prototype.difference'),
  receiverKey: "set",
  paramOrder: ["other"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Set.prototype.difference\`.
## Original signature(s)
  Overload 1: (other: ReadonlySetLike<U>) => Set<T>
## How to respond
- Behave EXACTLY as the original \`difference\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"other","type":"ReadonlySetLike<U>"}],
};
export function difference<T, U>(input: { set: Set<T>; other: ReadonlySetLike<U>; prompt?: string }): Promise<Set<T>>;
export async function difference(input: Record<string, unknown>): Promise<unknown> { return runMethod(__difference_3_spec, input); }

/** `Set.prototype.entries` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __entries_4_spec: GeneratedMethodSpec = {
  group: 'set',
  methodName: 'entries',
  functionId: 'Set.prototype.entries',
  kind: 'instance',
  native: resolveNative('Set.prototype.entries'),
  receiverKey: "set",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Set.prototype.entries\`.
## Original signature(s)
  Overload 1: () => SetIterator<[T, T]>
## JSDoc
Returns an iterable of [v,v] pairs for every value \`v\` in the set.

## How to respond
- Behave EXACTLY as the original \`entries\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function entries<T>(input: { set: Set<T>; prompt?: string }): Promise<SetIterator<[T, T]>>;
export async function entries(input: Record<string, unknown>): Promise<unknown> { return runMethod(__entries_4_spec, input); }

/** `Set.prototype.forEach` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __forEach_5_spec: GeneratedMethodSpec = {
  group: 'set',
  methodName: 'forEach',
  functionId: 'Set.prototype.forEach',
  kind: 'instance',
  native: resolveNative('Set.prototype.forEach'),
  receiverKey: "set",
  paramOrder: ["callbackfn","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Set.prototype.forEach\`.
## Original signature(s)
  Overload 1: (callbackfn?: (value: T, value2: T, set: Set<T>) => void, thisArg?: any) => void
## JSDoc
Executes a provided function once per each value in the Set object, in insertion order.

## How to respond
- Behave EXACTLY as the original \`forEach\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"callbackfn?","type":"(value: T, value2: T, set: Set<T>) => void"},{"name":"thisArg?","type":"any"}],
};
export function forEach<T>(input: { set: Set<T>; callbackfn?: (value: T, value2: T, set: Set<T>) => void; thisArg?: any; prompt?: string }): Promise<void>;
export async function forEach(input: Record<string, unknown>): Promise<unknown> { return runMethod(__forEach_5_spec, input); }

/** `Set.prototype.has` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __has_6_spec: GeneratedMethodSpec = {
  group: 'set',
  methodName: 'has',
  functionId: 'Set.prototype.has',
  kind: 'instance',
  native: resolveNative('Set.prototype.has'),
  receiverKey: "set",
  paramOrder: ["value"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Set.prototype.has\`.
## Original signature(s)
  Overload 1: (value: T) => boolean
## How to respond
- Behave EXACTLY as the original \`has\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"value","type":"T"}],
};
export function has<T>(input: { set: Set<T>; value: T; prompt?: string }): Promise<boolean>;
export async function has(input: Record<string, unknown>): Promise<unknown> { return runMethod(__has_6_spec, input); }

/** `Set.prototype.intersection` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __intersection_7_spec: GeneratedMethodSpec = {
  group: 'set',
  methodName: 'intersection',
  functionId: 'Set.prototype.intersection',
  kind: 'instance',
  native: resolveNative('Set.prototype.intersection'),
  receiverKey: "set",
  paramOrder: ["other"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Set.prototype.intersection\`.
## Original signature(s)
  Overload 1: (other: ReadonlySetLike<U>) => Set<T & U>
## How to respond
- Behave EXACTLY as the original \`intersection\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"other","type":"ReadonlySetLike<U>"}],
};
export function intersection<T, U>(input: { set: Set<T>; other: ReadonlySetLike<U>; prompt?: string }): Promise<Set<T & U>>;
export async function intersection(input: Record<string, unknown>): Promise<unknown> { return runMethod(__intersection_7_spec, input); }

/** `Set.prototype.isDisjointFrom` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __isDisjointFrom_8_spec: GeneratedMethodSpec = {
  group: 'set',
  methodName: 'isDisjointFrom',
  functionId: 'Set.prototype.isDisjointFrom',
  kind: 'instance',
  native: resolveNative('Set.prototype.isDisjointFrom'),
  receiverKey: "set",
  paramOrder: ["other"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Set.prototype.isDisjointFrom\`.
## Original signature(s)
  Overload 1: (other: ReadonlySetLike<unknown>) => boolean
## How to respond
- Behave EXACTLY as the original \`isDisjointFrom\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"other","type":"ReadonlySetLike<unknown>"}],
};
export function isDisjointFrom<T>(input: { set: Set<T>; other: ReadonlySetLike<unknown>; prompt?: string }): Promise<boolean>;
export async function isDisjointFrom(input: Record<string, unknown>): Promise<unknown> { return runMethod(__isDisjointFrom_8_spec, input); }

/** `Set.prototype.isSubsetOf` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __isSubsetOf_9_spec: GeneratedMethodSpec = {
  group: 'set',
  methodName: 'isSubsetOf',
  functionId: 'Set.prototype.isSubsetOf',
  kind: 'instance',
  native: resolveNative('Set.prototype.isSubsetOf'),
  receiverKey: "set",
  paramOrder: ["other"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Set.prototype.isSubsetOf\`.
## Original signature(s)
  Overload 1: (other: ReadonlySetLike<unknown>) => boolean
## How to respond
- Behave EXACTLY as the original \`isSubsetOf\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"other","type":"ReadonlySetLike<unknown>"}],
};
export function isSubsetOf<T>(input: { set: Set<T>; other: ReadonlySetLike<unknown>; prompt?: string }): Promise<boolean>;
export async function isSubsetOf(input: Record<string, unknown>): Promise<unknown> { return runMethod(__isSubsetOf_9_spec, input); }

/** `Set.prototype.isSupersetOf` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __isSupersetOf_10_spec: GeneratedMethodSpec = {
  group: 'set',
  methodName: 'isSupersetOf',
  functionId: 'Set.prototype.isSupersetOf',
  kind: 'instance',
  native: resolveNative('Set.prototype.isSupersetOf'),
  receiverKey: "set",
  paramOrder: ["other"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Set.prototype.isSupersetOf\`.
## Original signature(s)
  Overload 1: (other: ReadonlySetLike<unknown>) => boolean
## How to respond
- Behave EXACTLY as the original \`isSupersetOf\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"other","type":"ReadonlySetLike<unknown>"}],
};
export function isSupersetOf<T>(input: { set: Set<T>; other: ReadonlySetLike<unknown>; prompt?: string }): Promise<boolean>;
export async function isSupersetOf(input: Record<string, unknown>): Promise<unknown> { return runMethod(__isSupersetOf_10_spec, input); }

/** `Set.prototype.keys` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __keys_11_spec: GeneratedMethodSpec = {
  group: 'set',
  methodName: 'keys',
  functionId: 'Set.prototype.keys',
  kind: 'instance',
  native: resolveNative('Set.prototype.keys'),
  receiverKey: "set",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Set.prototype.keys\`.
## Original signature(s)
  Overload 1: () => SetIterator<T>
## JSDoc
Despite its name, returns an iterable of the values in the set.

## How to respond
- Behave EXACTLY as the original \`keys\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function keys<T>(input: { set: Set<T>; prompt?: string }): Promise<SetIterator<T>>;
export async function keys(input: Record<string, unknown>): Promise<unknown> { return runMethod(__keys_11_spec, input); }

/** `Set.prototype.symmetricDifference` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __symmetricDifference_12_spec: GeneratedMethodSpec = {
  group: 'set',
  methodName: 'symmetricDifference',
  functionId: 'Set.prototype.symmetricDifference',
  kind: 'instance',
  native: resolveNative('Set.prototype.symmetricDifference'),
  receiverKey: "set",
  paramOrder: ["other"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Set.prototype.symmetricDifference\`.
## Original signature(s)
  Overload 1: (other: ReadonlySetLike<U>) => Set<T | U>
## How to respond
- Behave EXACTLY as the original \`symmetricDifference\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"other","type":"ReadonlySetLike<U>"}],
};
export function symmetricDifference<T, U>(input: { set: Set<T>; other: ReadonlySetLike<U>; prompt?: string }): Promise<Set<T | U>>;
export async function symmetricDifference(input: Record<string, unknown>): Promise<unknown> { return runMethod(__symmetricDifference_12_spec, input); }

/** `Set.prototype.union` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __union_13_spec: GeneratedMethodSpec = {
  group: 'set',
  methodName: 'union',
  functionId: 'Set.prototype.union',
  kind: 'instance',
  native: resolveNative('Set.prototype.union'),
  receiverKey: "set",
  paramOrder: ["other"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Set.prototype.union\`.
## Original signature(s)
  Overload 1: (other: ReadonlySetLike<U>) => Set<T | U>
## How to respond
- Behave EXACTLY as the original \`union\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"other","type":"ReadonlySetLike<U>"}],
};
export function union<T, U>(input: { set: Set<T>; other: ReadonlySetLike<U>; prompt?: string }): Promise<Set<T | U>>;
export async function union(input: Record<string, unknown>): Promise<unknown> { return runMethod(__union_13_spec, input); }

/** `Set.prototype.values` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __values_14_spec: GeneratedMethodSpec = {
  group: 'set',
  methodName: 'values',
  functionId: 'Set.prototype.values',
  kind: 'instance',
  native: resolveNative('Set.prototype.values'),
  receiverKey: "set",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Set.prototype.values\`.
## Original signature(s)
  Overload 1: () => SetIterator<T>
## JSDoc
Returns an iterable of values in the set.

## How to respond
- Behave EXACTLY as the original \`values\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function values<T>(input: { set: Set<T>; prompt?: string }): Promise<SetIterator<T>>;
export async function values(input: Record<string, unknown>): Promise<unknown> { return runMethod(__values_14_spec, input); }

export { _delete as delete };
