// Auto-generated by scripts/generate-wrappers.ts. Do not edit.
import { runMethod, resolveNative, type GeneratedMethodSpec } from '../../runtime';

/** `WeakSet.prototype.add` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __add_0_spec: GeneratedMethodSpec = {
  group: 'weakSet',
  methodName: 'add',
  functionId: 'WeakSet.prototype.add',
  kind: 'instance',
  native: resolveNative('WeakSet.prototype.add'),
  receiverKey: "weakSet",
  paramOrder: ["value"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`WeakSet.prototype.add\`.
## Original signature(s)
  Overload 1: (value: T) => WeakSet<T>
## JSDoc
Appends a new value to the end of the WeakSet.

## How to respond
- Behave EXACTLY as the original \`add\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"value","type":"T"}],
};
export function add<T extends WeakKey>(input: { weakSet: WeakSet<T>; value: T; prompt?: string }): Promise<WeakSet<T>>;
export async function add(input: Record<string, unknown>): Promise<unknown> { return runMethod(__add_0_spec, input); }

/** `WeakSet.prototype.delete` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __delete_1_spec: GeneratedMethodSpec = {
  group: 'weakSet',
  methodName: 'delete',
  functionId: 'WeakSet.prototype.delete',
  kind: 'instance',
  native: resolveNative('WeakSet.prototype.delete'),
  receiverKey: "weakSet",
  paramOrder: ["value"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`WeakSet.prototype.delete\`.
## Original signature(s)
  Overload 1: (value: T) => boolean
## JSDoc
Removes the specified element from the WeakSet.

## How to respond
- Behave EXACTLY as the original \`delete\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"value","type":"T"}],
};
function _delete<T extends WeakKey>(input: { weakSet: WeakSet<T>; value: T; prompt?: string }): Promise<boolean>;
async function _delete(input: Record<string, unknown>): Promise<unknown> { return runMethod(__delete_1_spec, input); }

/** `WeakSet.prototype.has` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __has_2_spec: GeneratedMethodSpec = {
  group: 'weakSet',
  methodName: 'has',
  functionId: 'WeakSet.prototype.has',
  kind: 'instance',
  native: resolveNative('WeakSet.prototype.has'),
  receiverKey: "weakSet",
  paramOrder: ["value"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`WeakSet.prototype.has\`.
## Original signature(s)
  Overload 1: (value: T) => boolean
## How to respond
- Behave EXACTLY as the original \`has\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"value","type":"T"}],
};
export function has<T extends WeakKey>(input: { weakSet: WeakSet<T>; value: T; prompt?: string }): Promise<boolean>;
export async function has(input: Record<string, unknown>): Promise<unknown> { return runMethod(__has_2_spec, input); }

export { _delete as delete };
