// Auto-generated by scripts/generate-wrappers.ts. Do not edit.
import { runMethod, resolveNative, type GeneratedMethodSpec } from '../../runtime';

/** `Uint16Array.prototype.at` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __at_0_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'at',
  functionId: 'Uint16Array.prototype.at',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.at'),
  receiverKey: "uint16Array",
  paramOrder: ["index"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.at\`.
## Original signature(s)
  Overload 1: (index: number) => number
## JSDoc
Returns the item located at the specified index.

## How to respond
- Behave EXACTLY as the original \`at\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"index","type":"number"}],
};
export function at(input: { uint16Array: Uint16Array; index: number; prompt?: string }): Promise<number>;
export async function at(input: Record<string, unknown>): Promise<unknown> { return runMethod(__at_0_spec, input); }

/** `Uint16Array.prototype.copyWithin` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __copyWithin_1_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'copyWithin',
  functionId: 'Uint16Array.prototype.copyWithin',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.copyWithin'),
  receiverKey: "uint16Array",
  paramOrder: ["target","start","end"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.copyWithin\`.
## Original signature(s)
  Overload 1: (target: number, start: number, end?: number) => Uint16Array
## JSDoc
Returns the this object after copying a section of the array identified by start and end
to the same array starting at position target

## How to respond
- Behave EXACTLY as the original \`copyWithin\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"target","type":"number"},{"name":"start","type":"number"},{"name":"end?","type":"number"}],
};
export function copyWithin(input: { uint16Array: Uint16Array; target: number; start: number; end?: number; prompt?: string }): Promise<Uint16Array>;
export async function copyWithin(input: Record<string, unknown>): Promise<unknown> { return runMethod(__copyWithin_1_spec, input); }

/** `Uint16Array.prototype.entries` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __entries_2_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'entries',
  functionId: 'Uint16Array.prototype.entries',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.entries'),
  receiverKey: "uint16Array",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.entries\`.
## Original signature(s)
  Overload 1: () => ArrayIterator<[number, number]>
## JSDoc
Returns an array of key, value pairs for every entry in the array

## How to respond
- Behave EXACTLY as the original \`entries\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function entries(input: { uint16Array: Uint16Array; prompt?: string }): Promise<ArrayIterator<[number, number]>>;
export async function entries(input: Record<string, unknown>): Promise<unknown> { return runMethod(__entries_2_spec, input); }

/** `Uint16Array.prototype.every` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __every_3_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'every',
  functionId: 'Uint16Array.prototype.every',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.every'),
  receiverKey: "uint16Array",
  paramOrder: ["predicate","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.every\`.
## Original signature(s)
  Overload 1: (predicate?: (value: number, index: number, array: Uint16Array) => unknown, thisArg?: any) => boolean
## JSDoc
Determines whether all the members of an array satisfy the specified test.

## How to respond
- Behave EXACTLY as the original \`every\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: number, index: number, array: Uint16Array) => unknown"},{"name":"thisArg?","type":"any"}],
};
export function every(input: { uint16Array: Uint16Array; predicate?: (value: number, index: number, array: Uint16Array) => unknown; thisArg?: any; prompt?: string }): Promise<boolean>;
export async function every(input: Record<string, unknown>): Promise<unknown> { return runMethod(__every_3_spec, input); }

/** `Uint16Array.prototype.fill` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __fill_4_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'fill',
  functionId: 'Uint16Array.prototype.fill',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.fill'),
  receiverKey: "uint16Array",
  paramOrder: ["value","start","end"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.fill\`.
## Original signature(s)
  Overload 1: (value: number, start?: number, end?: number) => Uint16Array
## JSDoc
Changes all array elements from \`start\` to \`end\` index to a static \`value\` and returns the modified array

## How to respond
- Behave EXACTLY as the original \`fill\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"value","type":"number"},{"name":"start?","type":"number"},{"name":"end?","type":"number"}],
};
export function fill(input: { uint16Array: Uint16Array; value: number; start?: number; end?: number; prompt?: string }): Promise<Uint16Array>;
export async function fill(input: Record<string, unknown>): Promise<unknown> { return runMethod(__fill_4_spec, input); }

/** `Uint16Array.prototype.filter` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __filter_5_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'filter',
  functionId: 'Uint16Array.prototype.filter',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.filter'),
  receiverKey: "uint16Array",
  paramOrder: ["predicate","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.filter\`.
## Original signature(s)
  Overload 1: (predicate?: (value: number, index: number, array: Uint16Array) => any, thisArg?: any) => Uint16Array
## JSDoc
Returns the elements of an array that meet the condition specified in a callback function.

## How to respond
- Behave EXACTLY as the original \`filter\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: number, index: number, array: Uint16Array) => any"},{"name":"thisArg?","type":"any"}],
};
export function filter(input: { uint16Array: Uint16Array; predicate?: (value: number, index: number, array: Uint16Array) => any; thisArg?: any; prompt?: string }): Promise<Uint16Array>;
export async function filter(input: Record<string, unknown>): Promise<unknown> { return runMethod(__filter_5_spec, input); }

/** `Uint16Array.prototype.find` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __find_6_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'find',
  functionId: 'Uint16Array.prototype.find',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.find'),
  receiverKey: "uint16Array",
  paramOrder: ["predicate","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.find\`.
## Original signature(s)
  Overload 1: (predicate?: (value: number, index: number, obj: Uint16Array) => boolean, thisArg?: any) => number
## JSDoc
Returns the value of the first element in the array where predicate is true, and undefined
otherwise.

## How to respond
- Behave EXACTLY as the original \`find\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: number, index: number, obj: Uint16Array) => boolean"},{"name":"thisArg?","type":"any"}],
};
export function find(input: { uint16Array: Uint16Array; predicate?: (value: number, index: number, obj: Uint16Array) => boolean; thisArg?: any; prompt?: string }): Promise<number>;
export async function find(input: Record<string, unknown>): Promise<unknown> { return runMethod(__find_6_spec, input); }

/** `Uint16Array.prototype.findIndex` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __findIndex_7_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'findIndex',
  functionId: 'Uint16Array.prototype.findIndex',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.findIndex'),
  receiverKey: "uint16Array",
  paramOrder: ["predicate","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.findIndex\`.
## Original signature(s)
  Overload 1: (predicate?: (value: number, index: number, obj: Uint16Array) => boolean, thisArg?: any) => number
## JSDoc
Returns the index of the first element in the array where predicate is true, and -1
otherwise.

## How to respond
- Behave EXACTLY as the original \`findIndex\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: number, index: number, obj: Uint16Array) => boolean"},{"name":"thisArg?","type":"any"}],
};
export function findIndex(input: { uint16Array: Uint16Array; predicate?: (value: number, index: number, obj: Uint16Array) => boolean; thisArg?: any; prompt?: string }): Promise<number>;
export async function findIndex(input: Record<string, unknown>): Promise<unknown> { return runMethod(__findIndex_7_spec, input); }

/** `Uint16Array.prototype.findLast` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __findLast_8_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'findLast',
  functionId: 'Uint16Array.prototype.findLast',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.findLast'),
  receiverKey: "uint16Array",
  paramOrder: ["predicate","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.findLast\`.
## Original signature(s)
  Overload 1: (predicate?: (value: number, index: number, array: Uint16Array) => boolean, thisArg?: any) => S
  Overload 2: (predicate?: (value: number, index: number, array: Uint16Array) => unknown, thisArg?: any) => number
## JSDoc
Returns the value of the last element in the array where predicate is true, and undefined
otherwise.

## How to respond
- Behave EXACTLY as the original \`findLast\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: number, index: number, array: Uint16Array) => boolean"},{"name":"thisArg?","type":"any"}],
};
export function findLast<S extends number>(input: { uint16Array: Uint16Array; predicate?: (value: number, index: number, array: Uint16Array) => boolean; thisArg?: any; prompt?: string }): Promise<S>;
export function findLast(input: { uint16Array: Uint16Array; predicate?: (value: number, index: number, array: Uint16Array) => unknown; thisArg?: any; prompt?: string }): Promise<number>;
export async function findLast(input: Record<string, unknown>): Promise<unknown> { return runMethod(__findLast_8_spec, input); }

/** `Uint16Array.prototype.findLastIndex` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __findLastIndex_9_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'findLastIndex',
  functionId: 'Uint16Array.prototype.findLastIndex',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.findLastIndex'),
  receiverKey: "uint16Array",
  paramOrder: ["predicate","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.findLastIndex\`.
## Original signature(s)
  Overload 1: (predicate?: (value: number, index: number, array: Uint16Array) => unknown, thisArg?: any) => number
## JSDoc
Returns the index of the last element in the array where predicate is true, and -1
otherwise.

## How to respond
- Behave EXACTLY as the original \`findLastIndex\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: number, index: number, array: Uint16Array) => unknown"},{"name":"thisArg?","type":"any"}],
};
export function findLastIndex(input: { uint16Array: Uint16Array; predicate?: (value: number, index: number, array: Uint16Array) => unknown; thisArg?: any; prompt?: string }): Promise<number>;
export async function findLastIndex(input: Record<string, unknown>): Promise<unknown> { return runMethod(__findLastIndex_9_spec, input); }

/** `Uint16Array.prototype.forEach` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __forEach_10_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'forEach',
  functionId: 'Uint16Array.prototype.forEach',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.forEach'),
  receiverKey: "uint16Array",
  paramOrder: ["callbackfn","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.forEach\`.
## Original signature(s)
  Overload 1: (callbackfn?: (value: number, index: number, array: Uint16Array) => void, thisArg?: any) => void
## JSDoc
Performs the specified action for each element in an array.

## How to respond
- Behave EXACTLY as the original \`forEach\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"callbackfn?","type":"(value: number, index: number, array: Uint16Array) => void"},{"name":"thisArg?","type":"any"}],
};
export function forEach(input: { uint16Array: Uint16Array; callbackfn?: (value: number, index: number, array: Uint16Array) => void; thisArg?: any; prompt?: string }): Promise<void>;
export async function forEach(input: Record<string, unknown>): Promise<unknown> { return runMethod(__forEach_10_spec, input); }

/** `Uint16Array.from` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __from_11_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'from',
  functionId: 'Uint16Array.from',
  kind: 'static',
  native: resolveNative('Uint16Array.from'),
  receiverKey: "",
  paramOrder: ["arrayLike","mapfn","thisArg","elements"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.from\`.
## Original signature(s)
  Overload 1: (arrayLike: ArrayLike<number>) => Uint16Array
  Overload 2: (arrayLike: ArrayLike<T>, mapfn?: (v: T, k: number) => number, thisArg?: any) => Uint16Array
  Overload 3: (elements: Iterable<number>) => Uint16Array
  Overload 4: (elements: Iterable<T>, mapfn?: (v: T, k: number) => number, thisArg?: any) => Uint16Array
## JSDoc
Creates an array from an array-like or iterable object.

## How to respond
- Behave EXACTLY as the original \`from\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"arrayLike","type":"ArrayLike<number>"}],
};
export function from(input: { arrayLike: ArrayLike<number>; prompt?: string }): Promise<Uint16Array>;
export function from<T>(input: { arrayLike: ArrayLike<T>; mapfn?: (v: T, k: number) => number; thisArg?: any; prompt?: string }): Promise<Uint16Array>;
export function from(input: { elements: Iterable<number>; prompt?: string }): Promise<Uint16Array>;
export function from<T>(input: { elements: Iterable<T>; mapfn?: (v: T, k: number) => number; thisArg?: any; prompt?: string }): Promise<Uint16Array>;
export async function from(input: Record<string, unknown>): Promise<unknown> { return runMethod(__from_11_spec, input); }

/** `Uint16Array.prototype.includes` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __includes_12_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'includes',
  functionId: 'Uint16Array.prototype.includes',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.includes'),
  receiverKey: "uint16Array",
  paramOrder: ["searchElement","fromIndex"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.includes\`.
## Original signature(s)
  Overload 1: (searchElement: number, fromIndex?: number) => boolean
## JSDoc
Determines whether an array includes a certain element, returning true or false as appropriate.

## How to respond
- Behave EXACTLY as the original \`includes\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"searchElement","type":"number"},{"name":"fromIndex?","type":"number"}],
};
export function includes(input: { uint16Array: Uint16Array; searchElement: number; fromIndex?: number; prompt?: string }): Promise<boolean>;
export async function includes(input: Record<string, unknown>): Promise<unknown> { return runMethod(__includes_12_spec, input); }

/** `Uint16Array.prototype.indexOf` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __indexOf_13_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'indexOf',
  functionId: 'Uint16Array.prototype.indexOf',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.indexOf'),
  receiverKey: "uint16Array",
  paramOrder: ["searchElement","fromIndex"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.indexOf\`.
## Original signature(s)
  Overload 1: (searchElement: number, fromIndex?: number) => number
## JSDoc
Returns the index of the first occurrence of a value in an array, or -1 if it is not present.

## How to respond
- Behave EXACTLY as the original \`indexOf\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"searchElement","type":"number"},{"name":"fromIndex?","type":"number"}],
};
export function indexOf(input: { uint16Array: Uint16Array; searchElement: number; fromIndex?: number; prompt?: string }): Promise<number>;
export async function indexOf(input: Record<string, unknown>): Promise<unknown> { return runMethod(__indexOf_13_spec, input); }

/** `Uint16Array.prototype.join` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __join_14_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'join',
  functionId: 'Uint16Array.prototype.join',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.join'),
  receiverKey: "uint16Array",
  paramOrder: ["separator"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.join\`.
## Original signature(s)
  Overload 1: (separator?: string) => string
## JSDoc
Adds all the elements of an array separated by the specified separator string.

## How to respond
- Behave EXACTLY as the original \`join\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"separator?","type":"string"}],
};
export function join(input: { uint16Array: Uint16Array; separator?: string; prompt?: string }): Promise<string>;
export async function join(input: Record<string, unknown>): Promise<unknown> { return runMethod(__join_14_spec, input); }

/** `Uint16Array.prototype.keys` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __keys_15_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'keys',
  functionId: 'Uint16Array.prototype.keys',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.keys'),
  receiverKey: "uint16Array",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.keys\`.
## Original signature(s)
  Overload 1: () => ArrayIterator<number>
## JSDoc
Returns an list of keys in the array

## How to respond
- Behave EXACTLY as the original \`keys\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function keys(input: { uint16Array: Uint16Array; prompt?: string }): Promise<ArrayIterator<number>>;
export async function keys(input: Record<string, unknown>): Promise<unknown> { return runMethod(__keys_15_spec, input); }

/** `Uint16Array.prototype.lastIndexOf` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __lastIndexOf_16_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'lastIndexOf',
  functionId: 'Uint16Array.prototype.lastIndexOf',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.lastIndexOf'),
  receiverKey: "uint16Array",
  paramOrder: ["searchElement","fromIndex"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.lastIndexOf\`.
## Original signature(s)
  Overload 1: (searchElement: number, fromIndex?: number) => number
## JSDoc
Returns the index of the last occurrence of a value in an array, or -1 if it is not present.

## How to respond
- Behave EXACTLY as the original \`lastIndexOf\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"searchElement","type":"number"},{"name":"fromIndex?","type":"number"}],
};
export function lastIndexOf(input: { uint16Array: Uint16Array; searchElement: number; fromIndex?: number; prompt?: string }): Promise<number>;
export async function lastIndexOf(input: Record<string, unknown>): Promise<unknown> { return runMethod(__lastIndexOf_16_spec, input); }

/** `Uint16Array.prototype.map` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __map_17_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'map',
  functionId: 'Uint16Array.prototype.map',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.map'),
  receiverKey: "uint16Array",
  paramOrder: ["callbackfn","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.map\`.
## Original signature(s)
  Overload 1: (callbackfn?: (value: number, index: number, array: Uint16Array) => number, thisArg?: any) => Uint16Array
## JSDoc
Calls a defined callback function on each element of an array, and returns an array that
contains the results.

## How to respond
- Behave EXACTLY as the original \`map\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"callbackfn?","type":"(value: number, index: number, array: Uint16Array) => number"},{"name":"thisArg?","type":"any"}],
};
export function map(input: { uint16Array: Uint16Array; callbackfn?: (value: number, index: number, array: Uint16Array) => number; thisArg?: any; prompt?: string }): Promise<Uint16Array>;
export async function map(input: Record<string, unknown>): Promise<unknown> { return runMethod(__map_17_spec, input); }

/** `Uint16Array.of` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __of_18_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'of',
  functionId: 'Uint16Array.of',
  kind: 'static',
  native: resolveNative('Uint16Array.of'),
  receiverKey: "",
  paramOrder: ["items"],
  variadicKey: "items",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.of\`.
## Original signature(s)
  Overload 1: (...items: number[]) => Uint16Array
## JSDoc
Returns a new array from a set of elements.

## How to respond
- Behave EXACTLY as the original \`of\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"items","type":"number[]"}],
};
export function of(input: { items?: (number)[]; prompt?: string }): Promise<Uint16Array>;
export async function of(input: Record<string, unknown>): Promise<unknown> { return runMethod(__of_18_spec, input); }

/** `Uint16Array.prototype.reduce` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __reduce_19_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'reduce',
  functionId: 'Uint16Array.prototype.reduce',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.reduce'),
  receiverKey: "uint16Array",
  paramOrder: ["callbackfn","initialValue"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.reduce\`.
## Original signature(s)
  Overload 1: (callbackfn?: (previousValue: number, currentValue: number, currentIndex: number, array: Uint16Array) => number) => number
  Overload 2: (callbackfn?: (previousValue: number, currentValue: number, currentIndex: number, array: Uint16Array) => number, initialValue?: number) => number
  Overload 3: (callbackfn?: (previousValue: U, currentValue: number, currentIndex: number, array: Uint16Array) => U, initialValue?: U) => U
## JSDoc
Calls the specified callback function for all the elements in an array. The return value of
the callback function is the accumulated result, and is provided as an argument in the next
call to the callback function.

## How to respond
- Behave EXACTLY as the original \`reduce\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"callbackfn?","type":"(previousValue: number, currentValue: number, currentIndex: number, array: Uint16Array) => number"}],
};
export function reduce(input: { uint16Array: Uint16Array; callbackfn?: (previousValue: number, currentValue: number, currentIndex: number, array: Uint16Array) => number; prompt?: string }): Promise<number>;
export function reduce(input: { uint16Array: Uint16Array; callbackfn?: (previousValue: number, currentValue: number, currentIndex: number, array: Uint16Array) => number; initialValue?: number; prompt?: string }): Promise<number>;
export function reduce<U>(input: { uint16Array: Uint16Array; callbackfn?: (previousValue: U, currentValue: number, currentIndex: number, array: Uint16Array) => U; initialValue?: U; prompt?: string }): Promise<U>;
export async function reduce(input: Record<string, unknown>): Promise<unknown> { return runMethod(__reduce_19_spec, input); }

/** `Uint16Array.prototype.reduceRight` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __reduceRight_20_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'reduceRight',
  functionId: 'Uint16Array.prototype.reduceRight',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.reduceRight'),
  receiverKey: "uint16Array",
  paramOrder: ["callbackfn","initialValue"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.reduceRight\`.
## Original signature(s)
  Overload 1: (callbackfn?: (previousValue: number, currentValue: number, currentIndex: number, array: Uint16Array) => number) => number
  Overload 2: (callbackfn?: (previousValue: number, currentValue: number, currentIndex: number, array: Uint16Array) => number, initialValue?: number) => number
  Overload 3: (callbackfn?: (previousValue: U, currentValue: number, currentIndex: number, array: Uint16Array) => U, initialValue?: U) => U
## JSDoc
Calls the specified callback function for all the elements in an array, in descending order.
The return value of the callback function is the accumulated result, and is provided as an
argument in the next call to the callback function.

## How to respond
- Behave EXACTLY as the original \`reduceRight\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"callbackfn?","type":"(previousValue: number, currentValue: number, currentIndex: number, array: Uint16Array) => number"}],
};
export function reduceRight(input: { uint16Array: Uint16Array; callbackfn?: (previousValue: number, currentValue: number, currentIndex: number, array: Uint16Array) => number; prompt?: string }): Promise<number>;
export function reduceRight(input: { uint16Array: Uint16Array; callbackfn?: (previousValue: number, currentValue: number, currentIndex: number, array: Uint16Array) => number; initialValue?: number; prompt?: string }): Promise<number>;
export function reduceRight<U>(input: { uint16Array: Uint16Array; callbackfn?: (previousValue: U, currentValue: number, currentIndex: number, array: Uint16Array) => U; initialValue?: U; prompt?: string }): Promise<U>;
export async function reduceRight(input: Record<string, unknown>): Promise<unknown> { return runMethod(__reduceRight_20_spec, input); }

/** `Uint16Array.prototype.reverse` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __reverse_21_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'reverse',
  functionId: 'Uint16Array.prototype.reverse',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.reverse'),
  receiverKey: "uint16Array",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.reverse\`.
## Original signature(s)
  Overload 1: () => Uint16Array
## JSDoc
Reverses the elements in an Array.

## How to respond
- Behave EXACTLY as the original \`reverse\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function reverse(input: { uint16Array: Uint16Array; prompt?: string }): Promise<Uint16Array>;
export async function reverse(input: Record<string, unknown>): Promise<unknown> { return runMethod(__reverse_21_spec, input); }

/** `Uint16Array.prototype.set` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __set_22_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'set',
  functionId: 'Uint16Array.prototype.set',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.set'),
  receiverKey: "uint16Array",
  paramOrder: ["array","offset"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.set\`.
## Original signature(s)
  Overload 1: (array: ArrayLike<number>, offset?: number) => void
## JSDoc
Sets a value or an array of values.

## How to respond
- Behave EXACTLY as the original \`set\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"array","type":"ArrayLike<number>"},{"name":"offset?","type":"number"}],
};
export function set(input: { uint16Array: Uint16Array; array: ArrayLike<number>; offset?: number; prompt?: string }): Promise<void>;
export async function set(input: Record<string, unknown>): Promise<unknown> { return runMethod(__set_22_spec, input); }

/** `Uint16Array.prototype.slice` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __slice_23_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'slice',
  functionId: 'Uint16Array.prototype.slice',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.slice'),
  receiverKey: "uint16Array",
  paramOrder: ["start","end"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.slice\`.
## Original signature(s)
  Overload 1: (start?: number, end?: number) => Uint16Array
## JSDoc
Returns a section of an array.

## How to respond
- Behave EXACTLY as the original \`slice\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"start?","type":"number"},{"name":"end?","type":"number"}],
};
export function slice(input: { uint16Array: Uint16Array; start?: number; end?: number; prompt?: string }): Promise<Uint16Array>;
export async function slice(input: Record<string, unknown>): Promise<unknown> { return runMethod(__slice_23_spec, input); }

/** `Uint16Array.prototype.some` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __some_24_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'some',
  functionId: 'Uint16Array.prototype.some',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.some'),
  receiverKey: "uint16Array",
  paramOrder: ["predicate","thisArg"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.some\`.
## Original signature(s)
  Overload 1: (predicate?: (value: number, index: number, array: Uint16Array) => unknown, thisArg?: any) => boolean
## JSDoc
Determines whether the specified callback function returns true for any element of an array.

## How to respond
- Behave EXACTLY as the original \`some\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"predicate?","type":"(value: number, index: number, array: Uint16Array) => unknown"},{"name":"thisArg?","type":"any"}],
};
export function some(input: { uint16Array: Uint16Array; predicate?: (value: number, index: number, array: Uint16Array) => unknown; thisArg?: any; prompt?: string }): Promise<boolean>;
export async function some(input: Record<string, unknown>): Promise<unknown> { return runMethod(__some_24_spec, input); }

/** `Uint16Array.prototype.sort` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __sort_25_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'sort',
  functionId: 'Uint16Array.prototype.sort',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.sort'),
  receiverKey: "uint16Array",
  paramOrder: ["compareFn"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.sort\`.
## Original signature(s)
  Overload 1: (compareFn?: (a: number, b: number) => number) => Uint16Array
## JSDoc
Sorts an array.

## How to respond
- Behave EXACTLY as the original \`sort\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"compareFn?","type":"(a: number, b: number) => number"}],
};
export function sort(input: { uint16Array: Uint16Array; compareFn?: (a: number, b: number) => number; prompt?: string }): Promise<Uint16Array>;
export async function sort(input: Record<string, unknown>): Promise<unknown> { return runMethod(__sort_25_spec, input); }

/** `Uint16Array.prototype.subarray` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __subarray_26_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'subarray',
  functionId: 'Uint16Array.prototype.subarray',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.subarray'),
  receiverKey: "uint16Array",
  paramOrder: ["begin","end"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.subarray\`.
## Original signature(s)
  Overload 1: (begin?: number, end?: number) => Uint16Array
## JSDoc
Gets a new Uint16Array view of the ArrayBuffer store for this array, referencing the elements
at begin, inclusive, up to end, exclusive.

## How to respond
- Behave EXACTLY as the original \`subarray\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"begin?","type":"number"},{"name":"end?","type":"number"}],
};
export function subarray(input: { uint16Array: Uint16Array; begin?: number; end?: number; prompt?: string }): Promise<Uint16Array>;
export async function subarray(input: Record<string, unknown>): Promise<unknown> { return runMethod(__subarray_26_spec, input); }

/** `Uint16Array.prototype.toReversed` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toReversed_27_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'toReversed',
  functionId: 'Uint16Array.prototype.toReversed',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.toReversed'),
  receiverKey: "uint16Array",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.toReversed\`.
## Original signature(s)
  Overload 1: () => Uint16Array
## JSDoc
Copies the array and returns the copy with the elements in reverse order.

## How to respond
- Behave EXACTLY as the original \`toReversed\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function toReversed(input: { uint16Array: Uint16Array; prompt?: string }): Promise<Uint16Array>;
export async function toReversed(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toReversed_27_spec, input); }

/** `Uint16Array.prototype.toSorted` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toSorted_28_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'toSorted',
  functionId: 'Uint16Array.prototype.toSorted',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.toSorted'),
  receiverKey: "uint16Array",
  paramOrder: ["compareFn"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.toSorted\`.
## Original signature(s)
  Overload 1: (compareFn?: (a: number, b: number) => number) => Uint16Array
## JSDoc
Copies and sorts the array.

## How to respond
- Behave EXACTLY as the original \`toSorted\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"compareFn?","type":"(a: number, b: number) => number"}],
};
export function toSorted(input: { uint16Array: Uint16Array; compareFn?: (a: number, b: number) => number; prompt?: string }): Promise<Uint16Array>;
export async function toSorted(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toSorted_28_spec, input); }

/** `Uint16Array.prototype.values` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __values_29_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'values',
  functionId: 'Uint16Array.prototype.values',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.values'),
  receiverKey: "uint16Array",
  paramOrder: [],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.values\`.
## Original signature(s)
  Overload 1: () => ArrayIterator<number>
## JSDoc
Returns an list of values in the array

## How to respond
- Behave EXACTLY as the original \`values\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [],
};
export function values(input: { uint16Array: Uint16Array; prompt?: string }): Promise<ArrayIterator<number>>;
export async function values(input: Record<string, unknown>): Promise<unknown> { return runMethod(__values_29_spec, input); }

/** `Uint16Array.prototype.with` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __with_30_spec: GeneratedMethodSpec = {
  group: 'uint16Array',
  methodName: 'with',
  functionId: 'Uint16Array.prototype.with',
  kind: 'instance',
  native: resolveNative('Uint16Array.prototype.with'),
  receiverKey: "uint16Array",
  paramOrder: ["index","value"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Uint16Array.prototype.with\`.
## Original signature(s)
  Overload 1: (index: number, value: number) => Uint16Array
## JSDoc
Copies the array and inserts the given number at the provided index.

## How to respond
- Behave EXACTLY as the original \`with\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"index","type":"number"},{"name":"value","type":"number"}],
};
function _with(input: { uint16Array: Uint16Array; index: number; value: number; prompt?: string }): Promise<Uint16Array>;
async function _with(input: Record<string, unknown>): Promise<unknown> { return runMethod(__with_30_spec, input); }

export { _with as with };
