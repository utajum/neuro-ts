// Auto-generated by scripts/generate-wrappers.ts. Do not edit.
import { runMethod, resolveNative, type GeneratedMethodSpec } from '../../runtime';

/** `DataView.prototype.getBigInt64` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getBigInt64_0_spec: GeneratedMethodSpec = {
  group: 'dataView',
  methodName: 'getBigInt64',
  functionId: 'DataView.prototype.getBigInt64',
  kind: 'instance',
  native: resolveNative('DataView.prototype.getBigInt64'),
  receiverKey: "dataView",
  paramOrder: ["byteOffset","littleEndian"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`DataView.prototype.getBigInt64\`.
## Original signature(s)
  Overload 1: (byteOffset: number, littleEndian?: boolean) => bigint
## JSDoc
Gets the BigInt64 value at the specified byte offset from the start of the view. There is
no alignment constraint; multi-byte values may be fetched from any offset.

## How to respond
- Behave EXACTLY as the original \`getBigInt64\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"byteOffset","type":"number"},{"name":"littleEndian?","type":"boolean"}],
};
export function getBigInt64(input: { dataView: DataView; byteOffset: number; littleEndian?: boolean; prompt?: string }): Promise<bigint>;
export async function getBigInt64(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getBigInt64_0_spec, input); }

/** `DataView.prototype.getBigUint64` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getBigUint64_1_spec: GeneratedMethodSpec = {
  group: 'dataView',
  methodName: 'getBigUint64',
  functionId: 'DataView.prototype.getBigUint64',
  kind: 'instance',
  native: resolveNative('DataView.prototype.getBigUint64'),
  receiverKey: "dataView",
  paramOrder: ["byteOffset","littleEndian"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`DataView.prototype.getBigUint64\`.
## Original signature(s)
  Overload 1: (byteOffset: number, littleEndian?: boolean) => bigint
## JSDoc
Gets the BigUint64 value at the specified byte offset from the start of the view. There is
no alignment constraint; multi-byte values may be fetched from any offset.

## How to respond
- Behave EXACTLY as the original \`getBigUint64\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"byteOffset","type":"number"},{"name":"littleEndian?","type":"boolean"}],
};
export function getBigUint64(input: { dataView: DataView; byteOffset: number; littleEndian?: boolean; prompt?: string }): Promise<bigint>;
export async function getBigUint64(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getBigUint64_1_spec, input); }

/** `DataView.prototype.getFloat16` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getFloat16_2_spec: GeneratedMethodSpec = {
  group: 'dataView',
  methodName: 'getFloat16',
  functionId: 'DataView.prototype.getFloat16',
  kind: 'instance',
  native: resolveNative('DataView.prototype.getFloat16'),
  receiverKey: "dataView",
  paramOrder: ["byteOffset","littleEndian"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`DataView.prototype.getFloat16\`.
## Original signature(s)
  Overload 1: (byteOffset: number, littleEndian?: boolean) => number
## JSDoc
Gets the Float16 value at the specified byte offset from the start of the view. There is
no alignment constraint; multi-byte values may be fetched from any offset.

## How to respond
- Behave EXACTLY as the original \`getFloat16\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"byteOffset","type":"number"},{"name":"littleEndian?","type":"boolean"}],
};
export function getFloat16(input: { dataView: DataView; byteOffset: number; littleEndian?: boolean; prompt?: string }): Promise<number>;
export async function getFloat16(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getFloat16_2_spec, input); }

/** `DataView.prototype.getFloat32` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getFloat32_3_spec: GeneratedMethodSpec = {
  group: 'dataView',
  methodName: 'getFloat32',
  functionId: 'DataView.prototype.getFloat32',
  kind: 'instance',
  native: resolveNative('DataView.prototype.getFloat32'),
  receiverKey: "dataView",
  paramOrder: ["byteOffset","littleEndian"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`DataView.prototype.getFloat32\`.
## Original signature(s)
  Overload 1: (byteOffset: number, littleEndian?: boolean) => number
## JSDoc
Gets the Float32 value at the specified byte offset from the start of the view. There is
no alignment constraint; multi-byte values may be fetched from any offset.

## How to respond
- Behave EXACTLY as the original \`getFloat32\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"byteOffset","type":"number"},{"name":"littleEndian?","type":"boolean"}],
};
export function getFloat32(input: { dataView: DataView; byteOffset: number; littleEndian?: boolean; prompt?: string }): Promise<number>;
export async function getFloat32(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getFloat32_3_spec, input); }

/** `DataView.prototype.getFloat64` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getFloat64_4_spec: GeneratedMethodSpec = {
  group: 'dataView',
  methodName: 'getFloat64',
  functionId: 'DataView.prototype.getFloat64',
  kind: 'instance',
  native: resolveNative('DataView.prototype.getFloat64'),
  receiverKey: "dataView",
  paramOrder: ["byteOffset","littleEndian"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`DataView.prototype.getFloat64\`.
## Original signature(s)
  Overload 1: (byteOffset: number, littleEndian?: boolean) => number
## JSDoc
Gets the Float64 value at the specified byte offset from the start of the view. There is
no alignment constraint; multi-byte values may be fetched from any offset.

## How to respond
- Behave EXACTLY as the original \`getFloat64\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"byteOffset","type":"number"},{"name":"littleEndian?","type":"boolean"}],
};
export function getFloat64(input: { dataView: DataView; byteOffset: number; littleEndian?: boolean; prompt?: string }): Promise<number>;
export async function getFloat64(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getFloat64_4_spec, input); }

/** `DataView.prototype.getInt16` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getInt16_5_spec: GeneratedMethodSpec = {
  group: 'dataView',
  methodName: 'getInt16',
  functionId: 'DataView.prototype.getInt16',
  kind: 'instance',
  native: resolveNative('DataView.prototype.getInt16'),
  receiverKey: "dataView",
  paramOrder: ["byteOffset","littleEndian"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`DataView.prototype.getInt16\`.
## Original signature(s)
  Overload 1: (byteOffset: number, littleEndian?: boolean) => number
## JSDoc
Gets the Int16 value at the specified byte offset from the start of the view. There is
no alignment constraint; multi-byte values may be fetched from any offset.

## How to respond
- Behave EXACTLY as the original \`getInt16\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"byteOffset","type":"number"},{"name":"littleEndian?","type":"boolean"}],
};
export function getInt16(input: { dataView: DataView; byteOffset: number; littleEndian?: boolean; prompt?: string }): Promise<number>;
export async function getInt16(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getInt16_5_spec, input); }

/** `DataView.prototype.getInt32` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getInt32_6_spec: GeneratedMethodSpec = {
  group: 'dataView',
  methodName: 'getInt32',
  functionId: 'DataView.prototype.getInt32',
  kind: 'instance',
  native: resolveNative('DataView.prototype.getInt32'),
  receiverKey: "dataView",
  paramOrder: ["byteOffset","littleEndian"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`DataView.prototype.getInt32\`.
## Original signature(s)
  Overload 1: (byteOffset: number, littleEndian?: boolean) => number
## JSDoc
Gets the Int32 value at the specified byte offset from the start of the view. There is
no alignment constraint; multi-byte values may be fetched from any offset.

## How to respond
- Behave EXACTLY as the original \`getInt32\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"byteOffset","type":"number"},{"name":"littleEndian?","type":"boolean"}],
};
export function getInt32(input: { dataView: DataView; byteOffset: number; littleEndian?: boolean; prompt?: string }): Promise<number>;
export async function getInt32(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getInt32_6_spec, input); }

/** `DataView.prototype.getInt8` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getInt8_7_spec: GeneratedMethodSpec = {
  group: 'dataView',
  methodName: 'getInt8',
  functionId: 'DataView.prototype.getInt8',
  kind: 'instance',
  native: resolveNative('DataView.prototype.getInt8'),
  receiverKey: "dataView",
  paramOrder: ["byteOffset"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`DataView.prototype.getInt8\`.
## Original signature(s)
  Overload 1: (byteOffset: number) => number
## JSDoc
Gets the Int8 value at the specified byte offset from the start of the view. There is
no alignment constraint; multi-byte values may be fetched from any offset.

## How to respond
- Behave EXACTLY as the original \`getInt8\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"byteOffset","type":"number"}],
};
export function getInt8(input: { dataView: DataView; byteOffset: number; prompt?: string }): Promise<number>;
export async function getInt8(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getInt8_7_spec, input); }

/** `DataView.prototype.getUint16` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getUint16_8_spec: GeneratedMethodSpec = {
  group: 'dataView',
  methodName: 'getUint16',
  functionId: 'DataView.prototype.getUint16',
  kind: 'instance',
  native: resolveNative('DataView.prototype.getUint16'),
  receiverKey: "dataView",
  paramOrder: ["byteOffset","littleEndian"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`DataView.prototype.getUint16\`.
## Original signature(s)
  Overload 1: (byteOffset: number, littleEndian?: boolean) => number
## JSDoc
Gets the Uint16 value at the specified byte offset from the start of the view. There is
no alignment constraint; multi-byte values may be fetched from any offset.

## How to respond
- Behave EXACTLY as the original \`getUint16\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"byteOffset","type":"number"},{"name":"littleEndian?","type":"boolean"}],
};
export function getUint16(input: { dataView: DataView; byteOffset: number; littleEndian?: boolean; prompt?: string }): Promise<number>;
export async function getUint16(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getUint16_8_spec, input); }

/** `DataView.prototype.getUint32` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getUint32_9_spec: GeneratedMethodSpec = {
  group: 'dataView',
  methodName: 'getUint32',
  functionId: 'DataView.prototype.getUint32',
  kind: 'instance',
  native: resolveNative('DataView.prototype.getUint32'),
  receiverKey: "dataView",
  paramOrder: ["byteOffset","littleEndian"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`DataView.prototype.getUint32\`.
## Original signature(s)
  Overload 1: (byteOffset: number, littleEndian?: boolean) => number
## JSDoc
Gets the Uint32 value at the specified byte offset from the start of the view. There is
no alignment constraint; multi-byte values may be fetched from any offset.

## How to respond
- Behave EXACTLY as the original \`getUint32\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"byteOffset","type":"number"},{"name":"littleEndian?","type":"boolean"}],
};
export function getUint32(input: { dataView: DataView; byteOffset: number; littleEndian?: boolean; prompt?: string }): Promise<number>;
export async function getUint32(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getUint32_9_spec, input); }

/** `DataView.prototype.getUint8` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __getUint8_10_spec: GeneratedMethodSpec = {
  group: 'dataView',
  methodName: 'getUint8',
  functionId: 'DataView.prototype.getUint8',
  kind: 'instance',
  native: resolveNative('DataView.prototype.getUint8'),
  receiverKey: "dataView",
  paramOrder: ["byteOffset"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`DataView.prototype.getUint8\`.
## Original signature(s)
  Overload 1: (byteOffset: number) => number
## JSDoc
Gets the Uint8 value at the specified byte offset from the start of the view. There is
no alignment constraint; multi-byte values may be fetched from any offset.

## How to respond
- Behave EXACTLY as the original \`getUint8\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"byteOffset","type":"number"}],
};
export function getUint8(input: { dataView: DataView; byteOffset: number; prompt?: string }): Promise<number>;
export async function getUint8(input: Record<string, unknown>): Promise<unknown> { return runMethod(__getUint8_10_spec, input); }

/** `DataView.prototype.setBigInt64` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setBigInt64_11_spec: GeneratedMethodSpec = {
  group: 'dataView',
  methodName: 'setBigInt64',
  functionId: 'DataView.prototype.setBigInt64',
  kind: 'instance',
  native: resolveNative('DataView.prototype.setBigInt64'),
  receiverKey: "dataView",
  paramOrder: ["byteOffset","value","littleEndian"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`DataView.prototype.setBigInt64\`.
## Original signature(s)
  Overload 1: (byteOffset: number, value: bigint, littleEndian?: boolean) => void
## JSDoc
Stores a BigInt64 value at the specified byte offset from the start of the view.

## How to respond
- Behave EXACTLY as the original \`setBigInt64\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"byteOffset","type":"number"},{"name":"value","type":"bigint"},{"name":"littleEndian?","type":"boolean"}],
};
export function setBigInt64(input: { dataView: DataView; byteOffset: number; value: bigint; littleEndian?: boolean; prompt?: string }): Promise<void>;
export async function setBigInt64(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setBigInt64_11_spec, input); }

/** `DataView.prototype.setBigUint64` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setBigUint64_12_spec: GeneratedMethodSpec = {
  group: 'dataView',
  methodName: 'setBigUint64',
  functionId: 'DataView.prototype.setBigUint64',
  kind: 'instance',
  native: resolveNative('DataView.prototype.setBigUint64'),
  receiverKey: "dataView",
  paramOrder: ["byteOffset","value","littleEndian"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`DataView.prototype.setBigUint64\`.
## Original signature(s)
  Overload 1: (byteOffset: number, value: bigint, littleEndian?: boolean) => void
## JSDoc
Stores a BigUint64 value at the specified byte offset from the start of the view.

## How to respond
- Behave EXACTLY as the original \`setBigUint64\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"byteOffset","type":"number"},{"name":"value","type":"bigint"},{"name":"littleEndian?","type":"boolean"}],
};
export function setBigUint64(input: { dataView: DataView; byteOffset: number; value: bigint; littleEndian?: boolean; prompt?: string }): Promise<void>;
export async function setBigUint64(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setBigUint64_12_spec, input); }

/** `DataView.prototype.setFloat16` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setFloat16_13_spec: GeneratedMethodSpec = {
  group: 'dataView',
  methodName: 'setFloat16',
  functionId: 'DataView.prototype.setFloat16',
  kind: 'instance',
  native: resolveNative('DataView.prototype.setFloat16'),
  receiverKey: "dataView",
  paramOrder: ["byteOffset","value","littleEndian"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`DataView.prototype.setFloat16\`.
## Original signature(s)
  Overload 1: (byteOffset: number, value: number, littleEndian?: boolean) => void
## JSDoc
Stores an Float16 value at the specified byte offset from the start of the view.

## How to respond
- Behave EXACTLY as the original \`setFloat16\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"byteOffset","type":"number"},{"name":"value","type":"number"},{"name":"littleEndian?","type":"boolean"}],
};
export function setFloat16(input: { dataView: DataView; byteOffset: number; value: number; littleEndian?: boolean; prompt?: string }): Promise<void>;
export async function setFloat16(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setFloat16_13_spec, input); }

/** `DataView.prototype.setFloat32` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setFloat32_14_spec: GeneratedMethodSpec = {
  group: 'dataView',
  methodName: 'setFloat32',
  functionId: 'DataView.prototype.setFloat32',
  kind: 'instance',
  native: resolveNative('DataView.prototype.setFloat32'),
  receiverKey: "dataView",
  paramOrder: ["byteOffset","value","littleEndian"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`DataView.prototype.setFloat32\`.
## Original signature(s)
  Overload 1: (byteOffset: number, value: number, littleEndian?: boolean) => void
## JSDoc
Stores an Float32 value at the specified byte offset from the start of the view.

## How to respond
- Behave EXACTLY as the original \`setFloat32\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"byteOffset","type":"number"},{"name":"value","type":"number"},{"name":"littleEndian?","type":"boolean"}],
};
export function setFloat32(input: { dataView: DataView; byteOffset: number; value: number; littleEndian?: boolean; prompt?: string }): Promise<void>;
export async function setFloat32(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setFloat32_14_spec, input); }

/** `DataView.prototype.setFloat64` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setFloat64_15_spec: GeneratedMethodSpec = {
  group: 'dataView',
  methodName: 'setFloat64',
  functionId: 'DataView.prototype.setFloat64',
  kind: 'instance',
  native: resolveNative('DataView.prototype.setFloat64'),
  receiverKey: "dataView",
  paramOrder: ["byteOffset","value","littleEndian"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`DataView.prototype.setFloat64\`.
## Original signature(s)
  Overload 1: (byteOffset: number, value: number, littleEndian?: boolean) => void
## JSDoc
Stores an Float64 value at the specified byte offset from the start of the view.

## How to respond
- Behave EXACTLY as the original \`setFloat64\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"byteOffset","type":"number"},{"name":"value","type":"number"},{"name":"littleEndian?","type":"boolean"}],
};
export function setFloat64(input: { dataView: DataView; byteOffset: number; value: number; littleEndian?: boolean; prompt?: string }): Promise<void>;
export async function setFloat64(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setFloat64_15_spec, input); }

/** `DataView.prototype.setInt16` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setInt16_16_spec: GeneratedMethodSpec = {
  group: 'dataView',
  methodName: 'setInt16',
  functionId: 'DataView.prototype.setInt16',
  kind: 'instance',
  native: resolveNative('DataView.prototype.setInt16'),
  receiverKey: "dataView",
  paramOrder: ["byteOffset","value","littleEndian"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`DataView.prototype.setInt16\`.
## Original signature(s)
  Overload 1: (byteOffset: number, value: number, littleEndian?: boolean) => void
## JSDoc
Stores an Int16 value at the specified byte offset from the start of the view.

## How to respond
- Behave EXACTLY as the original \`setInt16\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"byteOffset","type":"number"},{"name":"value","type":"number"},{"name":"littleEndian?","type":"boolean"}],
};
export function setInt16(input: { dataView: DataView; byteOffset: number; value: number; littleEndian?: boolean; prompt?: string }): Promise<void>;
export async function setInt16(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setInt16_16_spec, input); }

/** `DataView.prototype.setInt32` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setInt32_17_spec: GeneratedMethodSpec = {
  group: 'dataView',
  methodName: 'setInt32',
  functionId: 'DataView.prototype.setInt32',
  kind: 'instance',
  native: resolveNative('DataView.prototype.setInt32'),
  receiverKey: "dataView",
  paramOrder: ["byteOffset","value","littleEndian"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`DataView.prototype.setInt32\`.
## Original signature(s)
  Overload 1: (byteOffset: number, value: number, littleEndian?: boolean) => void
## JSDoc
Stores an Int32 value at the specified byte offset from the start of the view.

## How to respond
- Behave EXACTLY as the original \`setInt32\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"byteOffset","type":"number"},{"name":"value","type":"number"},{"name":"littleEndian?","type":"boolean"}],
};
export function setInt32(input: { dataView: DataView; byteOffset: number; value: number; littleEndian?: boolean; prompt?: string }): Promise<void>;
export async function setInt32(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setInt32_17_spec, input); }

/** `DataView.prototype.setInt8` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setInt8_18_spec: GeneratedMethodSpec = {
  group: 'dataView',
  methodName: 'setInt8',
  functionId: 'DataView.prototype.setInt8',
  kind: 'instance',
  native: resolveNative('DataView.prototype.setInt8'),
  receiverKey: "dataView",
  paramOrder: ["byteOffset","value"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`DataView.prototype.setInt8\`.
## Original signature(s)
  Overload 1: (byteOffset: number, value: number) => void
## JSDoc
Stores an Int8 value at the specified byte offset from the start of the view.

## How to respond
- Behave EXACTLY as the original \`setInt8\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"byteOffset","type":"number"},{"name":"value","type":"number"}],
};
export function setInt8(input: { dataView: DataView; byteOffset: number; value: number; prompt?: string }): Promise<void>;
export async function setInt8(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setInt8_18_spec, input); }

/** `DataView.prototype.setUint16` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setUint16_19_spec: GeneratedMethodSpec = {
  group: 'dataView',
  methodName: 'setUint16',
  functionId: 'DataView.prototype.setUint16',
  kind: 'instance',
  native: resolveNative('DataView.prototype.setUint16'),
  receiverKey: "dataView",
  paramOrder: ["byteOffset","value","littleEndian"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`DataView.prototype.setUint16\`.
## Original signature(s)
  Overload 1: (byteOffset: number, value: number, littleEndian?: boolean) => void
## JSDoc
Stores an Uint16 value at the specified byte offset from the start of the view.

## How to respond
- Behave EXACTLY as the original \`setUint16\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"byteOffset","type":"number"},{"name":"value","type":"number"},{"name":"littleEndian?","type":"boolean"}],
};
export function setUint16(input: { dataView: DataView; byteOffset: number; value: number; littleEndian?: boolean; prompt?: string }): Promise<void>;
export async function setUint16(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setUint16_19_spec, input); }

/** `DataView.prototype.setUint32` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setUint32_20_spec: GeneratedMethodSpec = {
  group: 'dataView',
  methodName: 'setUint32',
  functionId: 'DataView.prototype.setUint32',
  kind: 'instance',
  native: resolveNative('DataView.prototype.setUint32'),
  receiverKey: "dataView",
  paramOrder: ["byteOffset","value","littleEndian"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`DataView.prototype.setUint32\`.
## Original signature(s)
  Overload 1: (byteOffset: number, value: number, littleEndian?: boolean) => void
## JSDoc
Stores an Uint32 value at the specified byte offset from the start of the view.

## How to respond
- Behave EXACTLY as the original \`setUint32\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"byteOffset","type":"number"},{"name":"value","type":"number"},{"name":"littleEndian?","type":"boolean"}],
};
export function setUint32(input: { dataView: DataView; byteOffset: number; value: number; littleEndian?: boolean; prompt?: string }): Promise<void>;
export async function setUint32(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setUint32_20_spec, input); }

/** `DataView.prototype.setUint8` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __setUint8_21_spec: GeneratedMethodSpec = {
  group: 'dataView',
  methodName: 'setUint8',
  functionId: 'DataView.prototype.setUint8',
  kind: 'instance',
  native: resolveNative('DataView.prototype.setUint8'),
  receiverKey: "dataView",
  paramOrder: ["byteOffset","value"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`DataView.prototype.setUint8\`.
## Original signature(s)
  Overload 1: (byteOffset: number, value: number) => void
## JSDoc
Stores an Uint8 value at the specified byte offset from the start of the view.

## How to respond
- Behave EXACTLY as the original \`setUint8\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"byteOffset","type":"number"},{"name":"value","type":"number"}],
};
export function setUint8(input: { dataView: DataView; byteOffset: number; value: number; prompt?: string }): Promise<void>;
export async function setUint8(input: Record<string, unknown>): Promise<unknown> { return runMethod(__setUint8_21_spec, input); }


