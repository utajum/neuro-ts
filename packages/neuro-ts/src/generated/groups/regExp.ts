// Auto-generated by scripts/generate-wrappers.ts. Do not edit.
import { runMethod, resolveNative, type GeneratedMethodSpec } from '../../runtime';

/** `RegExp.prototype.compile` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __compile_0_spec: GeneratedMethodSpec = {
  group: 'regExp',
  methodName: 'compile',
  functionId: 'RegExp.prototype.compile',
  kind: 'instance',
  native: resolveNative('RegExp.prototype.compile'),
  receiverKey: "regExp",
  paramOrder: ["pattern","flags"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`RegExp.prototype.compile\`.
## Original signature(s)
  Overload 1: (pattern: string, flags?: string) => RegExp
## How to respond
- Behave EXACTLY as the original \`compile\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"pattern","type":"string"},{"name":"flags?","type":"string"}],
};
export function compile(input: { regExp: RegExp; pattern: string; flags?: string; prompt?: string }): Promise<RegExp>;
export async function compile(input: Record<string, unknown>): Promise<unknown> { return runMethod(__compile_0_spec, input); }

/** `RegExp.prototype.exec` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __exec_1_spec: GeneratedMethodSpec = {
  group: 'regExp',
  methodName: 'exec',
  functionId: 'RegExp.prototype.exec',
  kind: 'instance',
  native: resolveNative('RegExp.prototype.exec'),
  receiverKey: "regExp",
  paramOrder: ["string"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`RegExp.prototype.exec\`.
## Original signature(s)
  Overload 1: (string: string) => RegExpExecArray
## JSDoc
Executes a search on a string using a regular expression pattern, and returns an array containing the results of that search.

## How to respond
- Behave EXACTLY as the original \`exec\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"string","type":"string"}],
};
export function exec(input: { regExp: RegExp; string: string; prompt?: string }): Promise<RegExpExecArray>;
export async function exec(input: Record<string, unknown>): Promise<unknown> { return runMethod(__exec_1_spec, input); }

/** `RegExp.prototype.test` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __test_2_spec: GeneratedMethodSpec = {
  group: 'regExp',
  methodName: 'test',
  functionId: 'RegExp.prototype.test',
  kind: 'instance',
  native: resolveNative('RegExp.prototype.test'),
  receiverKey: "regExp",
  paramOrder: ["string"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`RegExp.prototype.test\`.
## Original signature(s)
  Overload 1: (string: string) => boolean
## JSDoc
Returns a Boolean value that indicates whether or not a pattern exists in a searched string.

## How to respond
- Behave EXACTLY as the original \`test\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"string","type":"string"}],
};
export function test(input: { regExp: RegExp; string: string; prompt?: string }): Promise<boolean>;
export async function test(input: Record<string, unknown>): Promise<unknown> { return runMethod(__test_2_spec, input); }


