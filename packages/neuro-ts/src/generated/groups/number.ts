// Auto-generated by scripts/generate-wrappers.ts. Do not edit.
import { runMethod, resolveNative, type GeneratedMethodSpec } from '../../runtime';

/** `Number.isFinite` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __isFinite_0_spec: GeneratedMethodSpec = {
  group: 'number',
  methodName: 'isFinite',
  functionId: 'Number.isFinite',
  kind: 'static',
  native: resolveNative('Number.isFinite'),
  receiverKey: "",
  paramOrder: ["number"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Number.isFinite\`.
## Original signature(s)
  Overload 1: (number: unknown) => boolean
## JSDoc
Returns true if passed value is finite.
Unlike the global isFinite, Number.isFinite doesn't forcibly convert the parameter to a
number. Only finite values of the type number, result in true.

## How to respond
- Behave EXACTLY as the original \`isFinite\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"number","type":"unknown"}],
};
export function isFinite(input: { number: unknown; prompt?: string }): Promise<boolean>;
export async function isFinite(input: Record<string, unknown>): Promise<unknown> { return runMethod(__isFinite_0_spec, input); }

/** `Number.isInteger` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __isInteger_1_spec: GeneratedMethodSpec = {
  group: 'number',
  methodName: 'isInteger',
  functionId: 'Number.isInteger',
  kind: 'static',
  native: resolveNative('Number.isInteger'),
  receiverKey: "",
  paramOrder: ["number"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Number.isInteger\`.
## Original signature(s)
  Overload 1: (number: unknown) => boolean
## JSDoc
Returns true if the value passed is an integer, false otherwise.

## How to respond
- Behave EXACTLY as the original \`isInteger\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"number","type":"unknown"}],
};
export function isInteger(input: { number: unknown; prompt?: string }): Promise<boolean>;
export async function isInteger(input: Record<string, unknown>): Promise<unknown> { return runMethod(__isInteger_1_spec, input); }

/** `Number.isNaN` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __isNaN_2_spec: GeneratedMethodSpec = {
  group: 'number',
  methodName: 'isNaN',
  functionId: 'Number.isNaN',
  kind: 'static',
  native: resolveNative('Number.isNaN'),
  receiverKey: "",
  paramOrder: ["number"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Number.isNaN\`.
## Original signature(s)
  Overload 1: (number: unknown) => boolean
## JSDoc
Returns a Boolean value that indicates whether a value is the reserved value NaN (not a
number). Unlike the global isNaN(), Number.isNaN() doesn't forcefully convert the parameter
to a number. Only values of the type number, that are also NaN, result in true.

## How to respond
- Behave EXACTLY as the original \`isNaN\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"number","type":"unknown"}],
};
export function isNaN(input: { number: unknown; prompt?: string }): Promise<boolean>;
export async function isNaN(input: Record<string, unknown>): Promise<unknown> { return runMethod(__isNaN_2_spec, input); }

/** `Number.isSafeInteger` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __isSafeInteger_3_spec: GeneratedMethodSpec = {
  group: 'number',
  methodName: 'isSafeInteger',
  functionId: 'Number.isSafeInteger',
  kind: 'static',
  native: resolveNative('Number.isSafeInteger'),
  receiverKey: "",
  paramOrder: ["number"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Number.isSafeInteger\`.
## Original signature(s)
  Overload 1: (number: unknown) => boolean
## JSDoc
Returns true if the value passed is a safe integer.

## How to respond
- Behave EXACTLY as the original \`isSafeInteger\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"number","type":"unknown"}],
};
export function isSafeInteger(input: { number: unknown; prompt?: string }): Promise<boolean>;
export async function isSafeInteger(input: Record<string, unknown>): Promise<unknown> { return runMethod(__isSafeInteger_3_spec, input); }

/** `Number.parseFloat` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __parseFloat_4_spec: GeneratedMethodSpec = {
  group: 'number',
  methodName: 'parseFloat',
  functionId: 'Number.parseFloat',
  kind: 'static',
  native: resolveNative('Number.parseFloat'),
  receiverKey: "",
  paramOrder: ["string"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Number.parseFloat\`.
## Original signature(s)
  Overload 1: (string: string) => number
## JSDoc
Converts a string to a floating-point number.

## How to respond
- Behave EXACTLY as the original \`parseFloat\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"string","type":"string"}],
};
export function parseFloat(input: { string: string; prompt?: string }): Promise<number>;
export async function parseFloat(input: Record<string, unknown>): Promise<unknown> { return runMethod(__parseFloat_4_spec, input); }

/** `Number.parseInt` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __parseInt_5_spec: GeneratedMethodSpec = {
  group: 'number',
  methodName: 'parseInt',
  functionId: 'Number.parseInt',
  kind: 'static',
  native: resolveNative('Number.parseInt'),
  receiverKey: "",
  paramOrder: ["string","radix"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Number.parseInt\`.
## Original signature(s)
  Overload 1: (string: string, radix?: number) => number
## JSDoc
Converts A string to an integer.

## How to respond
- Behave EXACTLY as the original \`parseInt\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"string","type":"string"},{"name":"radix?","type":"number"}],
};
export function parseInt(input: { string: string; radix?: number; prompt?: string }): Promise<number>;
export async function parseInt(input: Record<string, unknown>): Promise<unknown> { return runMethod(__parseInt_5_spec, input); }

/** `Number.prototype.toExponential` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toExponential_6_spec: GeneratedMethodSpec = {
  group: 'number',
  methodName: 'toExponential',
  functionId: 'Number.prototype.toExponential',
  kind: 'instance',
  native: resolveNative('Number.prototype.toExponential'),
  receiverKey: "number",
  paramOrder: ["fractionDigits"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Number.prototype.toExponential\`.
## Original signature(s)
  Overload 1: (fractionDigits?: number) => string
## JSDoc
Returns a string containing a number represented in exponential notation.

## How to respond
- Behave EXACTLY as the original \`toExponential\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"fractionDigits?","type":"number"}],
};
export function toExponential(input: { number: number; fractionDigits?: number; prompt?: string }): Promise<string>;
export async function toExponential(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toExponential_6_spec, input); }

/** `Number.prototype.toFixed` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toFixed_7_spec: GeneratedMethodSpec = {
  group: 'number',
  methodName: 'toFixed',
  functionId: 'Number.prototype.toFixed',
  kind: 'instance',
  native: resolveNative('Number.prototype.toFixed'),
  receiverKey: "number",
  paramOrder: ["fractionDigits"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Number.prototype.toFixed\`.
## Original signature(s)
  Overload 1: (fractionDigits?: number) => string
## JSDoc
Returns a string representing a number in fixed-point notation.

## How to respond
- Behave EXACTLY as the original \`toFixed\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"fractionDigits?","type":"number"}],
};
export function toFixed(input: { number: number; fractionDigits?: number; prompt?: string }): Promise<string>;
export async function toFixed(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toFixed_7_spec, input); }

/** `Number.prototype.toPrecision` - pass an object literal with a `prompt` field to route through the LLM, or omit `prompt` to fall back to the native built-in. */
const __toPrecision_8_spec: GeneratedMethodSpec = {
  group: 'number',
  methodName: 'toPrecision',
  functionId: 'Number.prototype.toPrecision',
  kind: 'instance',
  native: resolveNative('Number.prototype.toPrecision'),
  receiverKey: "number",
  paramOrder: ["precision"],
  variadicKey: "",
  systemPrompt: `You are simulating the JavaScript built-in \`Number.prototype.toPrecision\`.
## Original signature(s)
  Overload 1: (precision?: number) => string
## JSDoc
Returns a string containing a number represented either in exponential or fixed-point notation with a specified number of digits.

## How to respond
- Behave EXACTLY as the original \`toPrecision\` would, but use the user's intent to choose any callback / comparator / transform logic that the original would normally accept as an argument.
- Strictly preserve the original return type and shape.
- Output ONLY the JSON-encoded return value of the function call.
- Do NOT include explanations, prose, comments, or markdown fences.
- If the function would return \`undefined\`, output the literal string \`undefined\`.
- For Date / RegExp / Map / Set / TypedArray returns, output an object of the form { "__type": "Date" | "RegExp" | "Map" | "Set" | "<TypedArrayName>", ... } so the SDK can rehydrate it.`,
  signatureHint: [{"name":"precision?","type":"number"}],
};
export function toPrecision(input: { number: number; precision?: number; prompt?: string }): Promise<string>;
export async function toPrecision(input: Record<string, unknown>): Promise<unknown> { return runMethod(__toPrecision_8_spec, input); }


