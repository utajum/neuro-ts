---
title: filter
description: neuro.iterator.filter wraps Iterator.prototype.filter for AI-augmented JavaScript. Call the original built-in with TypeScript types intact, or pass a prompt to route through GPT / OpenAI. Native fallback when prompt is empty.
sidebar:
  label: filter
---

import PromptCard from '../../../../components/PromptCard.astro';
import { Code } from '@astrojs/starlight/components';


## `neuro.iterator.filter`

Instance method on `Iterator.prototype`.

> Creates an iterator whose values are those from this iterator for which the provided predicate returns true.

## Signatures

```ts
filter(input: { iterator: <receiver>; predicate?: (value: T; index: number) => boolean; prompt?: string }): Promise<IteratorObject<S, undefined, unknown>>
filter(input: { iterator: <receiver>; predicate?: (value: T; index: number) => unknown; prompt?: string }): Promise<IteratorObject<T, undefined, unknown>>
```

The `prompt` field is optional. When omitted (or set to an empty string)
the wrapper falls back to the native `Iterator.prototype.filter` and returns a
resolved Promise without contacting the LLM. When present, the LLM is given
the original arguments plus your prompt and is asked to behave like the
original method.

## Example

<Code
  lang="ts"
  code={atob("")}
  wrap
/>




## System prompt

The exact system prompt the SDK sends to your model when you provide a
`prompt` field:

<PromptCard name="neuro.iterator.filter" />
